{
  if (super.isModified()) {
    myModuleDescriptor.setNamespace(myTextFieldName.getText());
  }
  if (myConfigurableItem instanceof DevKit)   myModuleDependenciesTab.apply();
 else   myRootsTableModel.apply();
}
