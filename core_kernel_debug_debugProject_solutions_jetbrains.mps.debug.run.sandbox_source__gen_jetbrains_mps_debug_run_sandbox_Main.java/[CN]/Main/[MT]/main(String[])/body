{
  new Main();
  new Thread(new Runnable(){
    public void run(){
      int i=0;
      while (true) {
        i++;
      }
    }
  }
).start();
  System.err.println("hello");
  for (int i=0; i < 2; i++) {
    doSomething();
  }
  new Main().foo();
  SNode n=null;
  System.err.println("" + n);
  doSomething();
  doSomething();
  doSomething();
  while (true) {
    System.err.println("hi");
  }
}
