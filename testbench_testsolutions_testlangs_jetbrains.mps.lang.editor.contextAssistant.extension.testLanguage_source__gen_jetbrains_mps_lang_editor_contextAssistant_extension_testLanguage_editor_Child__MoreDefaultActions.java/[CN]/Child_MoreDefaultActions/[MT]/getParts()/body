{
  return Arrays.<MenuPart>asList(new ActionItemMenuPart(){
    @Override protected String getText(    TransformationMenuContext _context){
      return "contributed action";
    }
    @Override protected void execute(    TransformationMenuContext _context){
      if (LOG.isInfoEnabled()) {
        LOG.info("contributed action");
      }
    }
  }
);
}
