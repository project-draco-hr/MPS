{
  try {
    return new ImageIcon(StreamUtil.loadFromStream(URLUtil.openStream(GoToNextChange_Action.class.getResource("nextOccurence.png"))));
  }
 catch (  IOException e) {
    if (log.isWarnEnabled()) {
      log.warn("Couldn't load icon for GoToNextChange",e);
    }
    return null;
  }
}
