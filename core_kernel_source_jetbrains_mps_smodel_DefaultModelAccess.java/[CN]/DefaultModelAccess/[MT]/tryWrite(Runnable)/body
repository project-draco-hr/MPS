{
  if (getWriteLock().tryLock()) {
    try {
      clearRepositoryStateCaches();
      r.run();
    }
  finally {
      getWriteLock().unlock();
    }
    return true;
  }
 else {
    return false;
  }
}
