{
  if (getWriteLock().tryLock()) {
    try {
      clearTransactionCaches();
      r.run();
    }
  finally {
      getWriteLock().unlock();
    }
    return true;
  }
 else {
    return false;
  }
}
