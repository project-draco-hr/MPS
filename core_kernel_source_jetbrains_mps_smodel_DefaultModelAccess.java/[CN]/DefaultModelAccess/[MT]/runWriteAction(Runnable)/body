{
  if (canWrite()) {
    r.run();
    return;
  }
  assertNotWriteFromRead();
  getWriteLock().lock();
  try {
    clearTransactionCaches();
    r.run();
  }
  finally {
    getWriteLock().unlock();
  }
}
