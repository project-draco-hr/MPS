{
  if (currentChar() != '\"') {
    error("");
    return null;
  }
  myOffset++;
  StringBuilder sb=new StringBuilder();
  while (currentChar() != '\"') {
    sb.append(currentChar());
    myOffset++;
  }
  myOffset++;
  return new StringValue(sb.toString());
}
