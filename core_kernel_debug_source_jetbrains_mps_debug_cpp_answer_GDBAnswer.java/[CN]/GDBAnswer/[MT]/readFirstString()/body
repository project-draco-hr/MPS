{
  StringBuilder sb=new StringBuilder();
  while (myOffset < myInput.length()) {
    char c=currentChar();
    if (c == ',') {
      myFirstString=sb.toString();
      myOffset++;
      return;
    }
    sb.append(c);
    myOffset++;
  }
  myFirstString=sb.toString();
}
