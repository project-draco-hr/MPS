{
  if (currentChar() != '[') {
    error("");
    return null;
  }
  myOffset++;
  ListValue listValue=new ListValue();
  while (true) {
    GDBValue value=getValue();
    if (currentChar() == ',') {
      listValue.add(value);
      myOffset++;
    }
 else     if (currentChar() == ']') {
      listValue.add(value);
      myOffset++;
      return listValue;
    }
 else {
      error("");
      return null;
    }
  }
}
