{
  List<IFile> result=new ArrayList<IFile>();
  File[] files=myFile.listFiles();
  if (files == null) {
    return null;
  }
  for (  File f : files) {
    result.add(new FileSystemFile(f));
  }
  return result;
}
