{
  ThreadUtils.runInUIThreadAndWait(new Runnable(){
    public void run(){
      mpsProject.dispose();
      IdeEventQueue.getInstance().flushQueue();
      System.gc();
    }
  }
);
  ThreadUtils.runInUIThreadAndWait(new Runnable(){
    public void run(){
      IdeEventQueue.getInstance().flushQueue();
    }
  }
);
  HELPER.dispose();
}
