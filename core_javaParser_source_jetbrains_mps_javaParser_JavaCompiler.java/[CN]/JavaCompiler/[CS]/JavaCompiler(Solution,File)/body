{
  mySolution=solution;
  mySourceDir=sourceDir;
  myPrefix=solution.getModuleFqName();
  initClassPathItem(solution);
  addSourceFromDirectory(mySourceDir,myPrefix);
  mySolution.getSolutionDescriptor().setOutputPath(mySourceDir.getPath());
  mySolution.save();
}
