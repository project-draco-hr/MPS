{
  if (iter instanceof Set) {
    return (Set<T>)iter;
  }
  if (iter == null) {
    return Collections.emptySet();
  }
  Set<T> result=new HashSet<T>();
  for (  T o : iter) {
    result.add(o);
  }
  return result;
}
