{
  ClassLoaderManager.getInstance().addReloadHandler(new ReloadAdapter(){
    public void unload(){
synchronized (myLock) {
        for (        SNode node : myNodes) {
          node.clearAdapter();
        }
        myNodes.clear();
      }
    }
  }
);
}
