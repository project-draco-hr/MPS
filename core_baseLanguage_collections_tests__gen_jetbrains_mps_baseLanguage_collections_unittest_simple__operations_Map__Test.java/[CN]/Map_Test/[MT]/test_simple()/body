{
  Map<String,String> map=MapSequence.<String,String>fromMap(new HashMap<String,String>());
  MapSequence.<String,String>fromMap(map).put("key1","value1");
  Assert.assertEquals("value1",MapSequence.<String,String>fromMap(map).get("key1"));
  Assert.assertTrue(MapSequence.fromMap(map).containsKey("key1"));
}
