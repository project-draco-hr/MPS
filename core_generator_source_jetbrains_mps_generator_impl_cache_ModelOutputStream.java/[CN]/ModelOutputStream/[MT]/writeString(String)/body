{
  if (s == null) {
    writeByte(0x70);
  }
 else {
    Integer index=stringToIndex.get(s);
    if (index == null) {
      stringToIndex.put(s,myStringIndex++);
      writeByte(0);
      writeUTF(s);
    }
 else {
      writeByte(1);
      writeInt(index);
    }
  }
}
