{
  myUsedNodes.clear();
  SNode var=chooseVar();
  while (var != null) {
    boolean result=solveRelationsForNode(var);
    if (result)     return result;
    myUsedNodes.add(var);
    var=chooseVar();
  }
  return false;
}
