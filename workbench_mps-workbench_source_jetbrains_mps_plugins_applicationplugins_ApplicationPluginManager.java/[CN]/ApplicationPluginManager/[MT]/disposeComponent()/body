{
  super.stopListeningToReload();
  if (!myPluginReloader.getLoadedContributors().isEmpty()) {
    LOG.warn("Some contributor plugins will not be unloaded because of too early component disposal.");
  }
}
