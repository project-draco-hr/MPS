{
  try {
    this.enable(event.getPresentation());
  }
 catch (  Throwable t) {
    if (LOG.isEnabledFor(Level.ERROR)) {
      LOG.error("User's action doUpdate method failed. Action:" + "ShowBookmarks",t);
    }
    this.disable(event.getPresentation());
  }
}
