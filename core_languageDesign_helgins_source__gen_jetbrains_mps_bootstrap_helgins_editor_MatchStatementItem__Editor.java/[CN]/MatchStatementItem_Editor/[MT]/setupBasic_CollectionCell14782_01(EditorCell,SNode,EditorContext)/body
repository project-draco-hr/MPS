{
  editorCell.putUserObject(EditorCell.CELL_ID,node.getId() + "_CollectionCell14782_01");
{
    Style inlineStyle=new Style(){
{
        this.set(StyleAttributes.SELECTABLE,false);
      }
    }
;
    inlineStyle.apply(editorCell);
  }
}
