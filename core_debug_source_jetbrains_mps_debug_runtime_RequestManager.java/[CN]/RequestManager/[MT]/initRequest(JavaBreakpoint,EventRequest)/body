{
  int suspendPolicy=requestor.getSuspendPolicy();
  if (suspendPolicy == EventRequest.SUSPEND_NONE)   suspendPolicy=EventRequest.SUSPEND_ALL;
  req.setSuspendPolicy(suspendPolicy);
  registerRequestInternal(requestor,req);
}
