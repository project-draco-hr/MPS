{
  return new Action[]{getOKAction(),getCancelAction(),new DialogWrapperAction("Apply"){
    @Override protected void doAction(    ActionEvent e){
      BasePropertiesDialog.super.saveChanges();
    }
  }
};
}
