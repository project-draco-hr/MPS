{
  Map<Long,Long> mll=MapSequence.fromMap(new TLongLongHashMapDecorator(new TLongLongHashMap()));
  MapSequence.<Long,Long>fromMap(mll).put(1L,2L);
  Assert.assertSame(2L,MapSequence.<Long,Long>fromMap(mll).get(1L));
}
