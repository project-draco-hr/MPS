{
  this.shell.pack();
  this.shell.open();
  Display display=this.getParent().getDisplay();
  while (!(this.shell.isDisposed())) {
    if (!(display.readAndDispatch())) {
      display.sleep();
    }
  }
}
