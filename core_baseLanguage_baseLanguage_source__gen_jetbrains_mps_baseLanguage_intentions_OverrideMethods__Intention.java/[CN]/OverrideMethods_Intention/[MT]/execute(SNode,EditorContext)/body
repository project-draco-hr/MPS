{
  SwingUtilities.invokeLater(new Runnable(){
    public void run(){
      Project project=editorContext.getOperationContext().getProject();
      new OverrideMethodAction(project,node,editorContext).run();
    }
  }
);
}
