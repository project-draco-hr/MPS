{
  if (obj == this)   return true;
  if (obj instanceof ReferencedNodeContext) {
    ReferencedNodeContext o=(ReferencedNodeContext)obj;
    return EqualUtil.equals(myRole,o.myRole) && EqualUtil.equals(myRefererNode,o.myRefererNode) && EqualUtil.equals(myNode,o.myNode);
  }
 else {
    return false;
  }
}
