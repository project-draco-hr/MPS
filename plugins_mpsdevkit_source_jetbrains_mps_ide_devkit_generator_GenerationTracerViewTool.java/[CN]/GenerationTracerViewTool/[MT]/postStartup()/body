{
  SwingUtilities.invokeLater(new Runnable(){
    public void run(){
      showNoTabsComponent();
      setTracingDataIsAvailable(myTracer.hasTracingData());
      setAvailable(false);
    }
  }
);
}
