{
  if (early) {
    StartupManager.getInstance(getProject()).registerPostStartupActivity(new Runnable(){
      public void run(){
        postStartup();
      }
    }
);
  }
 else {
    postStartup();
  }
}
