{
  return new MoveNodeRefactoringDataCollector<PsiElement,SNode>(){
    @Override public PsiElement beforeMove(    SNode sNode){
      return myPsiProvider.getPsi(sNode);
    }
    @Override public SNode afterMove(    SNode sNode){
      return sNode;
    }
  }
;
}
