{
  return new MoveNodeRefactoringDataCollector<SNodeReference,SNodeReference>(){
    @Override public SNodeReference beforeMove(    SNode sNode){
      return sNode.getReference();
    }
    @Override public SNodeReference afterMove(    SNode sNode){
      return sNode.getReference();
    }
  }
;
}
