{
  return ListSequence.<SNode>fromList(pathToRoot).any(new IWhereFilter<SNode>(){
    public boolean accept(    SNode node){
      return SetSequence.<SNodeId>fromSet(myAddedNodeIds).contains(node.getSNodeId());
    }
  }
);
}
