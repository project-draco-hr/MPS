{
  if (hasNext.unknown()) {
    init();
    moveToNext();
  }
  if (!(hasNext.hasNext())) {
    throw new NoSuchElementException();
  }
  U tmp=next;
  moveToNext();
  if (!(hasNext.hasNext())) {
    destroy();
  }
  return tmp;
}
