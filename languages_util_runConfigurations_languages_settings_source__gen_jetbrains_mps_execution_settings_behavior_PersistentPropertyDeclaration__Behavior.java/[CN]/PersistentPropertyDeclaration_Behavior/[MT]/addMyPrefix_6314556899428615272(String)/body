{
  String prefix="my";
  if (!(name.startsWith(prefix))) {
    if (name.length() > 1) {
      name="my" + name.substring(0,1).toUpperCase() + name.substring(1);
    }
 else {
      name="my" + name;
    }
  }
  return name;
}
