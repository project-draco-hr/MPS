{
  if (!(MapSequence.fromMap(cache).containsKey(target))) {
    if (targetRange.hasTarget(target)) {
      T vars=(MapSequence.fromMap(copyFrom).containsKey(target) ? targetRange.getTarget(target).createParameters(cls,(T)MapSequence.fromMap(copyFrom).get(target)) : targetRange.getTarget(target).createParameters(cls));
      MapSequence.fromMap(cache).put(target,vars);
    }
 else {
      return null;
    }
  }
  return cls.cast(MapSequence.fromMap(cache).get(target));
}
