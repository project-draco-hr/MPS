{
  if (!(MapSequence.fromMap(cache).containsKey(target))) {
    if (targetRange.hasTarget(target)) {
      T vars=(MapSequence.fromMap(copyFrom).containsKey(target) ? targetRange.getTarget(target).createParameters(cls,(T)MapSequence.<ITarget.Name,Object>fromMap(copyFrom).get(target)) : targetRange.getTarget(target).createParameters(cls));
      MapSequence.<ITarget.Name,Object>fromMap(cache).put(target,vars);
    }
 else {
      return null;
    }
  }
  return cls.cast(MapSequence.<ITarget.Name,Object>fromMap(cache).get(target));
}
