{
  if (value == null) {
    return true;
  }
  Iterator<_LeftRight_Enum> constants=_LeftRight_Enum.getConstants().iterator();
  while (constants.hasNext()) {
    _LeftRight_Enum constant=constants.next();
    if (value.equals(constant.getName())) {
      return true;
    }
  }
  return false;
}
