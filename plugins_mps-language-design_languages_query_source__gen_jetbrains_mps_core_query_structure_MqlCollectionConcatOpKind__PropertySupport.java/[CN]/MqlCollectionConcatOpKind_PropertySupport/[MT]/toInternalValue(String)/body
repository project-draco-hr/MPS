{
  if (value == null) {
    return null;
  }
  Iterator<MqlCollectionConcatOpKind> constants=ListSequence.fromList(MqlCollectionConcatOpKind.getConstants()).iterator();
  while (constants.hasNext()) {
    MqlCollectionConcatOpKind constant=constants.next();
    if (value.equals(constant.getName())) {
      return constant.getValueAsString();
    }
  }
  return null;
}
