{
  try {
    String newName=RenameConcept_Action.this.init(_params);
    if (newName == null) {
      return;
    }
    RefactoringContext c=RefactoringContext.createRefactoringContextByName("jetbrains.mps.lang.structure.refactorings.RenameConcept",Arrays.asList("newName"),Arrays.asList(newName),((SNode)MapSequence.fromMap(_params).get("target")),ProjectHelper.toMPSProject(((Project)MapSequence.fromMap(_params).get("project"))));
    new RefactoringFacade().execute(c);
  }
 catch (  Throwable t) {
    LOG.error("User's action execute method failed. Action:" + "RenameConcept",t);
  }
}
