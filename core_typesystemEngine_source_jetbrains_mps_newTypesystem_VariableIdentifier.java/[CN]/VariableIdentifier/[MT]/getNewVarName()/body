{
  String result=myVariableChar + (myVariableIndex == 0 ? "" : "" + myVariableIndex);
  if (myVariableChar == 'z') {
    myVariableIndex++;
    myVariableChar='a';
  }
 else {
    myVariableChar++;
  }
  return result;
}
