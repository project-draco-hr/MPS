{
  return ListSequence.fromList(SLinkOperations.getTargets(node,"conceptLinkDeclaration",true)).isNotEmpty() || ListSequence.fromList(SLinkOperations.getTargets(node,"conceptPropertyDeclaration",true)).isNotEmpty();
}
