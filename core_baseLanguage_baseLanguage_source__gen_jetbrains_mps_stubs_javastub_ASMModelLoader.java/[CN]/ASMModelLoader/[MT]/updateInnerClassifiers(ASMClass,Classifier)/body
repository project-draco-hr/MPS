{
  for (  InnerClassNode cn : ac.getInnerClasses()) {
    if ((cn.access & Opcodes.ACC_PRIVATE) != 0) {
      continue;
    }
    String name=cn.name;
    if (name == null) {
      continue;
    }
    int index=name.lastIndexOf("/");
    if (index != -1) {
      name=name.substring(index + 1);
    }
    getAnyClassifier(name,myModel.getLongName());
  }
}
