{
  return myTraceTypes.isSelected() ? GenerationProcessContext.TRACE_TYPES : myTraceLanguages.isSelected() ? GenerationProcessContext.TRACE_LANGS : myTraceSteps.isSelected() ? GenerationProcessContext.TRACE_STEPS : GenerationProcessContext.TRACE_OFF;
}
