{
  int shift=0;
  int size=0;
  if (usesBraces()) {
    shift=1;
    size=2;
  }
  if (myEditorCells.length > size) {
    return myEditorCells[myEditorCells.length - (1 + shift)];
  }
  return null;
}
