{
  int shift=0;
  int size=0;
  if (usesBraces()) {
    shift=1;
    size=2;
  }
  if (myEditorCellsWrapper.size() > size) {
    return myEditorCellsWrapper.get(myEditorCellsWrapper.size() - (1 + shift));
  }
  return null;
}
