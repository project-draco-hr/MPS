{
  myData.add(vertex);
  for (  IVertex next : vertex.getNexts()) {
    if (next == null) {
      LOG.error("Next of vertex " + vertex + " is null.");
    }
 else     if (!myData.contains(next)) {
      add((V)next);
    }
  }
}
