{
  if (myCachedExecutable == null) {
    myCachedExecutable=Collections.<IntentionExecutable>singletonList(new ToggleExceptionSection_Intention.IntentionImplementation());
  }
  return myCachedExecutable;
}
