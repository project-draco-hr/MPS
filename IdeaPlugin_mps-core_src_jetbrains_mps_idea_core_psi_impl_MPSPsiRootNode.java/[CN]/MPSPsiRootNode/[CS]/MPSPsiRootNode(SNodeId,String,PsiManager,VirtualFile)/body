{
  super(manager);
  myNodeId=nodeId;
  myName=name;
  myViewProvider=virtualFile == null ? new SingleRootFileViewProvider(manager,new LightVirtualFile(),false) : new SingleRootFileViewProvider(manager,virtualFile);
  separateFile=true;
}
