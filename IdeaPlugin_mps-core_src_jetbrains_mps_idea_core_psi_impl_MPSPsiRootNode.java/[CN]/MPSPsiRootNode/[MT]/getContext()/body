{
  VirtualFile vFile=mySeparateFile != null ? mySeparateFile : myModel.getSourceVirtualFile();
  return vFile != null && vFile.isValid() ? PsiManager.getInstance(getProject()).findFile(vFile) : null;
}
