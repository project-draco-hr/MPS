{
  final Entry<K,V> top=_top;
  if (top != e) {
    unlink(e);
    _top=e;
    e.setNext(top);
    if (top != null) {
      top.setPrevious(e);
    }
    if (_back == null) {
      _back=e;
    }
  }
}
