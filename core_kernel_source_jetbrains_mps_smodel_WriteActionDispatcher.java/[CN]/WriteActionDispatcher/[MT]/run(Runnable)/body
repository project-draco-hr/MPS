{
  if (myWriteActionLevel == 0)   onActionStarted();
  ++myWriteActionLevel;
  try {
    r.run();
  }
  finally {
    --myWriteActionLevel;
    if (myWriteActionLevel == 0)     onActionFinished();
  }
}
