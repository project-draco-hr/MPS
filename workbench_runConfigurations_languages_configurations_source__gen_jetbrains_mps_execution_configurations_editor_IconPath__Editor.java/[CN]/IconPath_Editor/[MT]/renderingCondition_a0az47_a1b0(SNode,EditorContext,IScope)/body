{
  String path=null;
  IModule module=EditorUtil.findAnchorModule(node);
  if (module != null) {
    path=MacrosUtil.expandPath(SPropertyOperations.getString(node,"path"),module.getModuleFqName());
  }
  return path != null && FileSystem.getInstance().getFileByPath(path).exists();
}
