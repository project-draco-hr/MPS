{
  try {
    return languageRuntime.getAspect(EditorAspectDescriptor.class);
  }
 catch (  NoClassDefFoundError error) {
    LOG.error("Failed to get editor aspect descriptor for language: " + languageRuntime.getNamespace() + ". Editors of this language will not be taken into account",error);
    return null;
  }
}
