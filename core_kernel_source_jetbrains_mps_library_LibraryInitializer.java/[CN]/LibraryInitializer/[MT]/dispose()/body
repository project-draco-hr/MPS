{
  ModelAccess.instance().runWriteAction(new Runnable(){
    public void run(){
      for (      SLibrary lib : myLibraries) {
        lib.dispose();
      }
      myLibraries.clear();
    }
  }
);
  INSTANCE=null;
}
