{
  try {
    Set<IModule> modules=CollectionUtil.set(((IModule)MapSequence.<String,Object>fromMap(_params).get("module")));
    ProgressManager.getInstance().run(new DefaultMakeTask(((Project)MapSequence.<String,Object>fromMap(_params).get("project")),"Compiling",modules,false));
  }
 catch (  Throwable t) {
    if (log.isErrorEnabled()) {
      log.error("User's action execute method failed. Action:" + "MakeModule",t);
    }
  }
}
