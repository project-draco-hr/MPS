{
  DynamicReference outputReference=new DynamicReference(prototype.getRole(),outputNode,targetModelRef,prototype.getResolveInfo());
  outputReference.setOrigin(prototype.getOrigin());
  return outputReference;
}
