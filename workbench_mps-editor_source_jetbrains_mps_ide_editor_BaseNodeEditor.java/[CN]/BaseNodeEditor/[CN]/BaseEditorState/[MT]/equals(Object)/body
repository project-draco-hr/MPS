{
  if (!(obj instanceof BaseEditorState)) {
    return false;
  }
  BaseEditorState state=(BaseEditorState)obj;
  return EqualUtil.equals(state.memento,memento) && EqualUtil.equals(state.inspectorMemento,inspectorMemento) && state.isEditorFocused == isEditorFocused && state.isInspectorFocused == isInspectorFocused;
}
