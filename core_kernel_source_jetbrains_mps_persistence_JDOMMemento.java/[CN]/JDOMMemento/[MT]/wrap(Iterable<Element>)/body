{
  return new Iterable<Memento>(){
    @Override public Iterator<Memento> iterator(){
      return new TranslatingIterator<Element,Memento>(list.iterator()){
        @Override protected Memento translate(        Element node){
          return wrap(node);
        }
      }
;
    }
  }
;
}
