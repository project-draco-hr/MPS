{
  try {
    return new ImageIcon(StreamUtil.loadFromStream(URLUtil.openStream(StepOut_Action.class.getResource("stepOut.png"))));
  }
 catch (  IOException e) {
    if (log.isWarnEnabled()) {
      log.warn("Couldn't load icon for StepOut",e);
    }
    return null;
  }
}
