{
  if ((file == null) || (ProjectLevelVcsManager.getInstance(myProject).getVcsRootFor(file) == null)) {
    setErrorMessage("File to patch found outside content root: " + name);
    return false;
  }
  return true;
}
