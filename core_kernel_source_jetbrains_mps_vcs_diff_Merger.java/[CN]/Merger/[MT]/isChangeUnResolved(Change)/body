{
  if (myPreviewMode) {
    return myConflictingChanges.contains(ch) && myUnresolved.contains(ch);
  }
 else {
    return myConflictingChanges.contains(ch) && myUnresolved.contains(ch) && myBaseRepoChange.contains(ch);
  }
}
