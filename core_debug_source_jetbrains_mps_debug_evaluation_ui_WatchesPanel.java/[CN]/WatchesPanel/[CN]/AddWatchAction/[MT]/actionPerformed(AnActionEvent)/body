{
  IEvaluationProvider evaluationProvider=DebugActionsUtil.getEvaluationProvider(e);
  if (evaluationProvider != null) {
    ((EvaluationProvider)evaluationProvider).createWatch(MPSDataKeys.PROJECT.getData(e.getDataContext()));
  }
}
