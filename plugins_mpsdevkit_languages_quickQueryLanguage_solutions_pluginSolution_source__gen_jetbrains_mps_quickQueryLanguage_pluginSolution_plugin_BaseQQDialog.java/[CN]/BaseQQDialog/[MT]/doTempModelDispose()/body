{
  myModelAccess.runWriteInEDT(new Runnable(){
    public void run(){
      if (myTempModel != null) {
        TemporaryModels.getInstance().dispose(myTempModel);
        myTempModel=null;
      }
    }
  }
);
}
