{
  if (myDisposed) {
    return;
  }
  myDisposed=true;
  myProject.getRepository().getModelAccess().runWriteInEDT(new Runnable(){
    public void run(){
      myEditor.disposeEditor();
    }
  }
);
  super.dispose();
}
