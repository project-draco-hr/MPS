{
  MockWidget.PropHandlers hdlrs=(MapSequence.fromMap(this.propHandlers).containsKey(key) ? MapSequence.fromMap(this.propHandlers).get(key) : new MockWidget.PropHandlers(key));
  MapSequence.fromMap(this.propHandlers).put(key,hdlrs);
  hdlrs.add(hdlr);
}
