{
{
    ActionGroup gTo=this.getGroup(ProjectPaneModelActions_ActionGroup.ID);
    ActionGroup gWhat=this.getGroup(FindModelUsages_ActionGroup.ID);
    if (gTo == null || gWhat == null) {
      return;
    }
    gTo.addEveryFrom(gWhat,owner);
  }
}
