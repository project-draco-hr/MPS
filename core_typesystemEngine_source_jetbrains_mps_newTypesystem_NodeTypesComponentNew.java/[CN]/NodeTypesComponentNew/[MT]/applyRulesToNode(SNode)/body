{
  Set<InferenceRule_Runtime> newRules=null;
  boolean result=false;
  if (newRules != null) {
    for (    InferenceRule_Runtime rule : newRules) {
      applyRuleToNode(node,rule);
    }
  }
  return result;
}
