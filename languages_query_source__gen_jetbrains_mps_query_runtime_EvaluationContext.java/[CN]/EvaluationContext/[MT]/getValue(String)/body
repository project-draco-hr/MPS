{
  if (parent != null && (vars == null || !(MapSequence.fromMap(vars).containsKey(varname)))) {
    return parent.getValue(varname);
  }
  return (vars != null ? MapSequence.<String,Object>fromMap(vars).get(varname) : null);
}
