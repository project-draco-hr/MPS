{
  ThreadReference thread=myThreadReference;
  return thread.name() + " (" + thread.referenceType().name()+ " from group "+ thread.threadGroup().name()+ ") : "+ getThreadStatusText(thread.status());
}
