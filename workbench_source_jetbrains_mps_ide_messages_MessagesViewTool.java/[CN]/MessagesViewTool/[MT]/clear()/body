{
  ThreadUtils.runInUIThreadNoWait(new Runnable(){
    public void run(){
      myModel.clear();
      myMessages.clear();
      myList.setFixedCellWidth(myList.getWidth());
      updateActions();
    }
  }
);
}
