{
  if (hasErrors() || hasWarnings() || hasInfo()) {
    if (getToolWindow() == null)     return;
    getToolWindow().setTitle(" (" + getHeaderPart(myErrors,"error") + "/"+ getHeaderPart(myWarnings,"warning")+ "/"+ getHeaderPart(myInfos,"info")+ ")");
  }
}
