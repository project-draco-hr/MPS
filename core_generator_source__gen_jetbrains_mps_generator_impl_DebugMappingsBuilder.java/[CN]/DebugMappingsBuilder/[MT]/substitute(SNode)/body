{
  SModel m=n.getModel();
  if (mySubstModels.containsKey(m)) {
    SModel t=mySubstModels.get(m);
    SNode tn=t.getNode(n.getNodeId());
    if (tn != null) {
      return tn;
    }
  }
  return n;
}
