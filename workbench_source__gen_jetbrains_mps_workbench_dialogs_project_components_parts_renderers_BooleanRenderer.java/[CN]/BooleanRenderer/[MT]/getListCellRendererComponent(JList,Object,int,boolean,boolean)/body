{
  this.myCheckBox.setSelected((value == null ? false : (Boolean)value));
  this.myCheckBox.setBackground((isSelected ? list.getSelectionBackground() : list.getBackground()));
  this.myCheckBox.setForeground((isSelected ? list.getSelectionForeground() : list.getForeground()));
  return this.myCheckBox;
}
