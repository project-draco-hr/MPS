{
  for (  PrintWriter w : writerMap.values()) {
    try {
      w.close();
    }
 catch (    Exception e) {
      System.err.println("cannot close stream: " + e.getMessage());
    }
  }
  writerMap=null;
}
