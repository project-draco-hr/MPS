{
  boolean changed=false;
synchronized (this) {
    LOG.assertLog(frame == null || frame.thread() == myThread);
    if (myStackFrame != frame) {
      myStackFrame=frame;
      changed=true;
    }
  }
  if (changed) {
    fireStateChanged();
  }
}
