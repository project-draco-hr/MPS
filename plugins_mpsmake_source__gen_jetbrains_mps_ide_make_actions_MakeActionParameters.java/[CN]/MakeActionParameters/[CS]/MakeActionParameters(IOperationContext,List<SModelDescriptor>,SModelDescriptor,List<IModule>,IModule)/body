{
  this.context=context;
  this.models=(models != null ? ListSequence.<SModelDescriptor>fromListWithValues(new ArrayList<SModelDescriptor>(),models) : null);
  this.cmodel=cmodel;
  this.modules=(modules != null ? ListSequence.<IModule>fromListWithValues(new ArrayList<IModule>(),modules) : null);
  this.cmodule=cmodule;
}
