{
  if (value == null) {
    return null;
  }
  Iterator<SNotationQuantifierKind> constants=ListSequence.fromList(SNotationQuantifierKind.getConstants()).iterator();
  while (constants.hasNext()) {
    SNotationQuantifierKind constant=constants.next();
    if (value.equals(constant.getName())) {
      return constant.getValueAsString();
    }
  }
  return null;
}
