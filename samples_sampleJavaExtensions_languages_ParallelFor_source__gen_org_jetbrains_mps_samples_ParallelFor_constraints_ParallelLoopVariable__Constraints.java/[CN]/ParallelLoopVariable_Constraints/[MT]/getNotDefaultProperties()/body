{
  Map<String,PropertyConstraintsDescriptor> properties=new HashMap();
  properties.put("isFinal",new BasePropertyConstraintsDescriptor("isFinal",this){
    @Override public boolean hasOwnValidator(){
      return true;
    }
    @Override public boolean validateValue(    SNode node,    String propertyValue,    IScope scope){
      String propertyName="isFinal";
      return (SPropertyOperations.getBoolean(propertyValue));
    }
  }
);
  return properties;
}
