{
  LOG.warning("Model=" + getReference().getSModelFqName() + ", file ts="+ getSource().getTimestamp()+ ", model ts="+ getSourceTimestamp(),new Throwable());
  DiskMemoryConflictResolver.getResolver().resolveDiskMemoryConflict(getSource(),this,this);
}
