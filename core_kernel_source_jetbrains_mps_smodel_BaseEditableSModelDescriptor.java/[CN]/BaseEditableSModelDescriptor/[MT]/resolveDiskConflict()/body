{
  LOG.warning("Model=" + getSModel().getReference().getSModelFqName() + ", file ts="+ getSource().getTimestamp()+ ", model ts="+ getSourceTimestamp(),new Throwable());
  DiskMemoryConflictResolver.getResolver().resolveDiskMemoryConflict(getSource(),getSModel(),this);
}
