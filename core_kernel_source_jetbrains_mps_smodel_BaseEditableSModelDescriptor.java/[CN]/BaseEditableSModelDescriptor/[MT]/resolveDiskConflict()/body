{
  LOG.warning("Model=" + getSModel().getSModelReference().getSModelFqName() + ", file ts="+ getSource().getTimestamp()+ ", model ts="+ getSourceTimestamp(),new Throwable());
  DiskMemoryConflictResolver.getResolver().resolveDiskMemoryConflict(getSource().getFile(),getSModel(),this);
}
