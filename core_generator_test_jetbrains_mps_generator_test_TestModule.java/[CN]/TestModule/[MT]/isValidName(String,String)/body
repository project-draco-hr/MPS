{
  SModelFqName sModelFqName=new SModelFqName(longName,stereotype);
  return SModelRepository.getInstance().getModelDescriptor(sModelFqName.toString()) == null && !myModels.containsKey(sModelFqName.toString());
}
