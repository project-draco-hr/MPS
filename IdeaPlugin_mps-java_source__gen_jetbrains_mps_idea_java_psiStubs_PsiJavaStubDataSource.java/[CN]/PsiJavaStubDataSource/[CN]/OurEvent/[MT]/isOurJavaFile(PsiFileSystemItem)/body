{
  if (!(fsItem instanceof PsiJavaFile)) {
    return false;
  }
  if (!(fsItem.isValid())) {
    return false;
  }
  if (!(myDirectory.equals(fsItem.getParent()))) {
    return false;
  }
  return true;
}
