{
  StringBuilder sb=new StringBuilder();
  while (myOffset < myInput.length()) {
    char c=currentChar();
    if (c == ',') {
      myOffset++;
      return sb.toString();
    }
    sb.append(c);
    myOffset++;
  }
  return sb.toString();
}
