{
  super("Go To",ID);
  this.setIsInternal(false);
  this.setPopup(true);
  try {
    GoToEditorPopupAddition_ActionGroup.this.addAction("jetbrains.mps.ide.actions.GoByCurrentReference_Action");
{
      LabelledAnchor action=new LabelledAnchor(GoToEditorPopupAddition_ActionGroup.LABEL_ID_other);
      ActionManagerEx manager=ActionManagerEx.getInstanceEx();
      manager.registerAction(action.getId(),action,PluginId.getId("jetbrains.mps.ide"));
      GoToEditorPopupAddition_ActionGroup.this.addAction(action);
    }
{
      LabelledAnchor action=new LabelledAnchor(GoToEditorPopupAddition_ActionGroup.LABEL_ID_refs);
      ActionManagerEx manager=ActionManagerEx.getInstanceEx();
      manager.registerAction(action.getId(),action,PluginId.getId("jetbrains.mps.ide"));
      GoToEditorPopupAddition_ActionGroup.this.addAction(action);
    }
  }
 catch (  Throwable t) {
    LOG.error("User group error",t);
  }
}
