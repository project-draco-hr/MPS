{
  return (List<MyEntry<K,V>>)(List)new ArrayWrapper<MyEntry>(){
    @Override protected MyEntry[] getArray(){
      return myEntries;
    }
    @Override protected void setArray(    MyEntry[] newArray){
      myEntries=newArray;
    }
    @Override protected MyEntry[] newArray(    int size){
      return new MyEntry[size];
    }
  }
;
}
