{
  while (cell != null) {
    List<HighlighterMessage> messages=cell.getMessages(HighlighterMessage.class);
    if (!messages.isEmpty()) {
      return messages.get(0);
    }
    cell=cell.getParent();
  }
  return null;
}
