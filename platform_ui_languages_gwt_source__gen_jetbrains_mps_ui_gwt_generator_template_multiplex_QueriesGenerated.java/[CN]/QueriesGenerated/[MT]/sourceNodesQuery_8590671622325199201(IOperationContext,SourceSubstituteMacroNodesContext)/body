{
  return ListSequence.fromList(SLinkOperations.getTargets(_context.getNode(),"annotation",true)).where(new IWhereFilter<SNode>(){
    public boolean accept(    SNode ann){
      return !(VariantsUtil.isGenerateVariantsAnn(ann));
    }
  }
);
}
