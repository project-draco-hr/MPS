{
  Set<ModuleReference> set=new HashSet<ModuleReference>();
  for (  DependenciesTableItem<?> tableItem : myTableItems)   if (tableItem.getItem() instanceof ModuleReference && tableItem.getRole() == DependenciesTableItemRole.RUNTIME)   set.add((ModuleReference)tableItem.getItem());
  return set;
}
