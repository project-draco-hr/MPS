{
  if (version != null) {
    setSModelId(model,version);
  }
  SModule module=new DiffTemporaryModule(as_3t8mfw_a0a0a0b0h(model,SModelBase.class).getSModelInternal(),(version == null ? "temporary" : version),project);
  MPSModuleRepository.getInstance().registerModule(module,project);
  ((SModuleBase)module).registerModel((SModelBase)model);
}
