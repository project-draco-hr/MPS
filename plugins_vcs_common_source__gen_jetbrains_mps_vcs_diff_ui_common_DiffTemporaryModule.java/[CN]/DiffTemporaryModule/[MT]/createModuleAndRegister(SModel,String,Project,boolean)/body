{
  if (version != null) {
    setSModelId(model,version);
  }
  createModuleForModel(model,(version == null ? "temporary" : version),project,mergeResultModel);
  registerModel(model.getModelDescriptor(),project);
}
