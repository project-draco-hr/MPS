{
  if (version != null) {
    setSModelId(model,version);
  }
  createModuleForModel(model,(version == null ? "module" : version),project,mergeResultModel);
  registerModel(model.getModelDescriptor(),project);
}
