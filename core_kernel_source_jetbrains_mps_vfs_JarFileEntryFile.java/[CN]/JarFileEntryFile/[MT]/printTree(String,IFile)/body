{
  if (file.isFile()) {
    System.out.println(indent + file.getName());
  }
 else {
    System.out.println(indent + file.getName() + "/");
    for (    IFile f : file.list()) {
      printTree("  " + indent,f);
    }
  }
}
