{
  String key=uid + "#" + id;
  if (myMap.containsKey(key)) {
    myMap.remove(key);
    myUIDToKeys.get(uid).remove(key);
    if (myUIDToKeys.isEmpty()) {
      myUIDToKeys.remove(uid);
    }
  }
}
