{
  super(debugSession);
  myDebugSession=debugSession;
  myContext=context;
  if (context == null) {
    myThread=null;
    myStackFrame=null;
  }
 else {
    myThread=findThread();
    LOG.assertLog(myThread != null);
    myStackFrame=findStackFrame();
  }
}
