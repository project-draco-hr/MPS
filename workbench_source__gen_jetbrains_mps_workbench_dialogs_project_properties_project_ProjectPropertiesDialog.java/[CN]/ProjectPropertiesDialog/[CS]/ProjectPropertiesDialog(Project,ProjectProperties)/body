{
  super(project.getName() + " Properties",project.getComponent(MPSProjectHolder.class).getMPSProject().createOperationContext());
  this.myProject=project;
  this.myProperties=properties;
  this.initUI();
}
