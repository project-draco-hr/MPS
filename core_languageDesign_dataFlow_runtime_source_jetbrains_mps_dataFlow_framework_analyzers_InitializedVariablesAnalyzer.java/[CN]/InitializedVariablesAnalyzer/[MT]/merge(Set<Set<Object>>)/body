{
  Set<Object> result=new HashSet<Object>();
  boolean first=true;
  for (  Set<Object> item : input) {
    if (first) {
      result.addAll(item);
      first=false;
    }
    result.retainAll(item);
  }
  return result;
}
