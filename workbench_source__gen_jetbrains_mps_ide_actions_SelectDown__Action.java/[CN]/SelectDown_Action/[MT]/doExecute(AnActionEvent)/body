{
  try {
    SelectDown_Action.this.editorCell.executeAction(CellActionType.SELECT_DOWN);
  }
 catch (  Throwable t) {
    if (log.isErrorEnabled()) {
      log.error("User's action execute method failed. Action:" + "SelectDown",t);
    }
  }
}
