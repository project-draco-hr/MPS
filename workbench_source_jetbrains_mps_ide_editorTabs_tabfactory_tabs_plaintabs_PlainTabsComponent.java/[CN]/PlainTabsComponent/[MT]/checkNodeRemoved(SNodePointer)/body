{
  if (myBaseNode.equals(node))   return false;
  for (  PlainEditorTab tab : myRealTabs) {
    if (new SNodePointer(tab.getNode()).equals(node)) {
      onNodeChange(myBaseNode.getNode());
      return true;
    }
  }
  return false;
}
