{
  if (!myFacetTabs.containsKey(moduleFacet.getFacetType())) {
    return null;
  }
  @SuppressWarnings("unchecked") TabFactory<SModuleFacet> tabFactory=myFacetTabs.get(moduleFacet.getFacetType());
  return tabFactory.getTab(moduleFacet);
}
