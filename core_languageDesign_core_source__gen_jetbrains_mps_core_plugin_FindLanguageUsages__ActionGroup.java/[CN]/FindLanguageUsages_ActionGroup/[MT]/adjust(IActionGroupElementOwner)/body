{
{
    ActionGroup gTo=this.getGroup(LanguageActions_ActionGroup.ID);
    ActionGroup gWhat=this.getGroup(FindLanguageUsages_ActionGroup.ID);
    if (gTo == null || gWhat == null) {
      return;
    }
    gTo.addEveryFrom(gWhat,owner);
  }
}
