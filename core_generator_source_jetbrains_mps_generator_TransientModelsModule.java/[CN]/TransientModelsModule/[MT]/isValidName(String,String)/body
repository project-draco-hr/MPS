{
  String modelName=stereotype == null ? longName : longName + "@" + stereotype;
  return SModelRepository.getInstance().getModelDescriptor(SModelFqName.fromString(modelName)) == null && !myModels.containsKey(modelName);
}
