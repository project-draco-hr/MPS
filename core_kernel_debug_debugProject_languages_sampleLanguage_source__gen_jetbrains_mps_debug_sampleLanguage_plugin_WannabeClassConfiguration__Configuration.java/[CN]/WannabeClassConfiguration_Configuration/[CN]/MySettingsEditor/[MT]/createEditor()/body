{
  try {
    this.myComponent=new WannabeClassConfiguration_Editor();
    return this.myComponent;
  }
 catch (  Throwable t) {
    Logger.getLogger(WannabeClassConfiguration_Configuration.class).error(t);
  }
  return new JLabel("Error during configuration editor initialization.");
}
