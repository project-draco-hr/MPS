{
  if (myRightToLeft) {
    recalculateIconRenderersWidth();
    recalculateTextColumnWidth();
  }
 else {
    recalculateIconRenderersWidth();
    updateSeparatorLinePosition();
  }
  updatePreferredSize();
}
