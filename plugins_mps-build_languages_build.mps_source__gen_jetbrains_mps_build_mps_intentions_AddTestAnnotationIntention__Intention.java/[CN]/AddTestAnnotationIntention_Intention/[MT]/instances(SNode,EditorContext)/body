{
  if (myCachedExecutable == null) {
    myCachedExecutable=Collections.<IntentionExecutable>singletonList(new AddTestAnnotationIntention_Intention.IntentionImplementation());
  }
  return myCachedExecutable;
}
