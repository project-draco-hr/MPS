{
  commitNextIterator();
  while (!myIterators.isEmpty()) {
    Iterator<T> nextIterator=myIterators.peek();
    if (nextIterator.hasNext()) {
      return nextIterator;
    }
    myIterators.remove();
  }
  return null;
}
