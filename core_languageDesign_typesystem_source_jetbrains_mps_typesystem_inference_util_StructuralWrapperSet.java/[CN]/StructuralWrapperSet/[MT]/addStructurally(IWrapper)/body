{
  if (wrapper == null) {
    return false;
  }
  IWrapperWrapper w=new IWrapperWrapper(wrapper);
  if (!myMWrapperToMWrapper.containsKey(w)) {
    addWrapper(w);
    return true;
  }
  return false;
}
