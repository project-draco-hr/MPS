{
  Money result=new Money(0,currency);
  for (  Entry each : entries) {
    if (period.includes(each.getDate()) && each.getAmount().isNegative())     result=result.add(each.getAmount());
  }
  return result;
}
