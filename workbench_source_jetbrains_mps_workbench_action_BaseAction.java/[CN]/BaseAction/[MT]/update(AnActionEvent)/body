{
  super.update(e);
  ModelAccess.instance().runReadAction(new Runnable(){
    public void run(){
      if (!fillFieldsIfNecessary(e)) {
        disable(e.getPresentation());
        return;
      }
      doUpdate(e);
    }
  }
);
}
