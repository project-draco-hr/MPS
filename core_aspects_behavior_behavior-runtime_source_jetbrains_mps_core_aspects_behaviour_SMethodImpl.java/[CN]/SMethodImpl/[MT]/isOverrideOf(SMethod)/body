{
  if (!isVirtual() || !another.isVirtual()) {
    throw new IllegalArgumentException("Methods must be virtual");
  }
  return getId().equals(another.getId()) && getConcept().isSubConceptOf(another.getConcept());
}
