{
  Set<Library> result=new HashSet<Library>();
  if (InternalFlag.isInternalMode()) {
    result.add(new Library("mps.workbench"){
      @Override @NotNull public String getPath(){
        return WorkbenchPathManager.getWorkbenchPath();
      }
    }
);
  }
  return result;
}
