{
  if (this.myWildcard == '+') {
    this.myWildcard='=';
    return new ASMExtendsType(type);
  }
  if (this.myWildcard == '-') {
    this.myWildcard='=';
    return new ASMSuperType(type);
  }
  return type;
}
