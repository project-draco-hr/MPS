{
  if ((SLinkOperations.getTarget(node,"type",true) == null)) {
    SNodeOperations.replaceWithAnother(node,SLinkOperations.getTarget(node,"expression",true));
  }
 else {
    SLinkOperations.setTarget(node,"type",null,true);
  }
}
