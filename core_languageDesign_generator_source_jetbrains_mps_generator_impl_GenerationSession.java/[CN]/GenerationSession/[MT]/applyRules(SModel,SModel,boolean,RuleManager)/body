{
  TemplateGenerator tg=myIsStrict && myParallelGeneration && !mySessionContext.getGenerationTracer().isTracing()&& ParallelTemplateGenerator.PARALLELING_ENABLED ? new ParallelTemplateGenerator(mySessionContext,myProgressMonitor,myLogger,ruleManager,currentInputModel,currentOutputModel,myIsStrict,ttrace) : new TemplateGenerator(mySessionContext,myProgressMonitor,myLogger,ruleManager,currentInputModel,currentOutputModel,myIsStrict,ttrace);
  return tg.apply(isPrimary);
}
