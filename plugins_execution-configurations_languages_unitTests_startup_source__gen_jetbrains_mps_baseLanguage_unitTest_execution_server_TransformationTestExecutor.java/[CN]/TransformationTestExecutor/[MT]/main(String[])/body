{
  TransformationTestExecutor executor=new TransformationTestExecutor(args);
  try {
    executor.init();
    executor.execute();
  }
 catch (  Throwable t) {
    t.printStackTrace(System.err);
    cleanUpAndExit(1);
  }
  cleanUpAndExit(0);
}
