{
  JFrame jf=new JFrame("UI Demo");
  jf.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
  JComponent cmp=(JComponent)this.createComponent(null);
  cmp.setOpaque(true);
  jf.setContentPane(cmp);
  jf.pack();
  jf.setSize(800,600);
  jf.setVisible(true);
}
