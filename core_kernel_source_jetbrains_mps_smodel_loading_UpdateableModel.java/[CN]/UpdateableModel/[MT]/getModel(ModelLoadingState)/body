{
  if (state == null)   return myModel;
  if (!ModelAccess.instance().canWrite()) {
synchronized (this) {
      ensureLoadedTo(state);
    }
  }
 else {
    ensureLoadedTo(state);
  }
  return myModel;
}
