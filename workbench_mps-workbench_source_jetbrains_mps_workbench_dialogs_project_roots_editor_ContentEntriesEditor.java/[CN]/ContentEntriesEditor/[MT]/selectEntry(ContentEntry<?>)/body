{
  if (entry != null && entry.equals(myFocucedContentEntry))   return;
  if (myFocucedContentEntry != null)   myFocucedContentEntry.setFocuced(false);
  entry.setFocuced(true);
  myEditorPanel.removeAll();
  myEditorPanel.add(entry.getEditor().getComponent());
  myFocucedContentEntry=entry;
}
