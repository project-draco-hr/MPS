{
  Set<ModelRootDescriptor> descriptorSet=new HashSet<ModelRootDescriptor>();
  for (  ContentEntry entry : myContentEntries)   descriptorSet.add(((DefaultModelRootEntry)entry).getEntry());
  return descriptorSet;
}
