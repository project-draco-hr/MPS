{
  try {
    this.enable(event.getPresentation());
  }
 catch (  Throwable t) {
    LOG.error("User's action doUpdate method failed. Action:" + "SetBookmark8",t);
    this.disable(event.getPresentation());
  }
}
