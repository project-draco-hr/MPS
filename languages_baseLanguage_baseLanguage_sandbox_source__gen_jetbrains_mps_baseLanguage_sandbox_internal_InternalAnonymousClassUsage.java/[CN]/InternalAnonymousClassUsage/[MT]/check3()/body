{
  (new Readable(){
    public int read(    CharBuffer cb) throws IOException {
      if (cb == null) {
        throw new IOException();
      }
      return 0;
    }
  }
).read(CharBuffer.wrap("aa"));
}
