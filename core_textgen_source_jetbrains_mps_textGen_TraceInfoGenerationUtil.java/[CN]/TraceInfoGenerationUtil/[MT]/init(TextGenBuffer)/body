{
  final TraceInfoCollector rv=new TraceInfoCollector(){
    @Override protected void updatePositions(    PositionInfo pi,    TextMark positionMarker){
    }
  }
;
  buffer.putUserObject(TraceInfoCollector.class,rv);
  return rv;
}
