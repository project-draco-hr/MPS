{
  getProjectView().changeView(getId());
  if (myRunReadAction) {
    ModelAccess.instance().runReadAction(new Runnable(){
      public void run(){
        doOnPaneActivation();
      }
    }
);
  }
 else {
    doOnPaneActivation();
  }
}
