{
  myErrorReadingThread=new Thread(new Runnable(){
    @Override public void run(){
      Scanner s=new Scanner(is);
      while (s.hasNextLine()) {
        logError(s.nextLine());
      }
    }
  }
);
}
