{
  assert myAdded;
  try {
    onRemove();
  }
 catch (  Throwable t) {
    LOG.error(t);
  }
  myAdded=false;
  if (!isInitialized()) {
    return;
  }
  for (  MPSTreeNode node : this) {
    node.removeThisAndChildren();
  }
}
