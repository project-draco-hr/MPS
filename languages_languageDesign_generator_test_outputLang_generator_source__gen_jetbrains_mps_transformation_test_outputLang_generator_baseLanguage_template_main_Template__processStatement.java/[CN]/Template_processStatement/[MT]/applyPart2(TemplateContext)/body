{
  final TemplateExecutionEnvironment environment=context.getEnvironment();
  Collection<SNode> tlist1=null;
  tlist1=new Template_aaaaa(((SNode)context.getVariable("expr"))).apply(environment,context);
  return tlist1;
}
