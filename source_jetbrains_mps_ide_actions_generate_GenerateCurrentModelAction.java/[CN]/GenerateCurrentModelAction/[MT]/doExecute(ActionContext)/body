{
  context.get(GeneratorManager.class).generateModelsWithProgressWindow(CollectionUtil.asList(context.getModel().getSModel()),BootstrapLanguages.getInstance().getBaseLanguage(),context.getOperationContext(),getGenerationType(),IGenerationScript.DEFAULT,true);
}
