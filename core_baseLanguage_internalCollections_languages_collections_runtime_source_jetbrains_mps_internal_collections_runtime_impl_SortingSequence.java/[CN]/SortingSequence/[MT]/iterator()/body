{
  List<U> sortedInput=inputSortedWithSelector();
  return ascending ? new UnmodifiableIterator<U>(sortedInput.listIterator()) : new UnmodifiableReverseIterator<U>(sortedInput.listIterator(sortedInput.size()));
}
