{
  if (parent != null && child != null) {
    SNode childParent=child.getParent();
    if (childParent != null) {
      childParent.removeChild(child);
    }
    SNodeOperations.insertChild(parent,role,child,null);
  }
  return child;
}
