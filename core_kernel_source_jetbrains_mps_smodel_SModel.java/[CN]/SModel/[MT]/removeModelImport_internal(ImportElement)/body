{
  ModelChange.assertLegalChange(this);
  if (importElement != null) {
    myImports.remove(importElement);
    myImplicitImports.add(importElement);
    fireImportRemovedEvent(importElement.getModelReference());
  }
}
