{
  super(md.getReference(),new NullDataSource());
  if (!isNotified) {
    isNotified=true;
    LOG.error("Fake model descriptor is created. " + "This means that SModel leaked from persistence-related code and may lead to problems later. " + "Fix the code using old SModel class, replace it with SModelDescriptor or SModel interface. "+ "The current code will fail with NPE in following releases",new Throwable());
  }
  myModel=md;
}
