{
  ReloadListenerContainer.getInstance().addReloadListener(new IReloadListener(){
    @Override public void reloadStarted(){
    }
    @Override public void reloadFinished(){
      notifyAfterReload();
    }
  }
);
}
