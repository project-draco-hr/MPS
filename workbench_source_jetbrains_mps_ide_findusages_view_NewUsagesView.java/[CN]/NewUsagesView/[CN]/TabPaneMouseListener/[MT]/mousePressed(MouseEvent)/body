{
  if (e.getButton() == MouseEvent.BUTTON2) {
    int tabIndex=myTabbedPane.indexAtLocation(e.getX(),e.getY());
    if (tabIndex != -1) {
      closeTab(tabIndex);
    }
  }
 else {
    if (e.isPopupTrigger()) {
      handlePopup(e);
    }
  }
}
