{
  List<MenuItem> result=new ArrayList<MenuItem>();
  for (  SAbstractConcept concept : myConcepts) {
    result.addAll(context.getMenuItemFactory().createItems(new DefaultMenuLookup(LanguageRegistry.getInstance(context.getEditorContext().getRepository()),concept),context.getNode()));
  }
  return result;
}
