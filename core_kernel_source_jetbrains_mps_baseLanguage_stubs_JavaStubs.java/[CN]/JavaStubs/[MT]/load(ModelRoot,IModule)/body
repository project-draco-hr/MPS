{
  Set<SModelDescriptor> result=new HashSet<SModelDescriptor>();
  IClassPathItem cp=createClassPathItem(root.getPath());
  getModelDescriptors(result,cp,root.getPrefix(),LanguageID.JAVA,module);
  return result;
}
