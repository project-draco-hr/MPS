{
  if (s == null) {
    return null;
  }
synchronized (myCache) {
    String result=myCache.tryKey(s);
    if (result != null) {
      return result;
    }
    s=new String(s);
    myCache.cacheObject(s,s);
  }
  return s;
}
