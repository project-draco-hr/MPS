{
  this.next=null;
  this.hasNext=0;
  while (inputIt.hasNext()) {
    U tmp=inputIt.next();
    if (cardMap.postInc(tmp) < maxCardinality) {
      this.next=tmp;
      this.hasNext=1;
      return;
    }
  }
  cardMap.clear();
}
