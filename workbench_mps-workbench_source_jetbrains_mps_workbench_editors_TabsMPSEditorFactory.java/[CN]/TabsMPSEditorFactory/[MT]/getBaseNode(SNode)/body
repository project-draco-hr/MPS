{
  if (!isUseTabs()) {
    return null;
  }
  for (  RelationDescriptor d : getTabDescriptors()) {
    SNode baseNode=d.getBaseNode(aspect);
    if (baseNode == aspect) {
      continue;
    }
    if (baseNode != null) {
      return baseNode;
    }
  }
  return null;
}
