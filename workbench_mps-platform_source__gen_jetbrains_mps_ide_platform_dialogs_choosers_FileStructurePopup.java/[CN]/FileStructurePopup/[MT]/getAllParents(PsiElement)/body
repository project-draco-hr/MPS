{
  Set<PsiElement> parents=new HashSet<PsiElement>();
  while (element != null) {
    parents.add(element);
    if (element instanceof PsiFile) {
      break;
    }
    element=element.getParent();
  }
  return parents;
}
