{
  if ("simple".equals(formatterName)) {
    PeriodFormatterBuilder builder=new PeriodFormatterBuilder();
    builder.appendLiteral("v1");
    builder.appendLiteral("v2");
    return builder.toFormatter();
  }
  return null;
}
