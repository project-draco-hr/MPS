{
  int len=s.length();
  if (len == 0 || !(isNameStartChar(s.codePointAt(0)))) {
    return false;
  }
  for (int i=1; i < len; i++) {
    if (!(isNameChar(s.codePointAt(i)))) {
      return false;
    }
  }
  return true;
}
