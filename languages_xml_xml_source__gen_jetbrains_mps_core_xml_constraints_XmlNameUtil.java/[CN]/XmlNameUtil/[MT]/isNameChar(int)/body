{
  if (c < 0x100) {
    if (c == '.' || c == '-' || c >= '0' && c <= '9' || c == 0xb7) {
      return true;
    }
  }
 else {
    if (c >= 0x300 && c <= 0x36f || c >= 0x203f && c <= 0x2040) {
      return true;
    }
  }
  return isNameStartChar(c);
}
