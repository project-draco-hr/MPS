{
  TraversalAxis axis=(TraversalAxis)parameterObject.o1;
  SNode defFeature=(SNode)parameterObject.o2;
  return "iterate " + axis.getName() + (((defFeature != null) ? " (default feature)" : " (all features)"));
}
