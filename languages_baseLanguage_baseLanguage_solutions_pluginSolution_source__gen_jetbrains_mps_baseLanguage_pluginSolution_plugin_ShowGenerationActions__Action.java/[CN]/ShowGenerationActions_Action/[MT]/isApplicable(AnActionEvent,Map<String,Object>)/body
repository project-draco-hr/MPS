{
  ActionGroup group=((BaseGroup)ActionManager.getInstance().getAction("jetbrains.mps.baseLanguage.pluginSolution.plugin.GenerateActions_ActionGroup"));
  group.update(event);
  return group.getChildren(event).length != 0;
}
