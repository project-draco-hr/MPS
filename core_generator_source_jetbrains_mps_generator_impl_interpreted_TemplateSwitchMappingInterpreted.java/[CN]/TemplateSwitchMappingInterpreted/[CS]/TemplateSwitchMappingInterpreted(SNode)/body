{
  mySwitch=aSwitch;
  rules=new ArrayList<TemplateReductionRule>();
  for (  SNode child : mySwitch.getChildren()) {
    if (RuleUtil.concept_Reduction_MappingRule.equals(child.getConcept())) {
      rules.add(new TemplateReductionRuleInterpreted(child));
    }
  }
}
