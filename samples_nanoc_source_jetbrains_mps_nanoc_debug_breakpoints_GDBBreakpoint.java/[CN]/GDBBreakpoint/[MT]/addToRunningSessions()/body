{
  CppDebugSession.performAllSessionsAction(myProject,new DebugSessionAction(){
    @Override public void run(    CppDebugSession debugSession){
      createBreakpointRequest(debugSession.getGDBRequestManager());
    }
  }
);
}
