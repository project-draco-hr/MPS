{
synchronized (myWaitLock) {
    myWaitCompleted=false;
    waitScheduling.run();
  }
  while (!(myWaitCompleted)) {
synchronized (myWaitLock) {
      try {
        myWaitLock.wait();
      }
 catch (      InterruptedException e) {
        e.printStackTrace();
      }
    }
  }
}
