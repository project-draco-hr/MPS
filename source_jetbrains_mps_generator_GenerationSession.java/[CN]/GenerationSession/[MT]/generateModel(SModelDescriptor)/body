{
  GenerationStatus status=generateModel_internal(sourceModel);
  if (mySaveTransientModels || status.isError()) {
    if (myCurrentContext != null) {
      mySavedContexts.add(myCurrentContext);
      myCurrentContext=null;
    }
  }
  return status;
}
