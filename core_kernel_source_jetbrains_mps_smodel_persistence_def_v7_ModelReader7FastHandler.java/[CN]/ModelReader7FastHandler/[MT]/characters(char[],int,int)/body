{
  globalHandleText(myValues.firstElement(),new String(array,start,len));
  ModelReader7FastHandler.ElementHandler current=(myHandlersStack.empty() ? (ModelReader7FastHandler.ElementHandler)null : myHandlersStack.peek());
  if (current != null) {
    current.handleText(myValues.peek(),new String(array,start,len));
  }
}
