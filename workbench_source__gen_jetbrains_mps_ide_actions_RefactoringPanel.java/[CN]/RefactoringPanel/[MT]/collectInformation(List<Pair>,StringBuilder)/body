{
  if (pairs.isEmpty()) {
    return;
  }
  for (  Pair<String,String> pair : pairs) {
    stringBuilder.append(pair.o1 + " -> " + pair.o2);
    stringBuilder.append("\n");
  }
  stringBuilder.append("\n\n");
}
