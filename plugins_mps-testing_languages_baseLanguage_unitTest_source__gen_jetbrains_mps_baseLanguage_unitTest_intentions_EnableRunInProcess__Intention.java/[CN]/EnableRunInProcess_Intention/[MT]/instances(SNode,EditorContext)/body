{
  if (myCachedExecutable == null) {
    myCachedExecutable=Collections.<IntentionExecutable>singletonList(new EnableRunInProcess_Intention.IntentionImplementation());
  }
  return myCachedExecutable;
}
