{
  return Sequence.fromIterable(myScope.getModules()).where(new ISelector<SModule,Boolean>(){
    @Override public Boolean select(    SModule module){
      return acceptModule(module);
    }
  }
);
}
