{
  myTypeChecker.getEquationManager().addEquation(new NodeWrapper(argument),new ConditionWrapper(new Condition<SNode>(){
    public boolean met(    SNode object){
      r.run();
      return true;
    }
  }
),null);
}
