{
  if (argument == null)   return;
  myTypeChecker.getEquationManager().addEquation(NodeWrapper.createNodeWrapper(argument),new ConditionWrapper(new Condition<SNode>(){
    public boolean met(    SNode object){
      r.run();
      return true;
    }
  }
,nodeModel,nodeId),null);
}
