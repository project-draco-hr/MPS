{
  SModelDescriptor transientModel=TransientModels.createTransientModel(modelOwner,longName,"" + myInvocationCount + "_"+ myTransientModelsCount);
  myTransientModelsCount++;
  transientModel.getSModel().setLoading(true);
  return transientModel.getSModel();
}
