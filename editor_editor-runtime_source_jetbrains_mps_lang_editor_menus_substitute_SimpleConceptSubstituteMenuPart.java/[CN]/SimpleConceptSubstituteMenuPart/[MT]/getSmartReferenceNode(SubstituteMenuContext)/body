{
  final SNode conceptSourceNode=getMyConceptSourceNode(context);
  if (conceptSourceNode == null) {
    return null;
  }
  return ReferenceConceptUtil.getCharacteristicReference(conceptSourceNode);
}
