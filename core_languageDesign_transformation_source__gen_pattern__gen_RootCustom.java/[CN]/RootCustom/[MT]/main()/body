{
{
    int e=5;
    int i=1;
    i*=287;
    e*=(i + 5);
    i=e * i;
    if (!(e != 10 || i != 43)) {
      boolean res=true;
      for (int qq=0; qq < 10; qq++) {
        res=!(!(res) || !((i - e > qq)));
      }
    }
{
      int var12=55;
    }
{
      int var12=175 * (1 + e);
    }
    int e33=42;
    int e32=(22 + "myWork1".length());
    int e31=58 + ("extraWork".length());
    int e30={};
  }
}
