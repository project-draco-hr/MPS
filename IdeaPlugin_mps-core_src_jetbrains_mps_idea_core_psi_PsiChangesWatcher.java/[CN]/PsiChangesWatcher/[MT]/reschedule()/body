{
  if (myTimerTask != null) {
    myTimerTask.cancel();
  }
  myTimerTask=new PsiChangeNotifier(myCollectedData);
  if (SYNCHRONOUS) {
    myTimerTask.run();
  }
 else {
    myTimer.schedule(myTimerTask,DELAY);
  }
}
