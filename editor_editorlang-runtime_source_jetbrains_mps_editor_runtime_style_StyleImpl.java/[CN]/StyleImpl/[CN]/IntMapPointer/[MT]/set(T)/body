{
  if (value == null) {
    if (!isEmpty()) {
      delete();
    }
  }
 else {
    if (isEmpty()) {
      myIterator.add(new IntPair<T>(myIndex,value));
    }
 else {
      getPair().value=value;
    }
  }
}
