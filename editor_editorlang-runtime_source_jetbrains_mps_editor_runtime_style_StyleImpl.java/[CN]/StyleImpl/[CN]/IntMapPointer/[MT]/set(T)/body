{
  if (value == null) {
    if (!isEmpty()) {
      delete();
    }
  }
 else {
    if (isEmpty()) {
      myIterator.add(new IntPair<T>(myIndex,value));
      myIterator.previous();
      myEmpty=false;
    }
 else {
      getPair().value=value;
    }
  }
}
