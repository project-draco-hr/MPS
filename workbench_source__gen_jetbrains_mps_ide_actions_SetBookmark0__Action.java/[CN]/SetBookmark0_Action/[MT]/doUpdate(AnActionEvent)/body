{
  try {
    this.enable(event.getPresentation());
  }
 catch (  Throwable t) {
    LOG.error("User's action doUpdate method failed. Action:" + "SetBookmark0",t);
    this.disable(event.getPresentation());
  }
}
