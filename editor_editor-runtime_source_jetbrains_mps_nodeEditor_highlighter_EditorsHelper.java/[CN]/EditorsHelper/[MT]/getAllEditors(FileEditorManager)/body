{
  FileEditor[] allEditors=ApplicationManager.getApplication().runReadAction(new Computable<FileEditor[]>(){
    @Override public FileEditor[] compute(){
      return manager.getAllEditors();
    }
  }
);
  return filterMPSEditors(allEditors);
}
