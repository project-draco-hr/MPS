{
  try {
    return new ZipFile(file);
  }
 catch (  ZipException e) {
    status.report(path,"cannot open zip: " + e.getMessage());
  }
catch (  IOException e) {
    status.report(path,"cannot open zip: " + e.getMessage());
  }
  return null;
}
