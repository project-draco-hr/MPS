{
  if (value == null) {
    return true;
  }
  Iterator<Size> constants=ListSequence.fromList(Size.getConstants()).iterator();
  while (constants.hasNext()) {
    Size constant=constants.next();
    if (value.equals(constant.getName())) {
      return true;
    }
  }
  return false;
}
