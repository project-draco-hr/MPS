{
  if (LOG.isInfoEnabled()) {
    LOG.info("Running test " + className);
  }
  Thread.sleep(10000);
  ModelAccess.instance().runWriteAction(new Runnable(){
    public void run(){
      projectTest.dispose();
    }
  }
);
}
