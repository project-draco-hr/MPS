{
synchronized (myChangesMessages) {
    return (MapSequence.fromMap(myChangesMessages).containsKey(change) ? MapSequence.fromMap(myChangesMessages).get(change) : ListSequence.fromList(new ArrayList<ChangeEditorMessage>()));
  }
}
