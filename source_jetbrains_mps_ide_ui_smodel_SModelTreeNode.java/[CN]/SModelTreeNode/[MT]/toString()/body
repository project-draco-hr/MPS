{
  String name=myShowLongName ? myModelDescriptor.getModelUID().toString() : myModelDescriptor.getModelUID().getShortName();
  if (myLabel != null) {
    return myLabel + " : " + name;
  }
  return name;
}
