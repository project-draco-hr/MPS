{
  if (getSModelDescriptor() != null && getSModelDescriptor().isInitialized() && SModelRepository.getInstance().isChanged(getSModelDescriptor())) {
    return new Color(0x00,0x00,0x90);
  }
  return Color.BLACK;
}
