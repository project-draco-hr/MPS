{
  super("Refactoring",ID);
  this.setIsInternal(false);
  this.setPopup(false);
  try {
    LanguageRefactoring_ActionGroup.this.addAction("jetbrains.mps.ide.actions.RenameSolution_Action");
  }
 catch (  Throwable t) {
    LOG.error("User group error",t);
  }
}
