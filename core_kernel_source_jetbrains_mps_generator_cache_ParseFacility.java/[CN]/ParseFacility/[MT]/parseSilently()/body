{
  if (!isValidInput()) {
    return null;
  }
  try {
    return parse();
  }
 catch (  FileNotFoundException ex) {
  }
catch (  IOException ex) {
    getLog().warn("Ignored parse error",ex);
  }
  return null;
}
