{
  if (myEnabled != enabled) {
    myEnabled=enabled;
    if (enabled) {
      myChangesTracker.scheduleFullUpdate();
    }
 else {
      myCommandQueue.addTask(new Runnable(){
        public void run(){
          setChangeSet(null);
        }
      }
);
    }
  }
}
