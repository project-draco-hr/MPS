{
  TSStatus status=generator.getTypeChecker().getNodeType(node);
  if (status.isError()) {
    generator.showErrorMessage(node,"Couldn't compute type of " + (description != null ? description : "node") + " : "+ status.getMessage());
    return null;
  }
  return status.getTypeObject();
}
