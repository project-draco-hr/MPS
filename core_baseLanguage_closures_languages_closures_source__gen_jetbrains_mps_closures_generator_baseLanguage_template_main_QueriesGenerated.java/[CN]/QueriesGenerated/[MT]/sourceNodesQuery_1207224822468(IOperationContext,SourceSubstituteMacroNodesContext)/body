{
  for (  SNode n : SNodeOperations.getDescendants(_context.getNode(),null,false)) {
    WrappersUtils.getPrepData(n,_context.getGenerator());
  }
  return SLinkOperations.getTargets(_context.getNode(),"statement",true);
}
