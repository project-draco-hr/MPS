{
  info("Start generating " + modulesSet);
  gm.generateModels(modelsToContext,new GenerateFilesGenerationType(){
    @Override public boolean requiresCompilationAfterGeneration(){
      return Boolean.parseBoolean(myWhatToDo.getProperty(GenerateTask.COMPILE));
    }
  }
,emptyProgressIndicator,myMessageHandler,false);
  info("Finished generating " + modulesSet);
}
