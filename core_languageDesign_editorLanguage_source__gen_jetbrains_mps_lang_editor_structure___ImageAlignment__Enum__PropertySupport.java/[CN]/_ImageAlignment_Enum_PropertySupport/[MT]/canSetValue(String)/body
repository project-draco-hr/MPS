{
  if (value == null) {
    return true;
  }
  Iterator<_ImageAlignment_Enum> constants=_ImageAlignment_Enum.getConstants().iterator();
  while (constants.hasNext()) {
    _ImageAlignment_Enum constant=constants.next();
    if (value.equals(constant.getName())) {
      return true;
    }
  }
  return false;
}
