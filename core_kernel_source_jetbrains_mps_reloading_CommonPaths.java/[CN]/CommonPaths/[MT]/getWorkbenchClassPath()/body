{
  String workbenchClasses=PathManager.getHomePath() + File.separator + "workbench"+ File.separator+ "classes";
  if (new File(workbenchClasses).exists()) {
    return AbstractClassPathItem.createFromPath(workbenchClasses);
  }
  return null;
}
