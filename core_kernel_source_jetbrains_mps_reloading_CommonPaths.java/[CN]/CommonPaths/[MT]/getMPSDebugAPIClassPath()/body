{
  String supportClasses=PathManager.getHomePath() + File.separator + "core"+ File.separator+ "debug-api"+ File.separator+ "classes";
  if (new File(supportClasses).exists()) {
    return ClassPathFactory.getInstance().createFromPath(supportClasses);
  }
  return null;
}
