{
  String generatorClasses=PathManager.getHomePath() + File.separator + "core"+ File.separator+ "typesystemEngine"+ File.separator+ "classes";
  if (new File(generatorClasses).exists()) {
    return ClassPathFactory.getInstance().createFromPath(generatorClasses);
  }
  return null;
}
