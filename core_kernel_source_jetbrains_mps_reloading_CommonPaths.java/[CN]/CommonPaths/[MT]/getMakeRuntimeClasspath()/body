{
  String makeClasses=PathManager.getHomePath() + File.separator + "core"+ File.separator+ "make-runtime"+ File.separator+ "classes";
  if (new File(makeClasses).exists()) {
    return ClassPathFactory.getInstance().createFromPath(makeClasses);
  }
  return null;
}
