{
  int index=-1;
  for (int i=0; i < list.size(); i++) {
    T item=list.get(i);
    if (comp.compare(item,object) == 0) {
      index=i;
      break;
    }
  }
  if (index != -1) {
    return list.remove(index);
  }
  return null;
}
