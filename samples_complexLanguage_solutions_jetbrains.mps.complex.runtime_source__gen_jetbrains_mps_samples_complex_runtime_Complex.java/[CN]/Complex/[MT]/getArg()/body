{
  double r=this.getAbs();
  if (r > EPSILON) {
    double result=Math.acos(this.myReal / r);
    if (this.myImaginary > 0.0) {
      return result;
    }
 else {
      return -1 * result;
    }
  }
 else {
    return 0.0;
  }
}
