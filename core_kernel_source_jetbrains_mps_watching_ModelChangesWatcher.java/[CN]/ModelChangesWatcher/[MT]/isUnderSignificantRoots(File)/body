{
  for (  VirtualFile f : getSignificantRoots()) {
    try {
      if (FileUtil.isAncestor(VFileSystem.toFile(f),file,false)) {
        return true;
      }
    }
 catch (    IOException e) {
      LOG.error(e);
    }
  }
  return false;
}
