{
  if (myCachedExecutable == null) {
    myCachedExecutable=Collections.<IntentionExecutable>singletonList(new RemoveArrayType_Intention.IntentionImplementation());
  }
  return myCachedExecutable;
}
