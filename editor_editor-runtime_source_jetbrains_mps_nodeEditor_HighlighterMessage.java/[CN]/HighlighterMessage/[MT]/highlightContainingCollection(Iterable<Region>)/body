{
  Region firstRegion=null;
  for (  Region region : regions) {
    if (firstRegion == null) {
      firstRegion=region;
      continue;
    }
    if (!firstRegion.isSameY(region)) {
      return false;
    }
  }
  return true;
}
