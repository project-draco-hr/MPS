{
  if (myManager != null) {
    myManager.dispose();
  }
  if (manager != null) {
    myManager=manager;
  }
 else {
    myManager=new DefaultFindUsagesManager(myClassLoaderManager);
  }
  myManager.init();
}
