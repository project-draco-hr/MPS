{
  if (this.hasNext != 1) {
    throw new NoSuchElementException();
  }
  T tmp=this.yielded;
  try {
    this.hasNext=(this.moveToNext() ? 1 : 0);
  }
 catch (  DelayedException ex) {
    this.delayedEx=ex;
    this.hasNext=0;
  }
  return tmp;
}
