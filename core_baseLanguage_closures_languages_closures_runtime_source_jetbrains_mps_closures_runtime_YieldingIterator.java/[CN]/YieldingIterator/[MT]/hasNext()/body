{
  if (this.hasNext < 0) {
    this.hasNext=(this.moveToNext() ? 1 : 0);
  }
  if (delayedEx != null) {
    DelayedException tmp=this.delayedEx;
    this.delayedEx=null;
    throw tmp;
  }
  return this.hasNext == 1;
}
