{
  myNodeId=nodeId;
  myRole=role;
  myModel=model;
  if (target == null || target.getModel() != model) {
    myInternal=false;
    myProxy=new SNodeProxy(target);
  }
 else {
    myInternal=true;
    myTargetId=target.getSNodeId();
  }
}
