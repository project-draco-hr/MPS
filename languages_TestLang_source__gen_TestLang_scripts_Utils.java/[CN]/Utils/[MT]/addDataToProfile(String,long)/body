{
  startWatchingIfNeeded();
synchronized (Utils.watchingLock) {
    MapSequence.fromMap(timeSummary).put(key,time + ((MapSequence.fromMap(timeSummary).containsKey(key) ? MapSequence.fromMap(timeSummary).get(key) : 0)));
    Utils.lastUpdateTime=System.currentTimeMillis();
  }
}
