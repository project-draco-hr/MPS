{
  super("TraceActions",ID);
  this.setIsInternal(false);
  this.setPopup(false);
  try {
    TraceActions_ActionGroup.this.addAction("jetbrains.mps.ide.devkit.actions.ShowTypeSystemTrace_Action");
  }
 catch (  Throwable t) {
    LOG.error("User group error",t);
  }
}
