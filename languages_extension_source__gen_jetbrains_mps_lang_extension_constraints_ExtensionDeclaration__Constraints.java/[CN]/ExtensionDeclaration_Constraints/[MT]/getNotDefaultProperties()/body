{
  Map<String,PropertyConstraintsDescriptor> properties=new HashMap();
  properties.put("name",new BasePropertyConstraintsDescriptor("name",this){
    @Override public boolean hasOwnGetter(){
      return true;
    }
    @Override public Object getValue(    SNode node,    IScope scope){
      String propertyName="name";
{
        SNode extensionPoint=SLinkOperations.getTarget(node,"extensionPoint",false);
        return ((extensionPoint != null ? SPropertyOperations.getString(extensionPoint,"extensionName") : "<unknown>")) + " Extension";
      }
    }
  }
);
  return properties;
}
