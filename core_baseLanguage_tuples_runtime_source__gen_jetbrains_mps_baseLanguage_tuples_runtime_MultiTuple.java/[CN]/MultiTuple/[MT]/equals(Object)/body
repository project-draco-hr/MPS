{
  if (this == that) {
    return true;
  }
  if (!(that instanceof MultiTuple)) {
    return false;
  }
  if (this.values.length != ((MultiTuple)that).values.length) {
    return false;
  }
  for (int i=0; i < this.values.length; i++) {
    if (!(MultiTuple.eq(this.values[i],((MultiTuple)that).values[i]))) {
      return false;
    }
  }
  return true;
}
