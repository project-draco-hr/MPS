{
  File cacheFile=getCacheFile(sm);
  if (!cacheFile.exists())   return null;
  InputStream is=null;
  try {
    is=new FileInputStream(cacheFile);
    return load(is);
  }
 catch (  IOException e) {
    LOG.error(e);
  }
 finally {
    try {
      if (is != null) {
        is.close();
      }
    }
 catch (    IOException e) {
    }
  }
  return null;
}
