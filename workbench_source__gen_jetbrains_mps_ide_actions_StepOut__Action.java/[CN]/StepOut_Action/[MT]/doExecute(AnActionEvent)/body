{
  try {
    StepOut_Action.this.getDebugSession().stepOut();
  }
 catch (  Throwable t) {
    if (log.isErrorEnabled()) {
      log.error("User's action execute method failed. Action:" + "StepOut",t);
    }
  }
}
