{
  try {
    event.getPresentation().setEnabled(StepOut_Action.this.getDebugSession() != null && StepOut_Action.this.getDebugSession().isPaused());
  }
 catch (  Throwable t) {
    if (log.isErrorEnabled()) {
      log.error("User's action doUpdate method failed. Action:" + "StepOut",t);
    }
    this.disable(event.getPresentation());
  }
}
