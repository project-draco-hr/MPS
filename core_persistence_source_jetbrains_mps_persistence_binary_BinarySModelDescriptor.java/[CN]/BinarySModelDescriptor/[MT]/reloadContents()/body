{
  try {
    myHeader=(BinaryModelHeader)myPersistence.readHeader();
  }
 catch (  ModelReadException e) {
    updateTimestamp();
    SuspiciousModelHandler.getHandler().handleSuspiciousModel(this,false);
    return;
  }
  super.reloadContents();
}
