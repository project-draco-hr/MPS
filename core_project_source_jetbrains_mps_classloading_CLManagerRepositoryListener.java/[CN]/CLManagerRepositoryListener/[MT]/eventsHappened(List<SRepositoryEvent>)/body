{
  MyModuleEventVisitor visitor=new MyModuleEventVisitor();
  for (  SRepositoryEvent event : events)   event.accept(visitor);
  unloadModules(visitor.getModulesToUnload());
  loadModules(visitor.getModulesToLoad());
}
