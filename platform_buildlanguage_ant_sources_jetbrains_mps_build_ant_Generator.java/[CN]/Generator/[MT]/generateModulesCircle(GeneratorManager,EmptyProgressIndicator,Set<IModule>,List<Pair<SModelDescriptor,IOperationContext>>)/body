{
  info("Start generating " + modulesSet);
  gm.generateModels(modelsToContext,new GenerateFilesGenerationType(),emptyProgressIndicator,myMessageHandler,false);
  info("Finished generating " + modulesSet);
}
