{
  GeneratorManager gm=project.getComponentSafe(GeneratorManager.class);
  gm.generateModels(models,new ProjectOperationContext(project),myGenerationType,new EmptyProgressIndicator(),myMessageHandler);
}
