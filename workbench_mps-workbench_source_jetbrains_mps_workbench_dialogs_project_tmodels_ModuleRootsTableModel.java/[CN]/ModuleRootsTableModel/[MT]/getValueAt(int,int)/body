{
  ModelRootDescriptor item=myTableItems.get(rowIndex);
  if (columnIndex == PATH_COLUMN)   return item.getRoot().getPath();
  if (columnIndex == MANAGER_COLUMN)   return item.getRoot().getManager();
  return null;
}
