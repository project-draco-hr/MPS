{
  return Sequence.fromIterable(((Iterable<SNode>)_context.getVariable("var:attached"))).where(new IWhereFilter<SNode>(){
    public boolean accept(    SNode it){
      return SNodeOperations.getConceptDeclaration(it) == _context.getNode();
    }
  }
);
}
