{
  return Sequence.fromIterable(((Iterable<SNode>)_context.getVariable("var:attached"))).select(new ISelector<SNode,SNode>(){
    public SNode select(    SNode it){
      return SNodeOperations.getConceptDeclaration(it);
    }
  }
).distinct();
}
