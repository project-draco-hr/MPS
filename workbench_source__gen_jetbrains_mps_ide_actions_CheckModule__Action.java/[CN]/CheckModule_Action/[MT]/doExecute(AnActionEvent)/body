{
  try {
    if (CheckModule_Action.this.modules.size() > 1) {
      CheckModule_Action.this.mpsProject.getPluginManager().getTool(ModelCheckerTool_Tool.class).checkModules(CheckModule_Action.this.modules,CheckModule_Action.this.operationContext,true);
    }
 else {
      CheckModule_Action.this.mpsProject.getPluginManager().getTool(ModelCheckerTool_Tool.class).checkModule(CheckModule_Action.this.module,CheckModule_Action.this.operationContext,true);
    }
  }
 catch (  Throwable t) {
    if (log.isErrorEnabled()) {
      log.error("User's action execute method failed. Action:" + "CheckModule",t);
    }
  }
}
