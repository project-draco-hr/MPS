{
  LanguageScope languageScope=lang == null ? LanguageScope.getGlobal() : LanguageScopeFactory.getInstance().getLanguageScope(LanguageDependenciesManager.getAllExtendedLanguageReferences(lang));
  try {
    LanguageScope.pushCurrent(languageScope,computable);
    return computable.compute();
  }
  finally {
    LanguageScope.popCurrent(languageScope,computable);
  }
}
