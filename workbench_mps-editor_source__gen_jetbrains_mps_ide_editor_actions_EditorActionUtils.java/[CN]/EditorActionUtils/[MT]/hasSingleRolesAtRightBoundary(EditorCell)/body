{
  if (!(hasSingleRole(cell))) {
    return false;
  }
  if (cell.isOnRightBoundary()) {
    EditorCell_Collection parentCell=cell.getParent();
    return (parentCell != null ? hasSingleRolesAtRightBoundary(parentCell) : true);
  }
  return true;
}
