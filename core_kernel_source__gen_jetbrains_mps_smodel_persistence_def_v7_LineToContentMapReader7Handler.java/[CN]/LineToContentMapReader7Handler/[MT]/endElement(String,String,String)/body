{
  LineToContentMapReader7Handler.ElementHandler current=myHandlersStack.pop();
  Object childValue=myValues.pop();
  if (current != null) {
    current.validate(childValue);
    LineToContentMapReader7Handler.ElementHandler parent=(myHandlersStack.empty() ? (LineToContentMapReader7Handler.ElementHandler)null : myHandlersStack.peek());
    if (parent != null) {
      parent.handleChild(myValues.peek(),qName,childValue);
    }
 else {
      myResult=(List<SNodeId>)childValue;
    }
  }
}
