{
  EditorCell selectedCell=event.getData(MPSEditorDataKeys.EDITOR_CONTEXT).getSelectedCell();
  if (!(selectedCell instanceof EditorCell_Label)) {
    return null;
  }
  EditorCell_Label editorCell=((EditorCell_Label)selectedCell);
  if (!(editorCell.isErrorState())) {
    return null;
  }
  String name=editorCell.getRenderedText();
  if (name.endsWith("()")) {
    name=name.substring(0,name.length() - 2);
  }
  if (!(name.matches("[a-zA-Z0-9_]*"))) {
    return null;
  }
  return name;
}
