{
  boolean changed=false;
  for (  IFile f : created) {
    if (ModulesMiner.getInstance().isModuleFile(f)) {
      changed=true;
    }
  }
  for (  IFile f : deleted) {
    if (ModulesMiner.getInstance().isModuleFile(f)) {
      changed=true;
    }
  }
  if (changed) {
    update(false);
  }
}
