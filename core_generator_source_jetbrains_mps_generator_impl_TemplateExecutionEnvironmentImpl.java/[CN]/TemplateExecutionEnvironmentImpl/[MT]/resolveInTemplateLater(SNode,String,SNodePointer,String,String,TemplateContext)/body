{
  ReferenceInfo_Template refInfo=new ReferenceInfo_Template(outputNode,role,sourceNode,templateNodeId,resolveInfo,context);
  PostponedReference postponedReference=new PostponedReference(refInfo,generator);
  outputNode.addReference(postponedReference);
}
