{
  Collection<SNode> collection=generator.tryToReduce(_switch,context,this);
  if (collection != null) {
    return collection;
  }
  TemplateSwitchMapping current=generator.getSwitch(_switch);
  if (current != null) {
    collection=current.applyDefault(this,_switch,context.getInputName(),context);
    if (collection != null) {
      return collection;
    }
  }
  return null;
}
