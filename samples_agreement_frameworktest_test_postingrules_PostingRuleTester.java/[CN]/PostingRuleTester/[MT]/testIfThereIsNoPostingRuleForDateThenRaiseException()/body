{
  AccountingEvent evt=new MonetaryEvent(Money.dollars(40),EventType.SERVICE_CALL,new MfDate(1999,1,5),new MfDate(1999,1,5),myFixture.getWatson());
  myFixture.getEventList().add(evt);
  try {
    myFixture.getEventList().process();
  }
 catch (  RuntimeException expected) {
  }
  assertTrue(myFixture.getEventList().hasProcessingErrors());
}
