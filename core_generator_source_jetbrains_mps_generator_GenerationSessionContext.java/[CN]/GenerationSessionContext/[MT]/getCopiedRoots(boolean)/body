{
  @SuppressWarnings("unchecked") Set<SNode> set=(Set<SNode>)getStepObject(COPYED_ROOTS);
  if (set == null && create) {
    putStepObject(COPYED_ROOTS,set=new HashSet<SNode>());
  }
  return set;
}
