{
  if (root == null) {
    return;
  }
  ModelAccess.instance().runReadAction(new Runnable(){
    public void run(){
      if (!(contains(root))) {
        ListSequence.fromList(myRoots).addElement(root);
      }
    }
  }
);
}
