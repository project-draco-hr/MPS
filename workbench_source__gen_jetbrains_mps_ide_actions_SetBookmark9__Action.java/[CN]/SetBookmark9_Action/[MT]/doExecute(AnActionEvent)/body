{
  try {
    BookmarkManager bookmarkManager=SetBookmark9_Action.this.project.getComponent(BookmarkManager.class);
    bookmarkManager.setBookmark(SetBookmark9_Action.this.node,9);
  }
 catch (  Throwable t) {
    if (log.isErrorEnabled()) {
      log.error("User's action execute method failed. Action:" + "SetBookmark9",t);
    }
  }
}
