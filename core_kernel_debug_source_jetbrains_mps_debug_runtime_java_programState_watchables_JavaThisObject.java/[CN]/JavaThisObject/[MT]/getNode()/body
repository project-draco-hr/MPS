{
  try {
    Location location=myStackFrame.getStackFrame().location();
    SNode snode=StacktraceUtil.getUnitNode(location.declaringType().name(),location.sourceName(),location.lineNumber());
    return snode;
  }
 catch (  AbsentInformationException ex) {
    LOG.error(ex);
    return null;
  }
}
