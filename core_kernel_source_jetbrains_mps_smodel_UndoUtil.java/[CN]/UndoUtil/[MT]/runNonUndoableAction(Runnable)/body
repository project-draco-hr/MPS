{
  if (!ThreadUtils.isEventDispatchThread()) {
    r.run();
  }
 else {
    if (ourUndoBlocked) {
      r.run();
      return;
    }
    setUndoBlocked();
    try {
      r.run();
    }
  finally {
      setUndoUnblocked();
    }
  }
}
