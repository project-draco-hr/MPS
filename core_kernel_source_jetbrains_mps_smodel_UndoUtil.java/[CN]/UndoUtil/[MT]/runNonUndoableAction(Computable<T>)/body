{
  if (!ThreadUtils.isEventDispatchThread()) {
    return t.compute();
  }
 else {
    if (ourUndoBlocked) {
      return t.compute();
    }
    setUndoBlocked();
    try {
      return t.compute();
    }
  finally {
      setUndoUnblocked();
    }
  }
}
