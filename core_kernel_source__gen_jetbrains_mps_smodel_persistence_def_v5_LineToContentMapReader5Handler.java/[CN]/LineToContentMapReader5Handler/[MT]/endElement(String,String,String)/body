{
  LineToContentMapReader5Handler.ElementHandler current=myHandlersStack.pop();
  Object childValue=myValues.pop();
  if (current != null) {
    current.validate(childValue);
    LineToContentMapReader5Handler.ElementHandler parent=(myHandlersStack.empty() ? (LineToContentMapReader5Handler.ElementHandler)null : myHandlersStack.peek());
    if (parent != null) {
      parent.handleChild(myValues.peek(),qName,childValue);
    }
 else {
      myResult=(List<SNodeId>)childValue;
    }
  }
}
