{
  super("New",ID);
  this.setIsInternal(false);
  this.setPopup(true);
  try {
    this.add(ActionFactory.getInstance().getRegisteredAction(new NewModel_Action()));
  }
 catch (  Throwable t) {
    LOG.error("User group error",t);
  }
}
