{
{
    DefaultActionGroup gTo=ActionUtils.getGroup(SolutionActions_ActionGroup.ID);
    DefaultActionGroup gWhat=ActionUtils.getGroup(SolutionNewActions_ActionGroup.ID);
    if (gTo == null || gWhat == null) {
      return;
    }
{
      String labelName;
      labelName=SolutionActions_ActionGroup.LABEL_ID_solutionNew;
      gTo.add(gWhat,new Constraints(Anchor.AFTER,labelName));
    }
  }
}
