{
  mySearchQuery=query;
  myContext=context;
  Thread t=new Thread(){
    public void run(){
      myTree.setContents(myResultProvider.getResults(mySearchQuery,myContext));
    }
  }
;
  t.start();
  updateUI();
}
