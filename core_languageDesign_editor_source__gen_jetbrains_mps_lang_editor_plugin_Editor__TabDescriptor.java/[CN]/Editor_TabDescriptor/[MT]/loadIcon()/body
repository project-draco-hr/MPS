{
  try {
    return new ImageIcon(StreamUtil.loadFromStream(URLUtil.openStream(Editor_TabDescriptor.class.getResource("editor.png"))));
  }
 catch (  IOException e) {
    LOG.warning("Couldn't load icon for Editor",e);
    return null;
  }
}
