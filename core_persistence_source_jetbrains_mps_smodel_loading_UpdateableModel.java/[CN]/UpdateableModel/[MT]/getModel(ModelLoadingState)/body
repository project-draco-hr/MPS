{
  if (state == null) {
    return myModel;
  }
  if (!ModelAccess.instance().canWrite()) {
synchronized (this) {
      if (state.ordinal() > myState.ordinal()) {
        ensureLoadedTo(state);
      }
    }
  }
 else {
    ensureLoadedTo(state);
  }
  return myModel;
}
