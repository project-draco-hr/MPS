{
  boolean alreadyHasParameters=SLinkOperations.getCount(sourceNode,"parameter") > 0;
  if (alreadyHasParameters) {
    return false;
  }
  return SModelActionsUtil.snodeOperation_canHaveParameters(sourceNode,operationContext.getScope());
}
