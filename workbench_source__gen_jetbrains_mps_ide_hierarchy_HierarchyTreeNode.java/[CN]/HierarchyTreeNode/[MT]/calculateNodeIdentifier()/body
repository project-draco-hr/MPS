{
  if (getNode() == null) {
    return "null";
  }
  if (myHierarchyTree.overridesNodeIdentifierCalculation()) {
    return myHierarchyTree.calculateNodeIdentifier(this);
  }
  String name=getNode().getName();
  return (StringUtils.isEmpty(name) ? "no concept" : name);
}
