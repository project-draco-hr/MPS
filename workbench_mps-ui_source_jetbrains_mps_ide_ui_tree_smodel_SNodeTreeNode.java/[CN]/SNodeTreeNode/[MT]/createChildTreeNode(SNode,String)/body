{
  SModelTreeNode sModelTreeNode=getSModelModelTreeNode();
  return sModelTreeNode == null ? new SNodeTreeNode(childNode,role,myCondition) : sModelTreeNode.createSNodeTreeNode(childNode,role,myCondition);
}
