{
  myContext=context;
  setCellRenderer(new CheckBoxNodeRenderer(true));
  setCellEditor(new CheckBoxNodeEditor(true){
    @Override protected NodeData createNodeData(    boolean selected){
      NodeData data=getObject();
      if (data instanceof GroupData) {
        return data;
      }
 else {
        return GroupedTree.this.createNodeData(selected,data);
      }
    }
  }
);
  setEditable(true);
}
