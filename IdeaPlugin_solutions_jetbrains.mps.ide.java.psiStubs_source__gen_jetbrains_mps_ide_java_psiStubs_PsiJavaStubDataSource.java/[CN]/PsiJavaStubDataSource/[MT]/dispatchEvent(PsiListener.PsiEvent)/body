{
synchronized (LOCK) {
{
      Iterator<PsiListener> l_it=ListSequence.fromList(myListeners).iterator();
      PsiListener l_var;
      while (l_it.hasNext()) {
        l_var=l_it.next();
        l_var.psiChanged(event);
      }
    }
  }
}
