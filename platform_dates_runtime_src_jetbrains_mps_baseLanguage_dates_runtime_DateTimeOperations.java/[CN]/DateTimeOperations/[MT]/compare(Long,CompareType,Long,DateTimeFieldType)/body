{
  op1=roundFloor(op1,type);
  op2=roundFloor(op2,type);
  if (op1 == null) {
    op1=Long.MIN_VALUE;
  }
  if (op2 == null) {
    op2=Long.MIN_VALUE;
  }
  return compareResult(op1.compareTo(op2),cmp);
}
