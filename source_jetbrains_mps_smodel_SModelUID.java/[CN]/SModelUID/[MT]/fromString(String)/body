{
  int index=s.indexOf("@");
  String stereotype="";
  if (index >= 0) {
    stereotype=s.substring(index + 1);
  }
  String fqName=s;
  if (index > 0) {
    fqName=s.substring(0,index);
  }
  return new SModelUID(fqName,stereotype);
}
