{
  StringBuilder sb=new StringBuilder();
  while (myOffset < myInput.length()) {
    char c=currentChar();
    if (c == ',' || c == '\n' || c == '\r') {
      myOffset++;
      return sb.toString();
    }
    sb.append(c);
    myOffset++;
  }
  return sb.toString();
}
