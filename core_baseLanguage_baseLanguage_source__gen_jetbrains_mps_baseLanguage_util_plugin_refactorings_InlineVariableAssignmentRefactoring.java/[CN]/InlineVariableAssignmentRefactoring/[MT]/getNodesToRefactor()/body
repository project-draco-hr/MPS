{
  List<SNode> result=new ArrayList<SNode>();
  for (  ReadInstruction read : SetSequence.<ReadInstruction>fromSet(this.myReadInstructions)) {
    ListSequence.<SNode>fromList(result).addElement(((SNode)read.getSource()));
  }
  return result;
}
