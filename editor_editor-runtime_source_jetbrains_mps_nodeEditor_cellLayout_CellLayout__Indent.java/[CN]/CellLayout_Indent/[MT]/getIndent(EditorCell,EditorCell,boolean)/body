{
  int result=0;
  if (overflow) {
    result+=2;
  }
  while (cell != root) {
    if (APIStyleAdapter.getStyleAttribute(cell,StyleAttributes.INDENT_LAYOUT_INDENT)) {
      result++;
    }
    cell=cell.getParent();
  }
  return result;
}
