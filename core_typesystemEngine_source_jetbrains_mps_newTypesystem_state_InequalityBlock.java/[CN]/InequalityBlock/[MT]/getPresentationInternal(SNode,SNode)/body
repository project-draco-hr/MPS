{
  String sign=myRelationKind.getRelationSign();
  if (myLeftNode == myOutput) {
    sign=":" + sign;
  }
 else   if (myRightNode == myOutput) {
    sign=sign + ":";
  }
  return myRelationKind.getTitle() + " : " + left+ sign+ right;
}
