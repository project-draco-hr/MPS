{
  for (  FoldingButton button : myFoldingButtons.values()) {
    if (button.isInside(e)) {
      if (!button.myMouseOver) {
        button.mouseEntered();
      }
    }
 else {
      if (button.myMouseOver) {
        button.mouseExited();
      }
    }
  }
}
