{
  TemporalCollection copy=((SingleTemporalCollection)mySubject).copy();
  Assert.assertEquals("after dec ish",copy.get(1999,12,30));
  Assert.assertEquals("after dec ish",copy.get(2000,2,1));
  copy.put(new MfDate(2000,2,1),"feb");
  Assert.assertEquals("after dec ish",copy.get(1999,12,30));
  Assert.assertEquals("feb",copy.get(2000,2,1));
  Assert.assertEquals("after dec ish",mySubject.get(1999,12,30));
  Assert.assertEquals("after dec ish",mySubject.get(2000,2,1));
}
