{
  applyExpanded(getFacade(tree),tree.getModel().getRoot());
  Object root=tree.getModel().getRoot();
  if (root instanceof DefaultMutableTreeNode) {
    applySelected(tree,((DefaultMutableTreeNode)root));
  }
}
