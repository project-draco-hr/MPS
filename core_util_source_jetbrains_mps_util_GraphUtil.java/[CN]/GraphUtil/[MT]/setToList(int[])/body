{
  int size=0;
  for (int i=0; i < set.length; i++) {
    if (set[i] == 1) {
      size++;
    }
  }
  int[] res=new int[size];
  int e=0;
  for (int i=0; i < set.length; i++) {
    if (set[i] == 1) {
      res[e++]=i;
    }
  }
  return res;
}
