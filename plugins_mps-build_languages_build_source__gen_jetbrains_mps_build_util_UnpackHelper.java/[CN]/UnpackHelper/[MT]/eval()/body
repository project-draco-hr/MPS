{
  if (evaluated) {
    return;
  }
  evaluated=true;
  for (  SNode n : required) {
    Collection<Object> artifacts=artifactIds.get(n);
    BuildLayout_PathElement_BehaviorDescriptor.unpack_id6bGbH3Svq6g.invoke(n,this,(artifacts != null ? artifacts : Collections.emptyList()));
  }
}
