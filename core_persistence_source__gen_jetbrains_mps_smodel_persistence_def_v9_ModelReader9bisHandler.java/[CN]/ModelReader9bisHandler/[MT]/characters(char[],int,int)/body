{
  ModelReader9bisHandler.ElementHandler current=(myHandlersStack.empty() ? (ModelReader9bisHandler.ElementHandler)null : myHandlersStack.peek());
  if (current != null) {
    current.handleText(myValues.peek(),new String(array,start,len));
  }
}
