{
  if (vector.myColumns != 1 || vector.myRows != myRows || i < 0 | i >= myColumns)   throw new IndexOutOfBoundsException();
  for (int j=0; j < vector.myRows; j++)   myCarrier[j][i]=vector.myCarrier[j][0];
}
