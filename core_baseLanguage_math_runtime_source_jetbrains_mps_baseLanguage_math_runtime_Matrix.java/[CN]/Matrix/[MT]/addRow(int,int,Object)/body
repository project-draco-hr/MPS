{
  if (source < 0 || dest < 0 || source >= myRows || dest >= myRows)   throw new IndexOutOfBoundsException();
  for (int i=0; i < myColumns; i++)   myCarrier[dest][i]=myOperations.add(myCarrier[dest][i],myOperations.mul(scalar,myCarrier[source][i]));
}
