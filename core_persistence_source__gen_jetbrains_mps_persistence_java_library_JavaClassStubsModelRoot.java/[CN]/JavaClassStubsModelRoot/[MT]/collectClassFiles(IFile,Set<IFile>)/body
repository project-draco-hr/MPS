{
  if (file.getPath().endsWith(".class")) {
    files.add(file);
    return;
  }
  if (!(file.isDirectory())) {
    return;
  }
  for (  IFile child : file.getChildren()) {
    collectClassFiles(child,files);
  }
}
