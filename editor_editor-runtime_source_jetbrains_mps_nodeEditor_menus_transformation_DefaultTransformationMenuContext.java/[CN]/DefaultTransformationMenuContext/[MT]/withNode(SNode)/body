{
  if (myNode == node) {
    return this;
  }
  return new DefaultTransformationMenuContext(myMenuItemFactory,myMenuLocation,myEditorContext,node,null,createSuitableForConstraintsPredicate(node,myEditorContext.getRepository()));
}
