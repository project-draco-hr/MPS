{
  @DisposableCommand SNode node=myNodePointer.getNode();
  if (node == null)   return;
  setIcon(IconManager.getIconFor(node));
  setAdditionalText(calculateAdditionalText());
  setAutoExpandable(false);
}
