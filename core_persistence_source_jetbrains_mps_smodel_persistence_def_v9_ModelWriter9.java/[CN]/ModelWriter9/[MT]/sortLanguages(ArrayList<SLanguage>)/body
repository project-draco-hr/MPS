{
  Collections.sort(keys,new Comparator<SLanguage>(){
    @Override public int compare(    SLanguage o1,    SLanguage o2){
      return o1.getQualifiedName().compareTo(o2.getQualifiedName());
    }
  }
);
}
