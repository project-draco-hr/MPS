{
  Environment env=IdeaEnvironment.getOrCreate(EnvironmentConfig.emptyEnvironment().withBootstrapLibraries().withWorkbenchPath().addPlugin("jetbrains.mps.build"));
  myProject=env.createEmptyProject();
}
