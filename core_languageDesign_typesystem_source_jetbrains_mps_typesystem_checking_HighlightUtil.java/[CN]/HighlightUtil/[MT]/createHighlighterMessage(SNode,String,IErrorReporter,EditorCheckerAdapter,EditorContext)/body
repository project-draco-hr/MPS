{
  if (errorReporter == null) {
    errorReporter=new SimpleErrorReporter(node,message,null,null,MessageStatus.ERROR,new NodeErrorTarget());
  }
  final MessageStatus status=errorReporter.getMessageStatus();
  HighlighterMessage error=new HighlighterMessage(node,status,errorReporter.getErrorTarget().toEditorMessageTarget(),getMessageColor(status),message,checker.getOwner(node.getContainingRoot(),editorContext.getNodeEditorComponent()));
  error.setErrorReporter(errorReporter);
  return error;
}
