{
  new Thread(){
    public void run(){
      ModelAccess.instance().runReadAction(new Runnable(){
        public void run(){
          SearchResults searchResults=provider.getResults(query,getMPSProject().getComponentSafe(AdaptiveProgressMonitorFactory.class).createMonitor());
          showResults(searchResults,showOne,newTab,provider,query,isRerunnable);
        }
      }
);
    }
  }
.start();
}
