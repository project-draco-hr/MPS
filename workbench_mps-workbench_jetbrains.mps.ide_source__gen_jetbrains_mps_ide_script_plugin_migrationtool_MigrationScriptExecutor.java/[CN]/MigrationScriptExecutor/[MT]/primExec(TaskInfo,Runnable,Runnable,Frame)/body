{
  Runnable process=new Runnable(){
    @Override public void run(){
      proc.run();
      if (afterFinish != null) {
        afterFinish.run();
      }
    }
  }
;
  execAsync(process,task,frame);
}
