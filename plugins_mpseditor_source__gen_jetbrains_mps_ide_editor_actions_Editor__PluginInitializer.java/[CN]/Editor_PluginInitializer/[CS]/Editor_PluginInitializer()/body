{
  PluginFactoriesRegistry.registerPluginFactory(new AbstractPluginFactory(){
    @Override public <T>T create(    Class<T> klass){
      if (BaseApplicationPlugin.class == klass) {
        return (T)new Editor_ApplicationPlugin();
      }
      return null;
    }
  }
);
}
