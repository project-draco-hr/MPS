{
  ListSequence.fromList(this.myListeners).addElement(listener);
  this.getTextField().addKeyListener(new KeyAdapter(){
    @Override public void keyReleased(    KeyEvent p0){
      BaseFieldWithButtonComponent.this.onChange();
    }
  }
);
}
