{
  Set<SModelReference> modelsInModel=new HashSet<SModelReference>(SModelOperations.getImportedModelUIDs(myModelDescriptor.getSModel()));
  Set<SModelReference> modelsInProps=new HashSet<SModelReference>(getImportedModels());
  modelsInProps.removeAll(modelsInModel);
  for (  SModelReference modelReference : modelsInProps) {
    SModelOperations.addImportedModel(myModelDescriptor.getSModel(),modelReference);
  }
}
