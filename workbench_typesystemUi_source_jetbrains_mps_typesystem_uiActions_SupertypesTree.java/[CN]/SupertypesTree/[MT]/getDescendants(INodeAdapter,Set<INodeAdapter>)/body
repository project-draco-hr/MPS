{
  if (node == null) {
    return new HashSet<INodeAdapter>();
  }
  Set<SNode> supertypes=TypeChecker.getInstance().getSubtypingManager().collectImmediateSupertypes(node.getNode(),!myShowOnlyStrong);
  return BaseAdapter.toAdapters(supertypes);
}
