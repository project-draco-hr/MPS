{
  try {
    return new ImageIcon(StreamUtil.loadFromStream(OldGoToNextChange_Action.class.getResourceAsStream("nextOccurence.png")));
  }
 catch (  IOException e) {
    if (log.isWarnEnabled()) {
      log.warn("Couldn't load icon for OldGoToNextChange",e);
    }
    return null;
  }
}
