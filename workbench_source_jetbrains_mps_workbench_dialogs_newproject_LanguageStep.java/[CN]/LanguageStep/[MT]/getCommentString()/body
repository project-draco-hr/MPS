{
  return "<h4>Language</h4>" + "Language is a module that is more complex than a solution. <br>" + "It consists of aspect models: structure, editor, actions, typesystem, etc.<br>"+ "Languages can extend other languages, which means they can use concepts from <br>"+ "an extended language. They can derive concepts from an extended language, use<br>"+ "them as a target of a reference, and store them as children.";
}
