{
  Set<String> ancestors=getAncestorsNames();
  return ancestors.contains(SNodeUtil.concept_InterfacePart) ? ConceptKind.INTERFACE : ancestors.contains(SNodeUtil.concept_ImplementationPart) ? ConceptKind.IMPLEMENTATION : ConceptKind.NORMAL;
}
