{
  if (isNew) {
    if (myCreated == null) {
      myCreated=new HashSet<File>();
    }
  }
 else {
    if (myTouched == null) {
      myTouched=new HashSet<File>();
    }
  }
  (isNew ? myCreated : myTouched).add(file);
}
