{
  try {
    new JavaPaster().pasteJava(((SNode)MapSequence.<String,Object>fromMap(_params).get("anchorNode")),((IOperationContext)MapSequence.<String,Object>fromMap(_params).get("operationContext")),FeatureKind.CLASS_CONTENT);
  }
 catch (  Throwable t) {
    if (log.isErrorEnabled()) {
      log.error("User's action execute method failed. Action:" + "PasteAsJavaMethods",t);
    }
  }
}
