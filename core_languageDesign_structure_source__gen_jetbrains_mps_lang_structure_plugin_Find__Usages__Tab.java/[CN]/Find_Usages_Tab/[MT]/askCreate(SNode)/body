{
  Frame frame=MPSDataKeys.FRAME.getData(DataManager.getInstance().getDataContext());
  int result=JOptionPane.showConfirmDialog(frame,"Do you want to create new finder?","Create New Finder",JOptionPane.YES_NO_OPTION);
  return result == JOptionPane.YES_NO_OPTION;
}
