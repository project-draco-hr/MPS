{
  IFile container=null;
  for (  String p : path.split("!")) {
    if (container == null) {
      container=FileSystem.getFile((this.isJar(p) ? p + "!/" : p));
    }
 else {
      File cf=asFile(FileSystem.getFile(container.getPath() + p));
      container=FileSystem.getFile(cf.getPath() + "!/");
    }
  }
  this.file=container;
}
