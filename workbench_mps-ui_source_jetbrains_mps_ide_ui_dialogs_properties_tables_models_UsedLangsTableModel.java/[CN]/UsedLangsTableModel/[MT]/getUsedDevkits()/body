{
  List<ModuleReference> list=new ArrayList<ModuleReference>();
  MPSModuleRepository moduleRepository=MPSModuleRepository.getInstance();
  for (  ModuleReference tableItem : myTableItems)   if (moduleRepository.getModuleById(tableItem.getModuleId()) instanceof DevKit)   list.add(tableItem);
  return list;
}
