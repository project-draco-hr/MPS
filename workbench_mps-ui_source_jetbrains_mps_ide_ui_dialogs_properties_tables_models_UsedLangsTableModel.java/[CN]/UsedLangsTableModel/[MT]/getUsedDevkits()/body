{
  List<SModuleReference> list=new ArrayList<SModuleReference>();
  MPSModuleRepository moduleRepository=MPSModuleRepository.getInstance();
  for (  SModuleReference tableItem : myTableItems)   if (moduleRepository.getModuleById(tableItem.getModuleId()) instanceof DevKit)   list.add(tableItem);
  return list;
}
