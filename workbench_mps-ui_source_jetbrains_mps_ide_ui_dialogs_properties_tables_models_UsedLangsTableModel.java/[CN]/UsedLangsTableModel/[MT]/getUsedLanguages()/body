{
  List<ModuleReference> list=new ArrayList<ModuleReference>();
  MPSModuleRepository moduleRepository=MPSModuleRepository.getInstance();
  for (  ModuleReference tableItem : myTableItems)   if (moduleRepository.getModuleById(tableItem.getModuleId()) instanceof Language)   list.add(tableItem);
  return list;
}
