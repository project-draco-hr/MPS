{
  List<SModuleReference> list=new ArrayList<SModuleReference>();
  MPSModuleRepository moduleRepository=MPSModuleRepository.getInstance();
  for (  SModuleReference tableItem : myTableItems)   if (moduleRepository.getModule(tableItem.getModuleId()) instanceof Language)   list.add(tableItem);
  return list;
}
