{
  myUID=new SModelUID(myUID.getLongName(),stereotype);
  if (SModelStereotype.JAVA_STUB.equals(myUID.getStereotype()) || SModelStereotype.GENERATED.equals(myUID.getStereotype())) {
    myIsExternallyResolved=true;
  }
}
