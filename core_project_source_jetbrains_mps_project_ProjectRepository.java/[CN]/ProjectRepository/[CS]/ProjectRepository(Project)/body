{
  myProject=project;
  myProjectModelAccess=new ProjectModelAccess(project);
  myProjectModelAccess.addCommandListener(myCommandListener=new CommandListener(){
    @Override public void commandStarted(){
      fireCommandStarted();
    }
    @Override public void commandFinished(){
      fireCommandFinished();
    }
  }
);
  init();
}
