{
  File[] files=dir.listFiles();
  for (  File f : files) {
    if (!f.isDirectory()) {
      list.add(f);
    }
 else     if (!FileUtil.isIgnoredDir(f.getName())) {
      getAllFiles(f,list);
    }
  }
  return list;
}
