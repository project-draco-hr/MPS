{
  List<Language> result=new LinkedList<Language>();
  for (  Language lang : getAllModules(Language.class)) {
    if (lang.getDependenciesManager().getExtendedLanguages().contains(l)) {
      result.add(lang);
    }
  }
  return result;
}
