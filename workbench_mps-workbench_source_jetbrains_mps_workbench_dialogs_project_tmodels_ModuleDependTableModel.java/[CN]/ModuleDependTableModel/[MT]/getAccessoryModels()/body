{
  Set<SModelReference> set=new HashSet<SModelReference>();
  for (  DependenciesTableItem<?> tableItem : myTableItems)   if (tableItem instanceof ModelDepTableItem && tableItem.getRole() == DependenciesTableItemRole.ACCESSORY)   set.add((SModelReference)((ModelDepTableItem)tableItem).getItem());
  return set;
}
