{
  if (myCachedExecutable == null) {
    myCachedExecutable=Collections.<IntentionExecutable>singletonList(new MakeFieldTransient_Intention.IntentionImplementation());
  }
  return myCachedExecutable;
}
