{
  try {
    if (myClosed) {
      command.cancel();
    }
 else {
      command.invoke();
    }
  }
 catch (  Throwable t) {
    if (LOG.isEnabledFor(Priority.ERROR)) {
      LOG.error("Command " + command + " threw an exception.",t);
    }
  }
}
