{
  try {
    if (myClosed) {
      command.cancel();
    }
 else {
      command.invoke();
    }
  }
 catch (  Throwable t) {
    if (log.isErrorEnabled()) {
      log.error("Command " + command + " threw an exception.",t);
    }
  }
}
