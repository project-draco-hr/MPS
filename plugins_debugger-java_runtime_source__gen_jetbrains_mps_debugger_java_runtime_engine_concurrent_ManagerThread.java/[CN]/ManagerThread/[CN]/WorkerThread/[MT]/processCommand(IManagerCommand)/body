{
  try {
    if (myClosed) {
      command.cancel();
    }
 else {
      command.invoke();
    }
  }
 catch (  Throwable t) {
    if (LOG.isEnabledFor(Level.ERROR)) {
      LOG.error("Command " + command + " threw an exception.",t);
    }
  }
}
