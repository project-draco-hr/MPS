{
  if ((modelName == null || modelName.length() == 0)) {
    return null;
  }
  return SModelRepository.getInstance().getModelDescriptor(jetbrains.mps.smodel.SModelReference.fromString(modelName));
}
