{
  globalHandleText(myValues.firstElement(),new String(array,start,len));
  ModelReader7FullHandler.ElementHandler current=(myHandlersStack.empty() ? (ModelReader7FullHandler.ElementHandler)null : myHandlersStack.peek());
  if (current != null) {
    current.handleText(myValues.peek(),new String(array,start,len));
  }
}
