{
  if (value == null) {
    return null;
  }
  Iterator<BuildMps_ModuleSourcesKind> constants=ListSequence.fromList(BuildMps_ModuleSourcesKind.getConstants()).iterator();
  while (constants.hasNext()) {
    BuildMps_ModuleSourcesKind constant=constants.next();
    if (value.equals(constant.getName())) {
      return constant.getValueAsString();
    }
  }
  return null;
}
