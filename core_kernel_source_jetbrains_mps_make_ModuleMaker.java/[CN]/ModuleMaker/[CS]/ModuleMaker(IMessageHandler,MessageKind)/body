{
  this(handler == null ? new LogHandler(Logger.wrap(LogManager.getLogger(ModuleMaker.class))) : handler,level,handler != null ? new PerformanceTracer("module maker") : new NullPerformanceTracer());
}
