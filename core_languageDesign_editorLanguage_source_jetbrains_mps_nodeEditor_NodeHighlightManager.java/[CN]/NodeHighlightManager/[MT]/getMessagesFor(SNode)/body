{
  List<IEditorMessage> result=new ArrayList<IEditorMessage>();
  for (  DefaultEditorMessage msg : myMessages()) {
    if (msg.getNode() == node) {
      result.add(msg);
    }
  }
  return result;
}
