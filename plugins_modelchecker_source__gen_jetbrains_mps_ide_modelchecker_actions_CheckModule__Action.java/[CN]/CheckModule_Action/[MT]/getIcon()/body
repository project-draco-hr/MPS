{
  try {
    return new ImageIcon(StreamUtil.loadFromStream(CheckModule_Action.class.getResourceAsStream("modelChecker.png")));
  }
 catch (  IOException e) {
    if (log.isWarnEnabled()) {
      log.warn("Couldn't load icon for CheckModule",e);
    }
    return null;
  }
}
