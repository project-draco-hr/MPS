{
  String message=info != null ? "textgen error: '" + info + "' in "+ SNodeOperations.getDebugText(getSNode()) : "textgen error in " + SNodeOperations.getDebugText(getSNode());
  getBuffer().foundError(message,getSNode(),new Throwable());
}
