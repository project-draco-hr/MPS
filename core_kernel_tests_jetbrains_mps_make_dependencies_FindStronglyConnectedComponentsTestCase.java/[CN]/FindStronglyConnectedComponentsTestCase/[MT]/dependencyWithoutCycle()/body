{
  IntGraph g=new IntGraph(2);
  g.addEdges(1,0);
  List<List<IVertex>> condensation=Graphs.findStronglyConnectedComponents(g.getGraph());
  Assert.assertEquals(Arrays.asList(Arrays.asList(new IntVertex(1)),Arrays.asList(new IntVertex(0))),condensation);
}
