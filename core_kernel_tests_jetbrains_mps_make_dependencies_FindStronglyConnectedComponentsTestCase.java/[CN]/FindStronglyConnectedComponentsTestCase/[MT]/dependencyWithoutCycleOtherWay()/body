{
  IntGraph g=new IntGraph(2);
  g.addEdges(0,1);
  List<List<IVertex>> condensation=Graphs.findStronglyConnectedComponents(g.getGraph());
  Assert.assertEquals(Arrays.asList(Arrays.asList(new IntVertex(0)),Arrays.asList(new IntVertex(1))),condensation);
}
