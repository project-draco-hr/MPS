{
  IntGraph g=new IntGraph(2);
  g.addEdges(0,1);
  List<List<IVertex>> condensation=Graphs.findStronglyConnectedComponents(g.getGraph());
  Assert.assertEquals(CollectionUtil.list(CollectionUtil.list(new IntVertex(0)),CollectionUtil.list(new IntVertex(1))),condensation);
}
