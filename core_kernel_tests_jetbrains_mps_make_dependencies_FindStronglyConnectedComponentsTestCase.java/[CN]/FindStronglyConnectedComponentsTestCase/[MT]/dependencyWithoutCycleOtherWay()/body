{
  IntGraph g=new IntGraph(2);
  g.addEdges(0,1);
  List<Set<IVertex>> condensation=Graphs.getInstance().findStronglyConnectedComponents(g.getGraph());
  Assert.assertEquals(CollectionUtil.list(CollectionUtil.set(new IntVertex(0)),CollectionUtil.set(new IntVertex(1))),condensation);
}
