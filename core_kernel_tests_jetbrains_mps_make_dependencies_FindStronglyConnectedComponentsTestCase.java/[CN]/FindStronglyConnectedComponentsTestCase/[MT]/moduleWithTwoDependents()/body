{
  IntGraph g=new IntGraph(3);
  g.addEdges(1,0);
  g.addEdges(2,0);
  List<List<IVertex>> condensation=Graphs.getInstance().findStronglyConnectedComponents(g.getGraph());
  Assert.assertEquals(CollectionUtil.list(CollectionUtil.list(new IntVertex(2)),CollectionUtil.list(new IntVertex(1)),CollectionUtil.list(new IntVertex(0))),condensation);
}
