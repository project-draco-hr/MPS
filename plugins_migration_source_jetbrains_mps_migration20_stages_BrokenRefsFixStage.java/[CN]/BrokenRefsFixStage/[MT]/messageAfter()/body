{
  if (myProblems == 0)   return null;
  return "References checking and resolving finished. There are " + myProblems + " unresolved references left. \n"+ "Most probably this means that the module with a reference doesn't import the module with referenced object or the referenced object does not exist anymore. "+ "We recommend to pause the migration now and correct module dependencies by hand. "+ "You can also use the \"Fix Missing imports\" action from Logical View's context menu to add these dependencies automatically and Ctrl-R shortcut to add imports one-by-one\n"+ "After fixing module dependency, continue the migration. It will begin with this step. ";
}
