{
  boolean complex=false;
  for (  SNode item : SLinkOperations.getTargets(thisNode,"item",true)) {
    if (SNodeOperations.isInstanceOf(item,"webr.gtext.structure.GItem") && GItem_Behavior.call_isComplex_1213877298853(item)) {
      complex=true;
      break;
    }
  }
  return complex;
}
