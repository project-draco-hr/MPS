{
  if (newChild != null && !SModelUtil.isAcceptableTarget(myLinkDeclaration,newChild)) {
    LOG.error("couldn't set instance of " + newChild.getConcept().getId() + " as child '"+ SModelUtil.getLinkDeclarationRole(myLinkDeclaration)+ "' to "+ SNodeUtil.getDebugText(parentNode));
    return newChild;
  }
  if (oldChild == null) {
    parentNode.addChild(SModelUtil.getGenuineLinkRole(myLinkDeclaration),newChild);
  }
 else {
    SNodeUtil.replaceWithAnother(oldChild,newChild);
    oldChild.delete();
  }
  return newChild;
}
