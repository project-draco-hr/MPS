{
  try {
    ((Project)MapSequence.fromMap(_params).get("project")).getComponent(ChangesManager.class).dump();
  }
 catch (  Throwable t) {
    if (log.isErrorEnabled()) {
      log.error("User's action execute method failed. Action:" + "DumpChangesManager",t);
    }
  }
}
