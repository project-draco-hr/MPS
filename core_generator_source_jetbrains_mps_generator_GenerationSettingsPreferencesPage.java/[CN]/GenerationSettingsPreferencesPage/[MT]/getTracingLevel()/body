{
  return myTraceTypes.isSelected() ? GenerationSettings.TRACE_TYPES : myTraceLanguages.isSelected() ? GenerationSettings.TRACE_LANGS : myTraceSteps.isSelected() ? GenerationSettings.TRACE_STEPS : GenerationSettings.TRACE_OFF;
}
