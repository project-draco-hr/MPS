{
  myRole=role;
  if ((sourceNode instanceof jetbrains.mps.smodel.SNode) && ((jetbrains.mps.smodel.SNode)sourceNode).workingMode() == Mode.ID)   myRoleId=IdUtil.getReferenceLinkId(sourceNode.getConceptId(),myRole);
  mySourceNode=sourceNode;
}
