{
  IClassPathItem item=JavaStubs.this.createClassPathItem(location);
  if (item == null) {
    return new HashSet<SNodeDescriptor>();
  }
  Set<SNodeDescriptor> result=new HashSet<SNodeDescriptor>();
  JavaStubs.this.iterateClasspath(item,result,"");
  return result;
}
