{
  final Collection<SModule> result=new ArrayList<SModule>();
  myDepGraph.dfs(modules,new VertexVisitor<SModule>(){
    @Override public void visit(    SModule module){
      result.add(module);
    }
  }
);
  return result;
}
