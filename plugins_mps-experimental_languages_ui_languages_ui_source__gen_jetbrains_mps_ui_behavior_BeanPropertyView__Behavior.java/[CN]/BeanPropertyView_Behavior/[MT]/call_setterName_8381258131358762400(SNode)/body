{
  String prefix="set";
  String pn=SPropertyOperations.getString(thisNode,"propertyName");
  return (pn != null && pn.length() > 0 ? prefix + pn.substring(0,1).toUpperCase() + pn.substring(1) : null);
}
