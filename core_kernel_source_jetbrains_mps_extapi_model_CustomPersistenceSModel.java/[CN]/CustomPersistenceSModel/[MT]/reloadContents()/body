{
  assertCanChange();
  if (!isLoaded()) {
    return;
  }
  final SModel oldModel=myModel;
  myModel=loadSModel();
  oldModel.setModelDescriptor(null);
  myModel.setModelDescriptor(this);
  oldModel.dispose();
  setChanged(false);
  fireModelReplaced();
}
