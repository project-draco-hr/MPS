{
  for (  String property : myPropertyNames) {
    if (property == myNameProperty) {
      continue;
    }
    addLabel(property);
    addLabel(":");
    addPropertyCell(property);
    addNewLine();
  }
}
