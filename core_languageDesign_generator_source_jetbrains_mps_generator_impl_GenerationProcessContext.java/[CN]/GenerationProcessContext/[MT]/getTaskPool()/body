{
  if (myParallelTaskPool != null || !isGenerateInParallel()) {
    return myParallelTaskPool;
  }
  myParallelTaskPool=USE_PARALLEL_POOL ? new GenerationTaskPool(myProgressIndicator,myNumberOfThreads) : new SimpleGenerationTaskPool();
  return myParallelTaskPool;
}
