{
  for (  IModule m : MPSModuleRepository.getInstance().getModules(owner)) {
    m.readModels();
  }
  for (  IModule m : MPSModuleRepository.getInstance().getModules(owner)) {
    m.convert();
  }
}
