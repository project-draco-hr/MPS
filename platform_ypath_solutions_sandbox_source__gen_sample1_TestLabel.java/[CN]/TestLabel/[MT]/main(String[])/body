{
  outer:   for (  Integer foo : ListSequence.<Integer>fromArray(1,2,3)) {
    label:     do {
      none:       for (int i=0; i < 10; i=i + 1) {
        System.out.println("i=" + i);
        if (i == 5) {
          continue outer;
        }
      }
    }
 while (true);
  }
}
