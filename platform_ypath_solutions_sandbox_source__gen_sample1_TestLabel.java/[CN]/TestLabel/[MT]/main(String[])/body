{
{
    ICursor<Integer> _zCursor=CursorFactory.createCursor(ListOperations.createList(new Integer[]{1,2,3}));
    try {
      outer:       while (_zCursor.moveToNext()) {
        Integer foo=_zCursor.getCurrent();
        label:         do {
          none:           for (int i=0; i < 10; i=i + 1) {
            System.out.println("i=" + i);
            if (i == 5) {
              continue outer;
            }
          }
        }
 while (true);
      }
    }
  finally {
      _zCursor.release();
    }
  }
}
