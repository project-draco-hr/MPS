{
  outer:   for (  Integer foo : ListSequence.fromListAndArray(new ArrayList<Integer>(),1,2,3)) {
    label:     do {
      none:       for (int i=0; i < 10; i++) {
        System.out.println("i=" + i);
        if (i == 5) {
          continue outer;
        }
      }
    }
 while (true);
  }
}
