{
  if (hasErrors() || hasWarnings() || hasInfo()) {
    setDisplayInfo(NameUtil.formatNumericalString(myErrors,"error") + "/" + NameUtil.formatNumericalString(myWarnings,"warning")+ "/"+ NameUtil.formatNumericalString(myInfos,"info"));
  }
 else {
    setDisplayInfo("");
  }
}
