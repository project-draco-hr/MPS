{
  path=path.replace("\\","/");
  try {
    path=new File(path).getCanonicalPath();
  }
 catch (  IOException ignore) {
  }
  if (addSlash && !(path.endsWith("/"))) {
    path=path + "/";
  }
  return path;
}
