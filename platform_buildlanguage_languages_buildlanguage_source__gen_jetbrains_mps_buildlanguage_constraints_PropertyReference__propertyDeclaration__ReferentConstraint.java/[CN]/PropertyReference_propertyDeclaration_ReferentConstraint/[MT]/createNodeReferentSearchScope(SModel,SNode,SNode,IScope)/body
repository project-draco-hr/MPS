{
  List<SNode> result=new ArrayList<SNode>();
{
    ICursor<SNode> _zCursor17=CursorFactory.createCursor(SNodeOperations.getAncestors(enclosingNode,"jetbrains.mps.buildlanguage.structure.IPropertyHolder",true));
    try {
      while (_zCursor17.moveToNext()) {
        SNode holder=_zCursor17.getCurrent();
        ListOperations.addAllElements(result,IPropertyHolder_Behavior.call_getProperties_1200425502495(holder));
      }
    }
  finally {
      _zCursor17.release();
    }
  }
  return new SimpleSearchScope(result);
}
