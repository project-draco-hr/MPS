{
  List<SNode> result=new ArrayList<SNode>();
{
    ICursor<SNode> _zCursor14=CursorFactory.createCursor(SNodeOperations.getAncestors(enclosingNode,"jetbrains.mps.buildlanguage.structure.IPropertyHolder",true));
    try {
      while (_zCursor14.moveToNext()) {
        SNode holder=_zCursor14.getCurrent();
        ListOperations.addAllElements(result,IPropertyHolder_Behavior.call_getProperties_1200425502495(holder));
      }
    }
  finally {
      _zCursor14.release();
    }
  }
  return new SimpleSearchScope(result);
}
