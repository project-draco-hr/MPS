{
  List<EditorCell> frontier=getIndentLeafs(myCell);
  for (  EditorCell cell : frontier) {
    if (isOnNewLine(cell)) {
      newLine();
    }
    appendCell(cell);
    if (isNewLineAfter(cell)) {
      newLine();
    }
  }
  newLine();
}
