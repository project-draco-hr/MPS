{
  int size=myTestRunIdLocks.size();
  int answer=size;
  for (int i=0; i < size; ++i) {
    if (!(myTestRunIdLocks.contains(i))) {
      answer=i;
    }
  }
  myTestRunIdLocks.add(answer);
  return answer;
}
