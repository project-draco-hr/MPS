{
  Vector min=null;
  Vector max=null;
  for (  Vector p : points) {
    min=(min == null ? p : min.min(p));
    max=(max == null ? p : max.max(p));
  }
  if (min == null) {
    return null;
  }
 else {
    return new Rectangle(min,max.sub(min));
  }
}
