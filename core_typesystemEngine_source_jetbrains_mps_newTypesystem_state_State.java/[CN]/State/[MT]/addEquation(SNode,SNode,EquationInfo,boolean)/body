{
  if (!checkOnly) {
    addEquation(left,right,info);
  }
 else {
    if (myTypeCheckingContext.isSingleTypeComputation())     return;
    addBlock(new CheckEquationBlock(this,left,right,RelationKind.CHECK_EQUATION,info));
  }
}
