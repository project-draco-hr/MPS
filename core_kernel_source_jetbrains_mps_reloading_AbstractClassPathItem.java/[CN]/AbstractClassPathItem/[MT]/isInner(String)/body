{
  if (className.contains("$")) {
    for (    String part : className.split("\\$")) {
      if (part.matches("\\d+")) {
        return true;
      }
    }
  }
  if (className.contains("."))   return true;
  return false;
}
