{
  if (myModel == null) {
    updateDiskTimestamp();
    return;
  }
  final SModel result=createModel();
  super.replaceModel(new Runnable(){
    @Override public void run(){
      myModel=result;
    }
  }
);
}
