{
  if (myModel == null) {
    updateTimestamp();
    return;
  }
  final SModel result=createModel();
  super.replaceModel(new Runnable(){
    @Override public void run(){
      myModel=result;
    }
  }
);
}
