{
  if (myModel == null) {
    updateDiskTimestamp();
    return;
  }
  final SModel result=createModel();
  super.replaceModel(new Runnable(){
    public void run(){
      myModel=result;
    }
  }
);
}
