{
  assertLegalWrite();
  myCommandLevel--;
  if (myCommandLevel == 0) {
    UndoHelper.getInstance().flushCommand(p != null ? p.getComponent(MPSProject.class) : null);
    onCommandFinished();
  }
}
