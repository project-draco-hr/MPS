{
  List<Dart> darts=face.getDarts();
  for (int index=0; index < ListSequence.<Dart>fromList(darts).count(); index++) {
    int nextIndex=index + 1;
    if (nextIndex == ListSequence.<Dart>fromList(darts).count()) {
      nextIndex=0;
    }
    Node curTarget=ListSequence.<Dart>fromList(darts).getElement(index).getTarget();
    Node nextSource=ListSequence.<Dart>fromList(darts).getElement(nextIndex).getSource();
    if (curTarget != nextSource) {
      return false;
    }
  }
  return true;
}
