{
  return Sequence.fromIterable(modules).select(new ISelector<IModule,IModule>(){
    public IModule select(    IModule it){
      return (it instanceof Generator ? ((Generator)it).getSourceLanguage() : it);
    }
  }
);
}
