{
  return (ListSequence.<ModelChange>fromList(myMetadataChanges).isEmpty() ? MapSequence.fromMap(myRootToChanges).keySet() : SetSequence.<SNodeId>fromSet(MapSequence.fromMap(myRootToChanges).keySet()).concat(ListSequence.<SNodeId>fromList(ListSequence.<SNodeId>fromListAndArray(new ArrayList<SNodeId>(),null))));
}
