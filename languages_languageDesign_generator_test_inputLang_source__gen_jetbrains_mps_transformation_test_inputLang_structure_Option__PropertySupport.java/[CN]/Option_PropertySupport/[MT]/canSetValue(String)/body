{
  if (value == null) {
    return true;
  }
  Iterator<Option> constants=ListSequence.fromList(Option.getConstants()).iterator();
  while (constants.hasNext()) {
    Option constant=constants.next();
    if (value.equals(constant.getName())) {
      return true;
    }
  }
  return false;
}
