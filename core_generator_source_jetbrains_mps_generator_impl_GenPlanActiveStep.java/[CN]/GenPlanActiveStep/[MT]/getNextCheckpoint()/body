{
  Iterator<Step> it=myPlan.getSteps_().iterator();
  while (it.hasNext()) {
    if (myStep.equals(it.next())) {
      break;
    }
  }
  while (it.hasNext()) {
    Step p=it.next();
    if (p instanceof Checkpoint) {
      return (Checkpoint)p;
    }
  }
  return null;
}
