{
  Checkpoint lastSeen=null;
  for (  Step p : myPlan.getSteps_()) {
    if (myStep.equals(p)) {
      break;
    }
    if (p instanceof Checkpoint) {
      lastSeen=(Checkpoint)p;
    }
  }
  return lastSeen;
}
