{
  return new GraphAnalyzer<V>(){
    @Override public Iterable<V> forwardEdges(    V v){
      return MapSequence.fromMap(fwEdges).get(v);
    }
    @Override public Iterable<V> backwardEdges(    V v){
      return MapSequence.fromMap(bkEdges).get(v);
    }
  }
;
}
