{
  Object memento=null;
  if (myEditorContext != null) {
    memento=myEditorContext.createMemento();
  }
  action.run();
  if (myEditorContext != null) {
    myEditorContext.setMemento(memento);
  }
}
