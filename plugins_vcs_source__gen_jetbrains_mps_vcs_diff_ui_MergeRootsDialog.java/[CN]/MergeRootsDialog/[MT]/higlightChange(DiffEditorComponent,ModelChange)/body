{
  diffEditor.highlightChange(new ChangeEditorMessage(diffEditor,change,diffEditor){
    @Override public boolean isConflicted(){
      return Sequence.fromIterable(myMergeContext.getConflictedWith(getChange())).isNotEmpty();
    }
  }
);
}
