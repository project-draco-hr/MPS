{
  String stereotype=(myModel.getStereotype().equals("") ? "" : " (" + myModel.getStereotype() + ")");
  String initText=myModel.getLongName() + stereotype;
  return createFieldPanel("Model UID:",initText);
}
