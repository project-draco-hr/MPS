{
  return TransactionCacheUtils.getFromCache(BLCachedScopes.class,MultiTuple.<SNode,SNode>from(classifier,kind),new _FunctionTypes._return_P1_E0<Scope,Tuples._2<SNode,SNode>>(){
    public Scope invoke(    Tuples._2<SNode,SNode> key){
      Scope scope=scopeCreator.invoke();
      if (doFlat) {
        scope=new SimpleScope(scope.getAvailableElements(null));
      }
      return scope;
    }
  }
);
}
