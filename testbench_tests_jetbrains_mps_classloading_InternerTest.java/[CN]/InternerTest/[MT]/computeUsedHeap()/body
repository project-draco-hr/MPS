{
  System.gc();
  System.gc();
  try {
    Thread.sleep(1000);
  }
 catch (  InterruptedException e) {
  }
  System.gc();
  MemoryUsage hmu=ManagementFactory.getMemoryMXBean().getHeapMemoryUsage();
  return hmu.getUsed();
}
