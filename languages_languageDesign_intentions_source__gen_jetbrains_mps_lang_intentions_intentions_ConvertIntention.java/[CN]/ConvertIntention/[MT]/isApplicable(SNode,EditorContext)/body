{
  if (!(isApplicableHelper(node,editorContext))) {
    return false;
  }
  SNode child=editorContext.getSelectedNode();
  if (child != node) {
    if (!(childFilter(node,child,editorContext))) {
      return false;
    }
  }
  return true;
}
