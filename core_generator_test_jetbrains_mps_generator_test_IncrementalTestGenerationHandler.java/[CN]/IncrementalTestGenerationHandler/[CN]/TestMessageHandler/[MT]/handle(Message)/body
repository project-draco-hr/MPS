{
switch (msg.getKind()) {
case ERROR:
case WARNING:
    Assert.fail((msg.getKind() == MessageKind.ERROR ? "error: " : "warning: ") + msg.getText());
  break;
case INFORMATION:
System.out.println(msg.getText());
break;
}
}
