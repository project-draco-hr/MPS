{
  if (mySModel == null) {
    updateDiskTimestamp();
    return;
  }
  final SModel result=getSource().loadSModel((IModule)myModule,this);
  updateDiskTimestamp();
  replaceModel(new Runnable(){
    public void run(){
      mySModel=result;
    }
  }
);
}
