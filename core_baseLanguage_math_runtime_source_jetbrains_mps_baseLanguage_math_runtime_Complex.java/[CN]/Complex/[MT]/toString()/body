{
  if (myImagPart == 0) {
    return "" + myRealPart;
  }
  if (myRealPart == 0) {
    return myImagPart + "*I";
  }
  return "(" + myRealPart + (Math.signum(myImagPart) > 0 ? "+" : "-")+ Math.abs(myImagPart)+ "*I)";
}
