{
  if (value == null) {
    return true;
  }
  Iterator<MyKind> constants=ListSequence.fromList(MyKind.getConstants()).iterator();
  while (constants.hasNext()) {
    MyKind constant=constants.next();
    if (value.equals(constant.getName())) {
      return true;
    }
  }
  return false;
}
