{
  IFile ifile=VirtualFileUtils.toIFile(file);
  BaseSModelDescriptorWithSource emd=SModelFileTracker.getInstance().findModel(ifile);
  if (emd != null) {
    rehighlightFeatureAndDescendants(new ModelFeature(emd.getSModelReference()));
  }
}
