{
  String result=null;
  for (  Scope scope : myScopes) {
    String refText=scope.getReferenceText(contextNode,node);
    if (refText != null) {
      if (result == null) {
        result=refText;
      }
 else {
        return null;
      }
    }
  }
  return result;
}
