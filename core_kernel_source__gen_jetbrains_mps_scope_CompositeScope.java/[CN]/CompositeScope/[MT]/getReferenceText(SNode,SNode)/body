{
  String result=null;
  for (  Scope scope : myScopes) {
    String refText=scope.getReferenceText(anchor,target);
    if (refText != null) {
      if (result == null) {
        result=refText;
      }
 else {
        return null;
      }
    }
  }
  return result;
}
