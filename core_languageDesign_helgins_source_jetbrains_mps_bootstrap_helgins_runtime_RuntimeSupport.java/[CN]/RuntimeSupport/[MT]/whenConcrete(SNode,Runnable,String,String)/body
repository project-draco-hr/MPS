{
  if (argument == null)   return;
  EquationManager equationManager=myTypeChecker.getEquationManager();
  equationManager.addNewWhenConcreteEntity(NodeWrapper.createNodeWrapper(argument,equationManager),new WhenConcreteEntity(r,nodeModel,nodeId));
}
