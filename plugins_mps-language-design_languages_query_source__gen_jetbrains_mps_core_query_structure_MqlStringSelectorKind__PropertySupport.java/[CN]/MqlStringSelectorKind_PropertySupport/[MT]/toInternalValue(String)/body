{
  if (value == null) {
    return null;
  }
  Iterator<MqlStringSelectorKind> constants=ListSequence.fromList(MqlStringSelectorKind.getConstants()).iterator();
  while (constants.hasNext()) {
    MqlStringSelectorKind constant=constants.next();
    if (value.equals(constant.getName())) {
      return constant.getValueAsString();
    }
  }
  return null;
}
