{
  if (myInnerTabbedPane == null) {
    CommandProcessor.instance().executeLightweightCommand(new Runnable(){
      public void run(){
        tryToInitComponent();
      }
    }
);
  }
  return myComponent;
}
