{
  editorCell.putUserObject(EditorCell.CELL_ID,node.getId() + "_ConstantCell12892_0");
{
    Style inlineStyle=new Style(editorCell){
{
        this.set(StyleAttributes.SELECTABLE,true);
      }
    }
;
    inlineStyle.apply(editorCell);
  }
}
