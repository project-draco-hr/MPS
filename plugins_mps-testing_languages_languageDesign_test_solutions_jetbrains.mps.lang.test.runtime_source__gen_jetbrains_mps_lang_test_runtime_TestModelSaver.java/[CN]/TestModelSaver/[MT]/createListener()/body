{
  MpsRunListener listener=new MpsRunListener(){
    @Override public void testRunDone(){
      TestModelSaver.ourInstance.clean();
    }
  }
;
  RunEventsDispatcher.getInstance().addListener(listener);
  return listener;
}
