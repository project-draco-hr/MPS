{
  if (myTest == null) {
    return;
  }
  myTest.getProject().getModelAccess().runWriteInEDT(new Runnable(){
    public void run(){
      myTest.dispose();
      myTest=null;
    }
  }
);
  ModelAccess.instance().flushEventQueue();
}
