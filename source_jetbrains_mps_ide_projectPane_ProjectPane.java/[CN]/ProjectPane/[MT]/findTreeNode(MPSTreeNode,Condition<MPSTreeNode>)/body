{
  if (condition.met(root)) {
    return root;
  }
  for (  MPSTreeNode node : root) {
    MPSTreeNode result=findTreeNode(node,condition);
    if (result != null) {
      return result;
    }
  }
  return null;
}
