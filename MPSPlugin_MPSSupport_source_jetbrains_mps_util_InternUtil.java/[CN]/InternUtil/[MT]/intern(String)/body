{
  if (s == null) {
    return null;
  }
synchronized (internCache) {
    String result=internCache.tryKey(s);
    if (result != null) {
      return result;
    }
    s=new String(s);
    internCache.cacheObject(s,s);
  }
  return s;
}
