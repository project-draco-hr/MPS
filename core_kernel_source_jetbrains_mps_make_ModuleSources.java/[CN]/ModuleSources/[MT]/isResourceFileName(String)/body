{
  int extPos=fileName.lastIndexOf('.');
  return extPos != -1 && !fileName.endsWith(MPSExtentions.DOT_JAVAFILE) && !fileName.endsWith(MPSExtentions.DOT_CLASSFILE) && !AllCaches.getInstance().isCacheFile(fileName);
}
