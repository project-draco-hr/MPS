{
  int extPos=fileName.lastIndexOf('.');
  if (extPos == -1)   return false;
  return !fileName.endsWith(MPSExtentions.DOT_JAVAFILE) && !fileName.endsWith(MPSExtentions.DOT_CLASSFILE) && !AllCaches.getInstance().isCacheFile(fileName);
}
