{
  int extPos=file.getName().lastIndexOf('.');
  if (extPos == -1)   return false;
  return !file.isDirectory() && !file.getName().endsWith(MPSExtentions.DOT_JAVAFILE) && !file.getName().endsWith(MPSExtentions.DOT_CLASSFILE)&& !AllCaches.getInstance().isCacheFile(file);
}
