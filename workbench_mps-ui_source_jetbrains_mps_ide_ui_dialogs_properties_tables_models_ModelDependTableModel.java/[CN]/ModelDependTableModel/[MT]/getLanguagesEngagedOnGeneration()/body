{
  List<ModuleReference> list=new ArrayList<ModuleReference>();
  for (  DependenciesTableItem<?> item : myTableItems) {
    if (item.getItem() instanceof ModuleReference && item.getRole() == DependenciesTableItemRole.ENGAGE_ON_GEN)     list.add((ModuleReference)item.getItem());
  }
  return list;
}
