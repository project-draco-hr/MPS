{
  List<ModuleReference> list=new ArrayList<ModuleReference>();
  for (  DependenciesTableItem<?> item : myTableItems) {
    if (item instanceof ModuleDepTableItem && item.getRole() == DependenciesTableItemRole.ENGAGE_ON_GEN)     list.add(((ModuleDepTableItem)item).getItem());
  }
  return list;
}
