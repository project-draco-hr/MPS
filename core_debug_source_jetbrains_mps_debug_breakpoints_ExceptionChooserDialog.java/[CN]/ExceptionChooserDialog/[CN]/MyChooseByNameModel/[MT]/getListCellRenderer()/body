{
  ListCellRendererWrapper wrapper=new ListCellRendererWrapper<BaseSNodeDescriptor>(new DefaultListCellRenderer()){
    @Override public void customize(    JList list,    BaseSNodeDescriptor value,    int index,    boolean selected,    boolean hasFocus){
      if (value != null) {
        String fullName=getFullName(value);
        if (fullName != null) {
          setText(fullName);
        }
      }
    }
  }
;
  return wrapper;
}
