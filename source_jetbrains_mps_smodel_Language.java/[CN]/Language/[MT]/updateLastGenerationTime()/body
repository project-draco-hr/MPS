{
  long lastGenerationTime=FileUtil.getNewestFileTime(new File(getSourceDir().getAbsolutePath() + File.separator + getNamespace().replace('.',File.separatorChar)));
  long lastChangeTime=getLastChangeTime();
  myUpToDate=lastGenerationTime >= lastChangeTime;
  myUpdateLastGenerationTimeCalled=true;
}
