{
  editorCell.putUserObject(EditorCell.CELL_ID,node.getId() + "_CollectionCell4539_06");
{
    Style inlineStyle=new Style(editorCell){
{
        this.set(StyleAttributes.SELECTABLE,false);
      }
    }
;
    inlineStyle.apply(editorCell);
  }
}
