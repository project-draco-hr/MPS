{
  try {
    ProjectOptionsDialog dialog=new ProjectOptionsDialog(MPSProjectPaths_Action.this.project);
    dialog.showDialog();
  }
 catch (  Throwable t) {
    LOG.error("User's action execute method failed. Action:" + "MPSProjectPaths",t);
  }
}
