{
  ApplicationManager.getApplication().runWriteAction(new Runnable(){
    public void run(){
      if (sources) {
        removeGenSources(p);
      }
      removeClassesGen(p);
      FSRecords.invalidateCaches();
    }
  }
);
}
