{
  for (  EditorCell cell : getIndentLeafs(myCell)) {
    if (isOnNewLine(cell)) {
      newLine();
    }
    appendCell(cell);
    if (isNewLineAfter(cell)) {
      newLine();
    }
  }
  newLine();
}
