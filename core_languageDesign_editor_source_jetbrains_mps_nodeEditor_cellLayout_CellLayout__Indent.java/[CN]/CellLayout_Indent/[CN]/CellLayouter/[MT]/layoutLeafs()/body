{
  while (hasMoreCells()) {
    if (isOnNewLine(myCell,current())) {
      newLine();
    }
    appendCell(current());
    if (isNewLineAfter(myCell,current())) {
      newLine();
    }
    nextCell();
  }
  newLine();
}
