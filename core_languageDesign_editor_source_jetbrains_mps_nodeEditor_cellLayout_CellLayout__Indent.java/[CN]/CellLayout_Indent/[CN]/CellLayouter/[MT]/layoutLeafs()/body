{
  for (  EditorCell cell : getIndentLeafs(myCell)) {
    if (isOnNewLine(myCell,cell)) {
      newLine();
    }
    appendCell(cell);
    if (isNewLineAfter(myCell,cell)) {
      newLine();
    }
  }
  newLine();
}
