{
  int result=0;
  if (myOverflow) {
    result++;
  }
  while (cell != myCell) {
    if (cell.getStyle().get(StyleAttributes.INDENT_LAYOUT_INDENT)) {
      result++;
    }
    cell=cell.getParent();
  }
  return result;
}
