{
  if (files.size() == 0 || (!isProjectUnderVcs()))   return;
  myTasksQueue.invokeLater(new Runnable(){
    public void run(){
      perform(new AddOperation(files,myManager,myProject,myAddOption,recursive,silently));
    }
  }
);
}
