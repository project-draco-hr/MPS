{
  if (files.size() == 0)   return;
  for (  File f : files) {
    f.delete();
  }
  if (!isProjectUnderVcs()) {
    return;
  }
  myRemoveOperationScheduler.invokeLater(new RemoveOperation(files,myManager,myProject,myRemoveOption,silently));
  return;
}
