{
  boolean serverMode=isServerMode();
  if (application)   serverMode=!serverMode;
  return "-Xrunjdwp:transport=" + (myUseSockets ? "dt_socket" : "dt_shmem") + ",server="+ ((serverMode ? 'y' : 'n'))+ ",suspend="+ ((isSuspend() ? 'y' : 'n'))+ ",address="+ getAddress();
}
