{
  List<SModelDescriptor> result=ListSequence.fromList(new ArrayList<SModelDescriptor>());
  IClassPathItem cp=CompiledJava.this.create(root.getPath());
  CompiledJava.this.getModelDescriptors(result,root.getPath(),cp,(root.getPrefix() == null ? "" : root.getPrefix()),LanguageID.JAVA,module);
  return result;
}
