{
  if (WannabeClassConfiguration_Configuration.this.getStateObject().modelId == null || WannabeClassConfiguration_Configuration.this.getStateObject().nodeId == null) {
    return null;
  }
  final Wrappers._T<SNode> node=new Wrappers._T<SNode>();
  ModelAccess.instance().runReadAction(new Runnable(){
    public void run(){
      node.value=new SNodePointer(WannabeClassConfiguration_Configuration.this.getStateObject().modelId,WannabeClassConfiguration_Configuration.this.getStateObject().nodeId).getNode();
    }
  }
);
  return node.value;
}
