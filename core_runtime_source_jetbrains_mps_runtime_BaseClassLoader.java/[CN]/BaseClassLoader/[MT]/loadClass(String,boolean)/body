{
  Class c=findLoadedClass(name);
  if (c == null) {
    c=findClass(name);
    if (c == null) {
      try {
        c=getParent().loadClass(name);
      }
 catch (      ClassNotFoundException e) {
        throw e;
      }
    }
    if (resolve) {
      resolveClass(c);
    }
  }
  return c;
}
