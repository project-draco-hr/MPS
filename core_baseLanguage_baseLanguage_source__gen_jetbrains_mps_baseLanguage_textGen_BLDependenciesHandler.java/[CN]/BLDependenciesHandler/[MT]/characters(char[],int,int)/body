{
  BLDependenciesHandler.ElementHandler current=(handlers.empty() ? (BLDependenciesHandler.ElementHandler)null : handlers.peek());
  if (current != null) {
    current.handleText(values.peek(),new String(array,start,len));
  }
}
