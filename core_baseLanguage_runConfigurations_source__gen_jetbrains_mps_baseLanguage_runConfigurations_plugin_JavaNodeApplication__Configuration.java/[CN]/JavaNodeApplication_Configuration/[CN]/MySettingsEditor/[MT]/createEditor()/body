{
  try {
    this.myComponent=new JavaNodeApplication_Editor();
    return this.myComponent;
  }
 catch (  Throwable t) {
    Logger.getLogger(JavaNodeApplication_Configuration.class).error(t);
  }
  return new JLabel("Error during configuration editor initialization.");
}
