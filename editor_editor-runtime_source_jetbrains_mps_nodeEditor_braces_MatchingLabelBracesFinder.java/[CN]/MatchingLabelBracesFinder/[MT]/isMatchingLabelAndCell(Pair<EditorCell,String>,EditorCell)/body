{
  EditorCell matchingCell=matchingLabelAndCell.o1;
  return cell != matchingCell && cell.getSNode() == matchingCell.getSNode() && matchingLabelAndCell.o2.equals(cell.getStyle().get(StyleAttributes.MATCHING_LABEL));
}
