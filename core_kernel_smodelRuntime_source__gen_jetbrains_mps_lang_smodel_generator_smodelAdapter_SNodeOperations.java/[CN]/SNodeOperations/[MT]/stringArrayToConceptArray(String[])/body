{
  int n=conceptFqNames.length;
  SConcept[] result=new SConcept[n];
  for (int i=0; i < n; i++) {
    result[i]=SConceptRepository.getInstance().getInstanceConcept(conceptFqNames[i]);
  }
  return result;
}
