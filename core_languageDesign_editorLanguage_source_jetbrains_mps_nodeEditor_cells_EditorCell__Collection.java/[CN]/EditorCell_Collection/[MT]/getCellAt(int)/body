{
  try {
    if (usesBraces()) {
      return myEditorCells.get(number + 1);
    }
 else {
      return myEditorCells.get(number);
    }
  }
 catch (  IndexOutOfBoundsException e) {
    return null;
  }
}
