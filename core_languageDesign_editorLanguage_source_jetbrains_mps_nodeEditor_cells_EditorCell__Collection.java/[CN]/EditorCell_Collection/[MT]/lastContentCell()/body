{
  int shift=0;
  int size=0;
  if (usesBraces()) {
    shift=1;
    size=2;
  }
  if (myEditorCells.size() > size) {
    return myEditorCells.get(myEditorCells.size() - (1 + shift));
  }
  return null;
}
