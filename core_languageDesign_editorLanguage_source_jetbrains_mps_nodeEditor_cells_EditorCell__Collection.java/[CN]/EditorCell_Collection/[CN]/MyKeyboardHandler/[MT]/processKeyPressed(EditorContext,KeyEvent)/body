{
  if (!isSelected())   return false;
  if (keyEvent.isAltDown() && keyEvent.isControlDown() && keyEvent.isShiftDown()&& (keyEvent.getKeyCode() == KeyEvent.VK_F)) {
    fold();
    return true;
  }
  return false;
}
