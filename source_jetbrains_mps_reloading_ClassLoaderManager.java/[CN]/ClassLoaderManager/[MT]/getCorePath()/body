{
  String coreClasses=PathManager.getHomePath() + File.separator + "languages"+ File.separator+ "core"+ File.separator+ "classes";
  if (new File(coreClasses).exists()) {
    return new FileClassPathItem(coreClasses);
  }
  return null;
}
