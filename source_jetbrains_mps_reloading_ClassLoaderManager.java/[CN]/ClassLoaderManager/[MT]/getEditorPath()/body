{
  String editorClasses=PathManager.getHomePath() + File.separator + "languages"+ File.separator+ "bootstrap"+ File.separator+ "editorLanguage"+ File.separator+ "classes";
  if (new File(editorClasses).exists()) {
    return new FileClassPathItem(editorClasses);
  }
  return null;
}
