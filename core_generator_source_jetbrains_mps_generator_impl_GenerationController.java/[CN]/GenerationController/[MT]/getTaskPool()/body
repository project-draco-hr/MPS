{
  if (myParallelTaskPool == null) {
    myParallelTaskPool=myOptions.isGenerateInParallel() && GenerationOptions.USE_PARALLEL_POOL ? new GenerationTaskPool(myOptions.getNumberOfThreads()) : new SimpleGenerationTaskPool();
  }
  return myParallelTaskPool;
}
