{
  if (myParallelTaskPool == null) {
    myParallelTaskPool=myOptions.isGenerateInParallel() ? new GenerationTaskPool(myOptions.getNumberOfThreads(),myContext.getProject().getModelAccess()) : new SimpleGenerationTaskPool(myContext.getProject().getModelAccess());
  }
  return myParallelTaskPool;
}
