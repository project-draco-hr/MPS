{
  myTracer=tracer;
  myMonitor=monitor;
  if (startMsg != null) {
    LOG.info(startMsg);
  }
  if (myMonitor != null) {
    myMonitor.start(startMsg,stepsCount);
  }
  myTracer.push(startMsg,false);
}
