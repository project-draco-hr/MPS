{
  mySender.info(startMsg);
  myCurrentStartMsg=startMsg;
  if (!startMsg.isEmpty()) {
    myTracer.push(startMsg,true);
  }
  if (myMonitor != null) {
    myMonitor.start(startMsg,stepsCount);
  }
}
