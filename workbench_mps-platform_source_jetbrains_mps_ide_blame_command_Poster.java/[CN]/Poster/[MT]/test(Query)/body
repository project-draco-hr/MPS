{
  Performable test=new Performable(){
    @NotNull public Response perform() throws Exception {
      HttpClient client=new HttpClient();
      setTimeouts(client);
      return Command.login(client,query);
    }
  }
;
  return myExecutor.execute(test);
}
