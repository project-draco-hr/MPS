{
  PsiElement e=this;
  while (e != null && !(e instanceof MPSPsiNode)) {
    e=e.getParent();
  }
  if (e instanceof MPSPsiNode) {
    return ((MPSPsiNode)e).getTextOffset();
  }
 else {
    return 0;
  }
}
