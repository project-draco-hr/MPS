{
  NewMergeModelsDialog.MyModelTreeNode modelNode=new NewMergeModelsDialog.MyModelTreeNode();
  for (  SNodeId rootId : SetSequence.fromSet(myMergeContext.getAffectedRoots())) {
    modelNode.add(new NewMergeModelsDialog.MyRootTreeNode(rootId));
  }
  return modelNode;
}
