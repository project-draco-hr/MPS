{
  myFileFilter=new IFileFilter(){
    @Override public boolean accept(    IFile file){
      if (file.isDirectory())       return true;
      for (      String e : extension) {
        if (file.getName().toLowerCase().endsWith(e))         return true;
      }
      return false;
    }
  }
;
}
