{
  Map<K2,V> map=myMap.get(k1);
  if (map == null)   return;
  V oldV=map.remove(k2);
  if (map.isEmpty()) {
    myMap.remove(k1);
  }
  if (oldV != null) {
    myValueToK1Map.remove(oldV);
    myValueToK2Map.remove(oldV);
  }
}
