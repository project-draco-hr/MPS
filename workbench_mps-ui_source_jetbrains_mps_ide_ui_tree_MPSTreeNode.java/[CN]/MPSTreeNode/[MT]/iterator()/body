{
  if (children == null) {
    return new Iterator<MPSTreeNode>(){
      @Override public boolean hasNext(){
        return false;
      }
      @Override public MPSTreeNode next(){
        throw new IllegalStateException();
      }
      @Override public void remove(){
        throw new IllegalStateException();
      }
    }
;
  }
  return children.iterator();
}
