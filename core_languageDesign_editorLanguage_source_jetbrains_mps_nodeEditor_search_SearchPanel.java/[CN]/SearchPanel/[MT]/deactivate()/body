{
  setVisible(false);
  clearHighlight();
  if (!myCells.isEmpty()) {
    myCells.clear();
  }
  myFindResult.setText("");
  myText.setBackground(Color.white);
  revalidate();
  myEditor.requestFocus();
}
