{
  setVisible(false);
  if (myHistory.getSearches() != null) {
    myEditor.getOperationContext().getProject().getComponent(SearchHistoryComponent.class).setSearches(myHistory.getSearches());
  }
  revalidate();
  myEditor.requestFocus();
}
