{
  if (myImagPart.signum() == 0) {
    return "" + myRealPart;
  }
  if (myRealPart.signum() == 0) {
    return myImagPart + "*I";
  }
  return "(" + myRealPart + (myImagPart.signum() > 0 ? "+" : "-")+ myImagPart.abs()+ "*I)";
}
