{
  return ModelAccess.instance().runReadAction(new Computable<ItemPresentation>(){
    @Override public ItemPresentation compute(){
      return new NodeTreeModel.NodeTreeElementPresentation(myNode);
    }
  }
);
}
