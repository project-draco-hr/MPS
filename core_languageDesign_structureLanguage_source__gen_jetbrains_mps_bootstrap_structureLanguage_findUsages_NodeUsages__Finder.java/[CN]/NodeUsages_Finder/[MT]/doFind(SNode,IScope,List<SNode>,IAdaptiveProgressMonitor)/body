{
  Set<SReference> resRefs=FindUsagesManager.getInstance().findUsages(CollectionUtil.asSet(node),scope,monitor,false);
  for (  SReference reference : resRefs) {
    ListOperations.addElement(_results,reference.getSourceNode());
  }
}
