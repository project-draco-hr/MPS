{
  if ((function == null)) {
    return null;
  }
  SNode lastStatement=IMethodLike__BehaviorDescriptor.getLastStatement_idi2fhS7A.invoke(function);
  if ((lastStatement == null)) {
    return null;
  }
  SRepository repository=lastStatement.getModel().getRepository();
  return NodeRenderUtil.renderNodeAsSingleLine(lastStatement,repository);
}
