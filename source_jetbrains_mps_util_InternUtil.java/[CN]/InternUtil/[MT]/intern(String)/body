{
  if (s == null)   return null;
synchronized (internCache) {
    String result=internCache.get(s);
    if (result == null) {
      internCache.put(s,s);
      result=s;
    }
    return result;
  }
}
