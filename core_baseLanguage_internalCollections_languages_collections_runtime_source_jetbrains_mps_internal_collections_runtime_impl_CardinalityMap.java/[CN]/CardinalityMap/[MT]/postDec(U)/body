{
  Integer c=map.get(u);
  if (c != null && c > 1) {
    map.put(u,c - 1);
    return c;
  }
 else   if (c != null && c > 0) {
    map.remove(u);
    return c;
  }
 else {
    map.remove(u);
    return 0;
  }
}
