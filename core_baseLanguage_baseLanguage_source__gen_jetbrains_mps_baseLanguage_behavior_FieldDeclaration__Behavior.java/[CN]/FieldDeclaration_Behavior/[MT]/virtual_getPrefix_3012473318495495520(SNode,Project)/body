{
  CodeStyleSettings settings=CodeStyleSettings.getInstance(project);
  if (settings == null) {
    return "";
  }
  return (settings.getFieldSettings().o1 == null ? "" : settings.getFieldSettings().o1);
}
