{
synchronized (myLock) {
    myBans--;
    if (myBans != 0)     return;
    if (myReloadSession == null)     return;
    if (!myReloadSession.hasAnythingToDo())     return;
    myTimer.resume();
  }
}
