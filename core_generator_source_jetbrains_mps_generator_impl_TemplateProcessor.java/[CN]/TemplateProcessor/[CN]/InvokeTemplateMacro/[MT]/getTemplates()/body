{
  TemplateContainer rv=myTemplates;
  if (rv == null) {
synchronized (this) {
      if ((rv=myTemplates) == null) {
        rv=new TemplateContainer(myInvokedTemplate);
        rv.initialize();
        myTemplates=rv;
      }
    }
  }
  return rv;
}
