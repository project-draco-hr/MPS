{
  if (model instanceof TransientModelDescriptor)   return;
  if (!(model instanceof EditableSModelDescriptor))   return;
  if (((EditableSModelDescriptor)model).isPackaged())   return;
  if (model.getStereotype() != null) {
    if (model.getStereotype().equals(SModelStereotype.INTERNAL) || model.getStereotype().equals(SModelStereotype.INTERNAL_COPY) || SModelStereotype.isStubModelStereotype(model.getStereotype()))     return;
  }
  myModels.add(model);
}
