{
  @SuppressWarnings("unchecked") AspectDescriptorFactory<T> factory=(AspectDescriptorFactory<T>)myAspectDescriptorFactories.get(descriptorInterface);
  if (factory == null) {
    return null;
  }
  return factory.createAspectDescriptor(languageRuntime);
}
