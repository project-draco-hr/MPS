{
  if (myAspectDescriptorFactories.containsKey(descriptorInterface)) {
    throw new IllegalMonitorStateException(myAspectDescriptorFactories.get(descriptorInterface) + " AspectDescriptorFactory was already registered for descriptor interface: " + descriptorInterface);
  }
  myAspectDescriptorFactories.put(descriptorInterface,factory);
}
