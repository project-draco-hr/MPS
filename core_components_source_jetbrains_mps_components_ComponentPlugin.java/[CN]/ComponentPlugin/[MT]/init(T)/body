{
  try {
    component.init();
    myComponents.push(component);
  }
 catch (  Throwable th) {
    throw new RuntimeException("Component " + component + " initialization error",th);
  }
  return component;
}
