{
  try {
    SwingUtilities.invokeAndWait(new Runnable(){
      public void run(){
        myProject.dispose();
      }
    }
);
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
  cleanTmpProject(myProject);
}
