{
  if (myTask.requiresReadAccess()) {
    ModelAccess.instance().runReadAction(new Runnable(){
      @Override public void run(){
        runInternal();
      }
    }
);
  }
 else {
    runInternal();
  }
}
