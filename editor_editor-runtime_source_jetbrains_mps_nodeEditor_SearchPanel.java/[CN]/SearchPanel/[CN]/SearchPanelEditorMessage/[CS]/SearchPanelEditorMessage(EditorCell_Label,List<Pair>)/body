{
  super(cell.getSNode(),EditorColorsManager.getInstance().getGlobalScheme().getAttributes(EditorColors.SEARCH_RESULT_ATTRIBUTES).getBackgroundColor(),"",SearchPanel.this.myOwner);
  myCell=cell;
  myPositions=positions;
}
