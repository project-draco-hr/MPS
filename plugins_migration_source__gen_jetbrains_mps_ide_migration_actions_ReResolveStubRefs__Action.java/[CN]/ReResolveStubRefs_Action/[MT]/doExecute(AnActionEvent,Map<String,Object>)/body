{
  try {
    int fixed=MigrationHelper.stage_3_2_reResolveStubRefs(((MPSProject)MapSequence.fromMap(_params).get("project")));
    JOptionPane.showMessageDialog(((Frame)MapSequence.fromMap(_params).get("frame")),fixed + " problems fixed");
  }
 catch (  Throwable t) {
    if (log.isErrorEnabled()) {
      log.error("User's action execute method failed. Action:" + "ReResolveStubRefs",t);
    }
  }
}
