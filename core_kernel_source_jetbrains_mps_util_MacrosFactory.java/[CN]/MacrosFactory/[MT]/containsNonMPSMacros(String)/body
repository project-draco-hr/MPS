{
  return path.contains("${") && !(path.contains("${solution_descriptor}") || path.contains("${language_descriptor}") || path.contains("${module_descriptor}")|| path.contains("${module}")|| path.contains(MacrosFactory.PROJECT)|| path.contains(MacrosFactory.MPS_HOME));
}
