{
  if (editorCell == null) {
    return null;
  }
  return CellFinderUtil.findParent(editorCell,new Condition<EditorCell_Collection>(){
    @Override public boolean met(    EditorCell_Collection object){
      return object.isCollapsed() || object.isFoldable();
    }
  }
);
}
