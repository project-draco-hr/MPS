{
  if (myCachedExecutable == null) {
    myCachedExecutable=Collections.<IntentionExecutable>singletonList(new MarkAsThreadSafe_Intention.IntentionImplementation());
  }
  return myCachedExecutable;
}
