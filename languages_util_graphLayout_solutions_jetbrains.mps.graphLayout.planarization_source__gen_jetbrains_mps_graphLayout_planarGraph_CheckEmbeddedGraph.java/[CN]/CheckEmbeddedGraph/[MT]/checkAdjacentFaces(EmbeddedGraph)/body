{
  for (  Edge edge : SetSequence.<Edge>fromSet(embeddedGraph.getEdges())) {
    if (ListSequence.<Face>fromList(embeddedGraph.getAdjacentFaces(edge)).count() != 2) {
      return false;
    }
  }
  return true;
}
