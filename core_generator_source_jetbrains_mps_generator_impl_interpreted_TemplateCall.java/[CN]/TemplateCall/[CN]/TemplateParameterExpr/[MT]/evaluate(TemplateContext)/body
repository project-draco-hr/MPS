{
  if (myParameterName == null) {
    context.getEnvironment().getLogger().error(myParameterExpr.getReference(),"cannot evaluate template argument #" + (myArgIndex) + ": invalid parameter reference",GeneratorUtil.describeInput(context));
    return null;
  }
  return context.getVariable(myParameterName);
}
