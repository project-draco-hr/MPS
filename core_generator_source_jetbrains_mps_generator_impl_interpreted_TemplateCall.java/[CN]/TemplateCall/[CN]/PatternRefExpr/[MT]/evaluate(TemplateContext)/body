{
  if (myPatternVar == null) {
    context.getEnvironment().getLogger().error(myPatternExpr.getReference(),"cannot evaluate template argument #" + (myArgIndex) + ": invalid pattern reference",GeneratorUtil.describeInput(context));
    return null;
  }
 else {
    return context.getPatternVariable(myPatternVar);
  }
}
