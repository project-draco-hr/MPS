{
  EditorCell cell=foundCell;
  while (cell != null) {
    SNode node=cell.getSNode();
    if (node != null) {
      if (TraceInfo.hasTrace(node)) {
        break;
      }
    }
    cell=cell.getParent();
  }
  return cell;
}
