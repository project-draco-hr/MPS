{
  Set<Dependency> dependencies=new HashSet<Dependency>();
  for (  DependenciesTableItem<?> tableItem : myTableItems)   if (tableItem.getItem() instanceof SModuleReference && tableItem.getRole() == SDependencyScope.DEFAULT)   dependencies.add(new Dependency((SModuleReference)tableItem.getItem(),tableItem.isReExport()));
  return dependencies;
}
