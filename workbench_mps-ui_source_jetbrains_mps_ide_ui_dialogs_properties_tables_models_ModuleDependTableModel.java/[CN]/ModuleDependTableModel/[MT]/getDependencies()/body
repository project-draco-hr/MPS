{
  Set<Dependency> dependencies=new HashSet<Dependency>();
  for (  DependenciesTableItem<?> tableItem : myTableItems)   if (tableItem instanceof ModuleDepTableItem && tableItem.getRole() == DependenciesTableItemRole.DEPEND)   dependencies.add(new Dependency(((ModuleDepTableItem)tableItem).getItem(),tableItem.isReExport()));
  return dependencies;
}
