{
  Set<SModuleReference> set=new HashSet<SModuleReference>();
  for (  DependenciesTableItem tableItem : myTableItems)   if (tableItem.getModuleType().equals(type))   set.add(tableItem.getItem().getModuleRef());
  return set;
}
