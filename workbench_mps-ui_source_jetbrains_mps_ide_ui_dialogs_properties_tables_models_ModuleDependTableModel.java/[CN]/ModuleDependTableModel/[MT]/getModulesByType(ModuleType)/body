{
  Set<ModuleReference> set=new HashSet<ModuleReference>();
  for (  DependenciesTableItem<?> tableItem : myTableItems)   if (tableItem.getItem() instanceof ModuleReference && tableItem.getModuleType().equals(type))   set.add((ModuleReference)tableItem.getItem());
  return set;
}
