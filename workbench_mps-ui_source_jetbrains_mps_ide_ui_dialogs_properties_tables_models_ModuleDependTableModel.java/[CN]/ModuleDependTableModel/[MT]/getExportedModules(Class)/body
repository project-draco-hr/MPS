{
  Set<ModuleReference> set=new HashSet<ModuleReference>();
  for (  DependenciesTableItem<?> tableItem : myTableItems)   if (tableItem.getItem() instanceof ModuleReference && tableItem.getRole() == DependenciesTableItemRole.EXPORT && MPSModuleRepository.getInstance().getModuleById(((ModuleReference)tableItem.getItem()).getModuleId()).getClass().equals(klass))   set.add((ModuleReference)tableItem.getItem());
  return set;
}
