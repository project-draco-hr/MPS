{
  Set<SModuleReference> set=new LinkedHashSet<SModuleReference>();
  for (  DependenciesTableItem tableItem : myTableItems)   if (tableItem.getItem().getScope() == SDependencyScope.EXTENDS)   set.add(tableItem.getItem().getModuleRef());
  return set;
}
