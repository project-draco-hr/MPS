{
  Set<ModuleReference> set=new HashSet<ModuleReference>();
  for (  DependenciesTableItem<?> tableItem : myTableItems)   if (tableItem instanceof ModuleDepTableItem && tableItem.getRole() == DependenciesTableItemRole.EXTEND)   set.add(((ModuleDepTableItem)tableItem).getItem());
  return set;
}
