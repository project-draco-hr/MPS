{
synchronized (myLock) {
    if (myStopping) {
      notifier.pleaseStop();
    }
    myNotifier=notifier;
  }
  if (myIgnoringFilter.accept(myRequest)) {
    myRequest.getRunner().run(notifier);
  }
 else {
    ignoreRequest(notifier);
  }
}
