{
  String nodeName=IOUtil.readUTFFast(myBuffer,in), conceptFqName=IOUtil.readUTFFast(myBuffer,in), modelRef=IOUtil.readUTFFast(myBuffer,in);
  return new SNodeDescriptor(nodeName,conceptFqName,SModelReference.fromString(modelRef));
}
