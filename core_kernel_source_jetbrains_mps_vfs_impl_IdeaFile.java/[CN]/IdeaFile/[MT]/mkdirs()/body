{
  if (findVirtualFile()) {
    return myVirtualFile.isDirectory();
  }
 else {
    try {
      myVirtualFile=VfsUtil.createDirectories(myPath);
      myPath=null;
      return true;
    }
 catch (    IOException e) {
      LOG.error(e);
      return false;
    }
  }
}
