{
  if (input == null || comparator == null) {
    throw new NullPointerException();
  }
  this.input=input;
  this.comparator=(ascending ? comparator : new SortingSequence.InversedComparator<U>(comparator));
}
