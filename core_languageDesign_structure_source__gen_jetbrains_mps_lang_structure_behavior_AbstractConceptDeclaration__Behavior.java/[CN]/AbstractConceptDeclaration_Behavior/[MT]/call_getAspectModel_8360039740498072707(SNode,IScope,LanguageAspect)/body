{
  Language language=SModelUtil.getDeclaringLanguage(thisNode);
  if (language == null) {
    return null;
  }
  SModelDescriptor md=aspect.get(language);
  if (md == null) {
    return null;
  }
  return md.getSModel();
}
