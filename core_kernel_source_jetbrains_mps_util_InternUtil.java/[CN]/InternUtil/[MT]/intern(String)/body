{
  if (s == null) {
    return null;
  }
  String result=internCache.tryKey(s);
  if (result != null) {
    return result;
  }
  result=s.intern();
  internCache.cacheObject(result,result);
  return result;
}
