{
  CommandProcessor.instance().executeLightweightCommandInEDT(new Runnable(){
    public void run(){
      if (getTree() == null) {
        return;
      }
      updatePresentation();
      updateNodePresentationInTree();
      if (reloadSubTree) {
        updateSubTree();
      }
    }
  }
);
}
