{
  String uniq=_context.createUniqueName("widget",SNodeOperations.getAncestor(_context.getNode(),null,false,true));
  SessionObjects.VARIABLE_NAME.associate(_context,_context.getNode(),null,uniq);
  return uniq;
}
