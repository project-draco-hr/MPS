{
  this((t.getMessage() == null ? t.toString() : t.getMessage()),Sequence.fromIterable(Sequence.fromArray(t.getStackTrace())).select(new ISelector<StackTraceElement,String>(){
    public String select(    StackTraceElement it){
      return it.toString();
    }
  }
).toGenericArray(String.class));
}
