{
  invokeLaterIfNeeded(new Runnable(){
    public void run(){
      if (error != null) {
        myTree.setError(error);
      }
 else {
        myTree.setError(message);
      }
      myTree.rebuildLater();
    }
  }
);
}
