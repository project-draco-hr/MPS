{
  if (Sequence.IGNORE_NULL_VALUES) {
    if (t == null) {
      return null;
    }
  }
  if (Sequence.NULL_WHEN_EMPTY) {
    if (size() == 0 && (idx == 0 || idx == -1)) {
      return null;
    }
  }
  set(idx,t);
  return t;
}
