{
  boolean overflow=myMatcher.addElementsByPattern(elementsArray,pattern,getNames(),myCheckboxState,myMaximumListSizeLimit,new Computable<Boolean>(){
    public Boolean compute(){
      return myCancelled;
    }
  }
);
  if (overflow) {
    elementsArray.add(EXTRA_ELEM);
  }
}
