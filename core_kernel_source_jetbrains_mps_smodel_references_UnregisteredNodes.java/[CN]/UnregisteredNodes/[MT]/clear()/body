{
synchronized (myLock) {
    for (    SNode node : myMap.values()) {
      node.detach();
    }
    for (    SNode node : myNodesWithoutRefs) {
      node.detach();
    }
    myMap.clear();
    myNodesWithoutRefs.clear();
  }
}
