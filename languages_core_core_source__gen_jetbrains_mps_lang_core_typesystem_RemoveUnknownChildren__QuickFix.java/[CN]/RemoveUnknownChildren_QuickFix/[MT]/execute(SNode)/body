{
  for (  SNode child : ListSequence.fromList(SNodeOperations.getChildren(node))) {
    if (((SContainmentLink)RemoveUnknownChildren_QuickFix.this.getField("role")[0]).equals(child.getContainmentLink())) {
      SNodeOperations.deleteNode(child);
    }
  }
}
