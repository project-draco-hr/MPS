{
  if (Sequence.USE_NULL_SEQUENCE) {
    if (seq == null) {
      return this;
    }
  }
  for (  Object t : seq.toIterable()) {
    map.entrySet().remove(((IMapping<U,V>)t).toEntry());
  }
  return this;
}
