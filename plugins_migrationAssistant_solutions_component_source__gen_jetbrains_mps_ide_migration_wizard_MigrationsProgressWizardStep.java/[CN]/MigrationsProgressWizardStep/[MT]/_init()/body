{
  super._init();
  if (!(myInitialized)) {
    myModalityState=ModalityState.stateForComponent(myList);
    myIsComplete=false;
    myErrorContainer.setErrorDescriptor(null);
    myInitialized=true;
  }
}
