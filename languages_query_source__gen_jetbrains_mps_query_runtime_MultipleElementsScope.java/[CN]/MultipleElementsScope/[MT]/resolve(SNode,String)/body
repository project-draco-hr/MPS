{
  SNode result=null;
  for (  SNode n : nodes) {
    String name=resolveInfo.invoke(n);
    if (name.equals(str)) {
      if (result == null) {
        result=n;
      }
 else {
        return null;
      }
    }
  }
  return result;
}
