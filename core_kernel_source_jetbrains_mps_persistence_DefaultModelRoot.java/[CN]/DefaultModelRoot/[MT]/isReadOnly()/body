{
  if (super.isReadOnly()) {
    return true;
  }
  Collection<String> modelRoots=getFiles(SOURCE_ROOTS);
  return modelRoots.isEmpty() || modelRoots.size() == 1 && isLanguageAspectsSourceRoot(modelRoots.iterator().next());
}
