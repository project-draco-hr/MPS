{
  if (!(obj instanceof BaseSNodeDescriptor))   return false;
  BaseSNodeDescriptor sd=(BaseSNodeDescriptor)obj;
  return sd.myNodeName.equals(myNodeName) && EqualUtil.equals(sd.myId,myId) && sd.getModelReference().equals(getModelReference());
}
