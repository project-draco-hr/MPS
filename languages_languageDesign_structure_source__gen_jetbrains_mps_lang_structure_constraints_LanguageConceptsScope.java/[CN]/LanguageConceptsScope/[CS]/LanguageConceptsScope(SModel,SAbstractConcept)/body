{
  super((ListSequence.fromList(SModelOperations.roots(model,null)).isEmpty() ? Collections.<SNode>emptyList() : Concepts.getAvailableLanguageConcepts(ListSequence.fromList(SModelOperations.roots(model,null)).first(),metaConcept)));
}
