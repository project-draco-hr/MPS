{
  INodeAdapter current;
  if (checkThis) {
    current=this;
  }
 else {
    current=getParent();
  }
  while (current != null) {
    if (cls.isInstance(current)) {
      return (BA)current;
    }
    current=current.getParent();
  }
  return null;
}
