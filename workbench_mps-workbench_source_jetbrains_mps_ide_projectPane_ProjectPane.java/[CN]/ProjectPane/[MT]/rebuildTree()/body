{
  myUpdateQueue.queue(new Update(null){
    public void run(){
      if (getTree() == null) {
        return;
      }
      getTree().rebuildNow();
      getTree().expandProjectNode();
    }
  }
);
}
