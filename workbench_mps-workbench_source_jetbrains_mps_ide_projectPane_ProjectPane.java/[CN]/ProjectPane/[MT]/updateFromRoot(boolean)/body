{
  myUpdateQueue.queue(new AbstractUpdate(UpdateID.REBUILD){
    public void run(){
      if (getTree() == null) {
        return;
      }
      getTree().rebuildNow();
    }
  }
);
  return new ActionCallback();
}
