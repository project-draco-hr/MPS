{
  myUpdateQueue.queue(new AbstractUpdate(UpateID.REBUILD){
    public void run(){
      if (getTree() == null) {
        return;
      }
      getTree().rebuildNow();
    }
  }
);
  return new ActionCallback();
}
