{
  this.hasNext=false;
  this.next=null;
  while (inputIterator.hasNext()) {
    V tmp=selector.invoke(inputIterator.next());
    if (Sequence.IGNORE_NULL_VALUES) {
      if (tmp == null) {
        continue;
      }
    }
    this.hasNext=true;
    this.next=tmp;
    break;
  }
}
