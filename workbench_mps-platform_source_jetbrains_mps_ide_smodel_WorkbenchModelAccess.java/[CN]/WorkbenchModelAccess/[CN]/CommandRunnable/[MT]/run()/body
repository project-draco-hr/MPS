{
  ModelAccess.instance().runWriteAction(new Runnable(){
    @Override public void run(){
      incCommandLevel();
      try {
        myRunnable.run();
      }
  finally {
        decCommandLevel(myProject);
      }
    }
  }
);
}
