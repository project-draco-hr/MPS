{
  System.setProperty("mps.playRefactorings","false");
  MPSCore.getInstance().setMergeDriverMode(true);
  MergeData md=new MergeData(new File(TESTDATA_HOME,"sandbox.mps.zip"));
  md.loadCommonData();
  md.generateResultData();
  md.saveAndClose();
}
