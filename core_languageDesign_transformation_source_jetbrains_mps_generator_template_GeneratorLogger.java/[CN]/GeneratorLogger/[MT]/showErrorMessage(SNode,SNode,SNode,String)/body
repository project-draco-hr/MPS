{
  myErrorsCount++;
  if (ruleNode != null) {
    if (myFailedRules.contains(ruleNode)) {
      return;
    }
    myFailedRules.add(ruleNode);
  }
  showErrorMessage((templateNode != null ? templateNode : ruleNode),message);
  if (inputNode != null) {
    LOG.error("-- was input node: " + inputNode.getDebugText(),new NodeWithContext(inputNode,myOperationContext));
  }
  if (ruleNode != null) {
    LOG.error("-- was rule: " + ruleNode.getDebugText(),new NodeWithContext(ruleNode,myOperationContext));
  }
  if (templateNode != null) {
    LOG.error("-- was template: " + templateNode.getDebugText(),new NodeWithContext(templateNode,myOperationContext));
  }
}
