{
  int idx=path.indexOf(File.separator);
  String tmp=tmpPath + File.separator + (idx < 0 ? path : path.substring(idx + 1));
  MapSequence.fromMap(path2tmp).put(path,tmp);
  return FileSystem.getInstance().getFileByPath(tmp);
}
