{
  final DependenciesTableItem<?> item=myTableItems.get(rowIndex);
  if (columnIndex == this.getExportColumnIndex())   return item != null && item.getRole() == SDependencyScope.DEFAULT;
  return false;
}
