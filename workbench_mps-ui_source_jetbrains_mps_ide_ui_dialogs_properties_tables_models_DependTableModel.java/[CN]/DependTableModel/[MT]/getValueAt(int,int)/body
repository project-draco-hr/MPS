{
  DependenciesTableItem<?> item=myTableItems.get(rowIndex);
  if (columnIndex == this.getItemColumnIndex())   return item.getItem();
  if (columnIndex == this.getRoleColumnIndex())   return item.getRole().toString();
  if (columnIndex == this.getExportColumnIndex() && item.isReExportable()) {
    return item.isReExport();
  }
  return null;
}
