{
  List<EditorTabDescriptor> result=new ArrayList();
  result.add(new Code_TabDescriptor());
  result.add(new Structure_TabDescriptor());
  result.add(new UICode_TabDescriptor());
  result.add(new UIStructure_TabDescriptor());
  return result;
}
