{
  String token=null;
  for (  String expectedToken : ListSequence.fromList(ALL_TOKENS)) {
    if (messageString.startsWith(expectedToken)) {
      token=expectedToken;
      break;
    }
  }
  return token;
}
