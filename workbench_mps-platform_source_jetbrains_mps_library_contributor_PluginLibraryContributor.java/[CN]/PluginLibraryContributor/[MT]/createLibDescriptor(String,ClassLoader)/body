{
  if (!InternalFlag.isInternalMode()) {
    return new LibDescriptor(myFileSystem.getFile(path),classLoader);
  }
 else {
    return new LibDescriptor(myFileSystem.getFile(path),classLoader);
  }
}
