{
  System.err.println();
  System.err.print("it = (");
  Iterable iterable=withoutNulls(it);
  for (  Object elem : iterable) {
    System.err.print(elem);
    System.err.print(",");
  }
  System.err.println(")");
}
