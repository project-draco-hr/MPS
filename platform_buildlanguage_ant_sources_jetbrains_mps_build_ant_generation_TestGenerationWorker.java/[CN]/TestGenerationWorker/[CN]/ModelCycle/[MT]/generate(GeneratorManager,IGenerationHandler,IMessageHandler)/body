{
  gm.generateModels(Collections.<SModelDescriptor>singletonList(mySModel),new ModuleContext(myModule,myProject),generationHandler,new EmptyProgressIndicator(),messageHandler,isInvokeTestsSet(),true);
}
