{
  CodeStyleSettings settings=CodeStyleSettings.getInstance(ProjectHelper.toIdeaProject(project));
  if (settings == null) {
    return "";
  }
  return (settings.getLocalVariableSettings().o1 == null ? "" : settings.getLocalVariableSettings().o1);
}
