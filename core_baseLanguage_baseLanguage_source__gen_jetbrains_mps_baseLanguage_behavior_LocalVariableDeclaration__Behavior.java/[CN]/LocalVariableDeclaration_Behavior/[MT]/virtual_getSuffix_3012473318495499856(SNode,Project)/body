{
  CodeStyleSettings settings=CodeStyleSettings.getInstance(ProjectHelper.toIdeaProject(project));
  if (settings == null) {
    return "";
  }
  return (settings.getLocalVariableSettings().o2 == null ? "" : settings.getLocalVariableSettings().o2);
}
