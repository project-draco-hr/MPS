{
  if (getMessagesCache().isEmpty()) {
    return true;
  }
  for (  EditorCell cell : getMessagesCache().keySet()) {
    if (!myEditor.isValid(cell)) {
      return true;
    }
  }
  return false;
}
