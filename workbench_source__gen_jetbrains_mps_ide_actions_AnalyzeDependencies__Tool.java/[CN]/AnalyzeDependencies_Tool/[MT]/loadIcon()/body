{
  try {
    return new ImageIcon(StreamUtil.loadFromStream(URLUtil.openStream(AnalyzeDependencies_Tool.class.getResource("inspector.png"))));
  }
 catch (  IOException e) {
    if (log.isWarnEnabled()) {
      log.warn("Couldn't load icon for AnalyzeDependencies",e);
    }
    return null;
  }
}
