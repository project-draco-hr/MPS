{
  int i=0;
  for (; i < list.size(); i++) {
    T item=list.get(i);
    if (comp.compare(item,object) == 0) {
      list.set(i,object);
      return object;
    }
  }
  if (i == list.size()) {
    list.add(object);
    return object;
  }
  throw new RuntimeException("can't happen");
}
