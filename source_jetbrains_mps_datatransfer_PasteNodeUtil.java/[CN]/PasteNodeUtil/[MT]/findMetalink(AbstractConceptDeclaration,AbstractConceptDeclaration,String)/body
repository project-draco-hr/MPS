{
  for (  LinkDeclaration link : new ConceptAndSuperConceptsScope(sourceConcept).getAdapters(LinkDeclaration.class)) {
    if (SModelUtil_new.isAssignableConcept(targetConcept,link.getTarget()) && (role == null || link.getRole().equals(role))) {
      return link;
    }
  }
  return null;
}
