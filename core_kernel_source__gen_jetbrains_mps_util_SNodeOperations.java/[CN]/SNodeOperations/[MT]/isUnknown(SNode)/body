{
  SConceptId111 conceptId=node.getConceptId();
  if (conceptId == null) {
    return true;
  }
  SAbstractConcept concept=SConceptRepository.getInstance().getConcept(conceptId);
  if (concept == null) {
    return true;
  }
  SModule sourceModule=IdHelper.getModuleReference(conceptId.getLanguage()).resolve(MPSModuleRepository.getInstance());
  return sourceModule == null;
}
