{
  Iterator<? extends SNode> iterator=node.getChildren().iterator();
  SNode firstChild=(iterator.hasNext() ? iterator.next() : null);
  return new SNodeOperations.DescendantsIterable(node,(includeFirst ? node : firstChild),cond);
}
