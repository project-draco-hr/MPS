{
  String message=info != null ? "textgen error: '" + info + "' in "+ getSNode().getDebugText() : "textgen error in " + getSNode().getDebugText();
  LOG.error(message,getSNode());
  getBuffer().foundError(message);
}
