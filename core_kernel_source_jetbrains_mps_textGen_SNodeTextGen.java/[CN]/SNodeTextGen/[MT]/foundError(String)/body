{
  String message=info != null ? "textgen error: '" + info + "' in "+ SNodeUtil.getDebugText(getSNode()) : "textgen error in " + SNodeUtil.getDebugText(getSNode());
  getBuffer().foundError(message,getSNode(),new Throwable());
}
