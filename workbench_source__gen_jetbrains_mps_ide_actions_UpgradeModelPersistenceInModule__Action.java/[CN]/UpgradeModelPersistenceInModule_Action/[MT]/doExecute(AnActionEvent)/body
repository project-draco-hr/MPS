{
  try {
    PersistenceUpdater persistenceUpdater=new PersistenceUpdater();
    persistenceUpdater.upgradePersistenceInModule(UpgradeModelPersistenceInModule_Action.this.module,UpgradeModelPersistenceInModule_Action.this.mainFrame);
  }
 catch (  Throwable t) {
    if (log.isErrorEnabled()) {
      log.error("User's action execute method failed. Action:" + "UpgradeModelPersistenceInModule",t);
    }
  }
}
