{
  final SRepository repo=MPSModuleRepository.getInstance();
  return new ModelAccessHelper(repo).runReadAction(new Computable<Iterable<SModule>>(){
    @Override public Iterable<SModule> compute(){
      return repo.getModules();
    }
  }
);
}
