{
  ModelAccess.instance().runReadAction(new Runnable(){
    public void run(){
      markOldStubs();
      SModelRepository.getInstance().refreshModels();
      updateStubs();
      markNewStubs();
    }
  }
);
}
