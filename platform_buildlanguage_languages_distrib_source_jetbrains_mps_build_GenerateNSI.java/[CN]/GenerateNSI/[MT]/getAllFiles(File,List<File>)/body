{
  File[] files=dir.listFiles();
  for (  File f : files) {
    if (!f.isDirectory()) {
      list.add(f);
    }
 else     if (!f.getName().equals(".svn")) {
      getAllFiles(f,list);
    }
  }
  return list;
}
