{
  if (FileUtil.isIgnoredDir(dir.getName()))   return list;
  list.add(dir);
  File[] files=dir.listFiles();
  for (  File f : files) {
    if (f.isDirectory()) {
      getAllDirs(f,list);
    }
  }
  return list;
}
