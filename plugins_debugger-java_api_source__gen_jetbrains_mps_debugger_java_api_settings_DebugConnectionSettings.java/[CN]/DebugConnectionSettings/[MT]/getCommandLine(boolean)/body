{
  boolean serverMode=isServerMode();
  if (application) {
    serverMode=!(serverMode);
  }
  return DebugConnectionSettings.getCommandLine(serverMode,myUseSockets,isSuspend(),myHostName,myPort,mySharedMemoryAddress);
}
