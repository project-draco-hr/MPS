{
  Iterator<? extends SNode> it=parent.getChildren(role).iterator();
  for (int i=0; i < num; i++) {
    if (!(it.hasNext())) {
      return false;
    }
    it.next();
  }
  return !(it.hasNext());
}
