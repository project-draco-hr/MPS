{
  try {
    return new ImageIcon(StreamUtil.loadFromStream(URLUtil.openStream(ModelProperties_Action.class.getResource("modelProperties.png"))));
  }
 catch (  IOException e) {
    if (log.isWarnEnabled()) {
      log.warn("Couldn't load icon for ModelProperties",e);
    }
    return null;
  }
}
