{
  String prefix="my";
  if (name.length() > 2) {
    if (!(name.startsWith(prefix))) {
      name=prefix + name.substring(0,1).toUpperCase() + name.substring(1);
    }
  }
 else {
    name=prefix + name;
  }
  return name;
}
