{
  StringBuilder sb=new StringBuilder();
  classHeader(shortClassName,sb);
  methodHeader(sb);
  sb.append(source);
  methodFooter(sb);
  classFooter(sb);
  String result=sb.toString();
  return result;
}
