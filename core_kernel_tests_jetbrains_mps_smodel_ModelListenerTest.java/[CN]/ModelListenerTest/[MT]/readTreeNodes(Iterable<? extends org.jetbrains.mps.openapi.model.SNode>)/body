{
  for (  SNode n : nodes) {
    for (    SProperty p : n.getProperties()) {
      n.getProperty(p);
    }
    for (    org.jetbrains.mps.openapi.model.SReference r : n.getReferences()) {
      n.getReferenceTarget(r.getLink());
    }
    readTreeNodes(n.getChildren());
  }
}
