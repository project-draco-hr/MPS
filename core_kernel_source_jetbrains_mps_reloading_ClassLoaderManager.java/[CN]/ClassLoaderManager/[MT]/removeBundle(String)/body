{
  if (ourUseOSGI) {
    try {
      myOSGIBundles.get(moduleUID).uninstall();
    }
 catch (    BundleException e) {
      LOG.error(e);
    }
  }
 else {
    myRuntimeEnvironment.unload(moduleUID);
  }
}
