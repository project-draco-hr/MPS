{
  try {
    return new ImageIcon(StreamUtil.loadFromStream(URLUtil.openStream(Behavior_TabDescriptor.class.getResource("behavior.png"))));
  }
 catch (  IOException e) {
    if (log.isWarnEnabled()) {
      log.warn("Couldn't load icon for Behavior",e);
    }
    return null;
  }
}
