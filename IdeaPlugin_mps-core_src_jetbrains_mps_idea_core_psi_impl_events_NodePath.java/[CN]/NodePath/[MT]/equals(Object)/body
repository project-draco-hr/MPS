{
  if (that == null)   return false;
  if (that.getClass() != NodePath.class)   return false;
  return this.myPath == ((NodePath)that).myPath || (this.myPath != null && this.myPath.equals(((NodePath)that).myPath));
}
