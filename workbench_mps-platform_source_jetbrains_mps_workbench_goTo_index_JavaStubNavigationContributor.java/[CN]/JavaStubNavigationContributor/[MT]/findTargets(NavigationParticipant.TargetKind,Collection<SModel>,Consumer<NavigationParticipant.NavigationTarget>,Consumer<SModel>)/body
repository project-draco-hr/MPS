{
  for (  SModel model : scope) {
    if (!(model instanceof JavaClassStubModelDescriptor)) {
      continue;
    }
    for (    SNode root : model.getRootNodes()) {
      consumer.consume(new SNodeDescriptor(root.getName(),root));
    }
    processedConsumer.consume(model);
  }
}
