{
  try {
    return new ImageIcon(StreamUtil.loadFromStream(URLUtil.openStream(Find_Usages_TabDescriptor.class.getResource("usagesFinder.png"))));
  }
 catch (  IOException e) {
    LOG.warning("Couldn't load icon for Find Usages",e);
    return null;
  }
}
