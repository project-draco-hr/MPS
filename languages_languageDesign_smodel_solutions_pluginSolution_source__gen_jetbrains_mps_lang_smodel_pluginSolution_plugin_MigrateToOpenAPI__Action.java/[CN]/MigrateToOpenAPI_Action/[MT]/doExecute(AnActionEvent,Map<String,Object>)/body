{
  try {
    new ApiMigrationHelper().migrate(((MPSProject)MapSequence.fromMap(_params).get("project")),((Project)MapSequence.fromMap(_params).get("iproject")),((MPSProject)MapSequence.fromMap(_params).get("project")).getScope());
  }
 catch (  Throwable t) {
    if (log.isErrorEnabled()) {
      log.error("User's action execute method failed. Action:" + "MigrateToOpenAPI",t);
    }
  }
}
