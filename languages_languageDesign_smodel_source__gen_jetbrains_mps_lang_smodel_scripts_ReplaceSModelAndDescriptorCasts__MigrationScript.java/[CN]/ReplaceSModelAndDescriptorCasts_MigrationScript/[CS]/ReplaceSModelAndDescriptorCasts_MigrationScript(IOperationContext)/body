{
  super("replace casts to SModel and SModelDescriptor");
  this.addRefactoring(new AbstractMigrationRefactoring(operationContext){
    public String getName(){
      return "replace casts to SModel and SModelDescriptor";
    }
    public String getAdditionalInfo(){
      return "replace casts to SModel and SModelDescriptor";
    }
    public String getFqNameOfConceptToSearchInstances(){
      return "jetbrains.mps.baseLanguage.structure.CastExpression";
    }
    public boolean isApplicableInstanceNode(    SNode node){
      return MatchingUtil.matchNodes(SLinkOperations.getTarget(node,"type",true),_quotation_createNode_k9e3fw_a0a0a0a()) || MatchingUtil.matchNodes(SLinkOperations.getTarget(node,"type",true),_quotation_createNode_k9e3fw_a0a0a0a_0());
    }
    public void doUpdateInstanceNode(    SNode node){
      SLinkOperations.setTarget(node,"type",_quotation_createNode_k9e3fw_a0a0a0(),true);
    }
    public boolean isShowAsIntention(){
      return true;
    }
  }
);
}
