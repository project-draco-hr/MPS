{
  try {
    FileOutputStream fos=new FileOutputStream(to);
    ZipOutputStream out=new ZipOutputStream(fos);
    _zip(dir,"/",out);
    out.close();
    fos.close();
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
