{
  super("",ID);
  this.setIsInternal(false);
  this.setPopup(false);
  try {
{
      IModule module=MPSModuleRepository.getInstance().getModule(new ModuleReference("jetbrains.mps.lang.editor"));
      this.add(ActionFactory.getInstance().acquireRegisteredAction(module.getClass("jetbrains.mps.lang.editor.plugin.CopyThisDown_Action"),module.getModuleFqName()));
    }
  }
 catch (  Throwable t) {
    LOG.error("User group error",t);
  }
}
