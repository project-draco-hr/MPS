{
  super("",ID);
  this.setIsInternal(false);
  this.setPopup(false);
  try {
    this.add(ActionFactory.getInstance().getRegisteredAction(new CopyThisDown_Action()));
  }
 catch (  Throwable t) {
    LOG.error("User group error",t);
  }
}
