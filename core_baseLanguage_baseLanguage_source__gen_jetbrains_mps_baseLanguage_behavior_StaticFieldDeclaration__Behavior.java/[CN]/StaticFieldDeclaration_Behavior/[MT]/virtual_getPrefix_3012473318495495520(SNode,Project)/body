{
  CodeStyleSettings settings=CodeStyleSettings.getInstance(ProjectHelper.toIdeaProject(project));
  if (settings == null) {
    return "";
  }
  return (settings.getStaticFieldSettings().o1 == null ? "" : settings.getStaticFieldSettings().o1);
}
