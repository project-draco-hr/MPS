{
  CodeStyleSettings settings=CodeStyleSettings.getInstance(project);
  if (settings == null) {
    return "";
  }
  return (settings.getStaticFieldSettings().o2 == null ? "" : settings.getStaticFieldSettings().o2);
}
