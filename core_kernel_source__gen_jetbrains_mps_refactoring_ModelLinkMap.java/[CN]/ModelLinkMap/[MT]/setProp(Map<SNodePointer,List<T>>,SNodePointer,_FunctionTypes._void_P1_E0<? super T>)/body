{
  List<T> list=MapSequence.<SNodePointer,List<T>>fromMap(map).get(ptr);
  if (list == null) {
    return false;
  }
  ListSequence.<T>fromList(list).visitAll(new IVisitor<T>(){
    public void visit(    T it){
      f.invoke(it);
    }
  }
);
  return true;
}
