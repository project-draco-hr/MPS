{
  List<T> list=map.remove(ptr);
  if (list == null) {
    return false;
  }
  ListSequence.fromList(list).visitAll(new IVisitor<T>(){
    public void visit(    T it){
      f.invoke(it);
    }
  }
);
  return true;
}
