{
  if (value == null) {
    return;
  }
  List<T> list=MapSequence.<K,List<T>>fromMap(map).get(key);
  if (list == null) {
    MapSequence.<K,List<T>>fromMap(map).put(key,list=ListSequence.<T>fromList(new ArrayList<T>()));
  }
  ListSequence.<T>fromList(list).addElement(value);
}
