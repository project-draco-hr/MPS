{
  try {
    event.getPresentation().setEnabled(VariablesTree.MPS_DEBUGGER_VALUE.getData(event.getDataContext()) != null);
  }
 catch (  Throwable t) {
    if (log.isErrorEnabled()) {
      log.error("User's action doUpdate method failed. Action:" + "CopyValue",t);
    }
    this.disable(event.getPresentation());
  }
}
