{
  try {
    return new ImageIcon(StreamUtil.loadFromStream(URLUtil.openStream(Data_Flow_TabDescriptor.class.getResource("goRound.png"))));
  }
 catch (  IOException e) {
    LOG.warning("Couldn't load icon for Data Flow",e);
    return null;
  }
}
