{
  super.initialize(_context);
  v=new Computable<Integer>(){
    public Integer compute(){
      return 10;
    }
  }
.compute();
  x=new Computable<Integer>(){
    public Integer compute(){
      return v + 2;
    }
  }
.compute();
}
