{
  if (!willLoad()) {
    LOG.warn("The solution " + getModule() + " is asked for classloader though it does not possess a valid class loading facet.\n"+ "Try changing solution kind in the module properties dialog or adding a new idea plugin facet for this module.");
  }
}
