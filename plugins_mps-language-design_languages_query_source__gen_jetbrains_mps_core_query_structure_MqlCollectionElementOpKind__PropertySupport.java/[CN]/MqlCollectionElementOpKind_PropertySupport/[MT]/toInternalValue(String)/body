{
  if (value == null) {
    return null;
  }
  Iterator<MqlCollectionElementOpKind> constants=ListSequence.fromList(MqlCollectionElementOpKind.getConstants()).iterator();
  while (constants.hasNext()) {
    MqlCollectionElementOpKind constant=constants.next();
    if (value.equals(constant.getName())) {
      return constant.getValueAsString();
    }
  }
  return null;
}
