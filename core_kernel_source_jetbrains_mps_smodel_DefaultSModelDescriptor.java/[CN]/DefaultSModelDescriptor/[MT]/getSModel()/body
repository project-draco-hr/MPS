{
  if (mySModel == null) {
    mySModel=new SModel(getSModelReference());
    mySModel=loadModel();
    doPostLoadStuff();
    addListenersToSModel();
    mySModel.fireModelInitialized();
  }
  return mySModel;
}
