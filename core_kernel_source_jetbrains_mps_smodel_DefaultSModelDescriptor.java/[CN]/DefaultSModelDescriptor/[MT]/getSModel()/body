{
  if (mySModel == null) {
    mySModel=new SModel(getSModelReference());
    mySModel=loadModel();
    doPostLoadStuff();
    addListenersToNewModel();
    mySModel.fireModelInitialized();
  }
  return mySModel;
}
