{
  if (mySModel == null) {
    mySModel=new SModel(getModelUID());
    mySModel=loadModel();
    doPostLoadStuff();
    addListenersToNewModel();
    mySModel.fireModelInitialized();
  }
  return mySModel;
}
