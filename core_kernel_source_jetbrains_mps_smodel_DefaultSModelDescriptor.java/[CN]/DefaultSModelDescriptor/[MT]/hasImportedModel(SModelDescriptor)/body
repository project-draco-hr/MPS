{
  if (!myModelRootManager.isFindUsagesSupported())   return false;
  Set<String> strings=(mySModel == null || !SModelRepository.getInstance().isChanged(mySModel)) ? Collections.singleton(modelDescriptor.toString()) : Collections.<String>emptySet();
  if (!myModelRootManager.containsSomeString(this,strings))   return false;
  getSModel();
  if (mySModel != null) {
    return mySModel.hasImportedModel(modelDescriptor.getSModelReference());
  }
  return false;
}
