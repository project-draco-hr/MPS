{
  Generator generator=new Generator(WhatToGenerate.fromDumpInFile(new File(args[0])),new SystemOutLogger());
  try {
    generator.generate();
    System.exit(0);
  }
 catch (  Exception e) {
    generator.log(e);
    System.exit(1);
  }
}
