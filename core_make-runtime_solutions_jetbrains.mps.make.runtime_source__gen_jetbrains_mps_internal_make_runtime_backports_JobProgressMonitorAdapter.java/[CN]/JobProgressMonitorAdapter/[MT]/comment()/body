{
  String currsubtitle=(StringUtils.isEmpty(myTitle) || myTitle.startsWith(myName) ? "" : myTitle);
  String currstep=(StringUtils.isNotEmpty(myStep) ? myStep : "");
  return (StringUtils.isEmpty(currsubtitle) ? currstep : currsubtitle + ((StringUtils.isEmpty(currstep) ? "" : " " + currstep)));
}
