{
  boolean changed;
  if (exclude) {
    changed=myIncludedClassPath.remove(path);
  }
 else {
    changed=myIncludedClassPath.add(path);
  }
  setIncludedClassPath();
  if (changed) {
    save();
  }
  return changed;
}
