{
  List<ModuleReference> list=new ArrayList<ModuleReference>();
  MPSModuleRepository moduleRepository=MPSModuleRepository.getInstance();
  for (  UsedLangTableItem tableItem : myTableItems)   if (moduleRepository.getModuleById(tableItem.getItem().getModuleId()) instanceof DevKit)   list.add(tableItem.getItem());
  return list;
}
