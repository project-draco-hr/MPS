{
  Style style=new Style(cell);
  style.putAll(PackagingStyles_StyleSheet.getVariable(null));
  style.set(StyleAttributes.UNDERLINED,new AttributeCalculator<Boolean>(){
    public Boolean calculate(    EditorCell cell){
      return CustomMpsStyles_StyleSheet._StyleParameter_QueryFunction_1345_0((cell == null ? null : cell.getSNode()),(cell == null ? null : cell.getEditorContext()));
    }
  }
);
  return style;
}
