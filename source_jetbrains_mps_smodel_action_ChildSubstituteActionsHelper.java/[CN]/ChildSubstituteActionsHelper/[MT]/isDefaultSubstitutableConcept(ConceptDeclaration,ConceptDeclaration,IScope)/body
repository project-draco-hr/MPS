{
  if (!concept.hasConceptProperty(ABSTRACT,scope) && !concept.hasConceptProperty(DONT_SUBSTITUTE_BY_DEFAULT,scope)) {
    return SModelUtil.isAssignableConcept(concept,expectedConcept);
  }
  return false;
}
