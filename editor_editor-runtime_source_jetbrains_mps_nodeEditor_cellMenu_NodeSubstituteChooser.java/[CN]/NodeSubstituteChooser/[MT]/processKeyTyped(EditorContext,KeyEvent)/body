{
  if (getPatternEditor().processKeyTyped(keyEvent)) {
    if (myPopupActivated) {
      rebuildMenuEntries();
      if (!RuntimeFlags.isTestMode()) {
        relayoutPopupMenu();
      }
      tryToApplyIntelligentInput();
    }
    return true;
  }
  return false;
}
