{
  super._init();
  if (myOptions.getSolutionNamespace() == null)   myOptions.setSolutionNamespace(myOptions.getProjectName() + ".sandbox");
  myNamespace.setText(myOptions.getSolutionNamespace());
  myPath.setPath(myOptions.getSolutionPath());
  updateSolutionPath();
}
