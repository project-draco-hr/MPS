{
  Map<AbstractConceptDeclaration,Set<SNode>> map;
  if (noInheritance) {
    map=myNodesNoInheritance;
  }
 else {
    map=myNodesAll;
  }
  Set<SNode> set=map.get(acd);
  set.remove(node);
  if (set.isEmpty()) {
    map.remove(acd);
  }
}
