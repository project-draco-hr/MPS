{
  if (myProject != null) {
    if (myProject != ActionUtils.createContext(e).get(MPSProject.class)) {
      e.getPresentation().setVisible(false);
      return false;
    }
    return true;
  }
  return true;
}
