{
  if (myProject != null) {
    if (myProject != createContext(e).get(MPSProject.class)) {
      e.getPresentation().setVisible(false);
      return false;
    }
    return true;
  }
  return true;
}
