{
  return new RelationEliminator(){
    @Override boolean inRelation(    SNode left,    SNode right){
      return TypeChecker.getInstance().getSubtypingManager().isSubtype(left,right,true);
    }
  }
.doEliminate(types);
}
