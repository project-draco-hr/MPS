{
  LineNumberReader lnr=new LineNumberReader(new InputStreamReader(stream));
  while (true) {
    String line=lnr.readLine();
    if (line == null) {
      break;
    }
    this.parseLine(line);
  }
}
