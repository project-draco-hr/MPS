{
  if (node == null) {
    return null;
  }
  String result=getName(node);
  for (  SNode n : elements) {
    if (n == node) {
      continue;
    }
    String name=getName(n);
    if (name.equals(result)) {
      return null;
    }
  }
  return result;
}
