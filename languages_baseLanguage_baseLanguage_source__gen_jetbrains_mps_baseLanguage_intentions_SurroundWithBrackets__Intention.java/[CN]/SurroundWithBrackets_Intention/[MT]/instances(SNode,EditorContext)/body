{
  if (myCachedExecutable == null) {
    myCachedExecutable=Collections.<IntentionExecutable>singletonList(new SurroundWithBrackets_Intention.IntentionImplementation());
  }
  return myCachedExecutable;
}
