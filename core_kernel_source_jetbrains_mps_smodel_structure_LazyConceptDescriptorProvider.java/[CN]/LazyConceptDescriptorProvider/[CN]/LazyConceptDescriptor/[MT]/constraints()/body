{
  if (constraints != null) {
    return constraints;
  }
synchronized (lock) {
    if (constraints != null) {
      return constraints;
    }
    constraints=constraintsDescriptorProvider.getDescriptor(fqName);
    return constraints;
  }
}
