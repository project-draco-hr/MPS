{
  if (behavior != null) {
    return behavior;
  }
synchronized (lock) {
    if (behavior != null) {
      return behavior;
    }
    behavior=behaviorDescriptorProvider.getDescriptor(fqName);
    return behavior;
  }
}
