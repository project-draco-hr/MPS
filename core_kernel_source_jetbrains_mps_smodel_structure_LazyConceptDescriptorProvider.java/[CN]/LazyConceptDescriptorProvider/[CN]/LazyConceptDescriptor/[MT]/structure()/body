{
  if (structure != null) {
    return structure;
  }
synchronized (lock) {
    if (structure != null) {
      return structure;
    }
    structure=structureDescriptorProvider.getDescriptor(fqName);
    return structure;
  }
}
