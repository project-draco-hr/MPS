{
  try {
    SwingUtilities.invokeLater(new Runnable(){
      @Override public void run(){
        if (canShowListForEmptyPattern()) {
          myTextField.setText(" ");
          myTextField.setText("");
        }
      }
    }
);
  }
  finally {
    return myPanel;
  }
}
