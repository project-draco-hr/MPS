{
  Display dsp=new Display();
  Shell sh=new Shell(dsp);
  sh.setLayout(new FillLayout());
  this.createComponent(sh);
  sh.pack();
  sh.setSize(800,600);
  sh.open();
  while (!(sh.isDisposed())) {
    if (!(dsp.readAndDispatch())) {
      dsp.sleep();
    }
  }
  dsp.dispose();
}
