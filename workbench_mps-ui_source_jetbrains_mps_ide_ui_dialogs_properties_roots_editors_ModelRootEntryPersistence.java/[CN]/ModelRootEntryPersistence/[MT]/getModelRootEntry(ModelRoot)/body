{
  if (!myModelRootEntries.containsKey(modelRoot.getType()))   return null;
  ModelRootEntryFactory factory=myModelRootEntries.get(modelRoot.getType());
  return factory.getModelRootEntry(modelRoot);
}
