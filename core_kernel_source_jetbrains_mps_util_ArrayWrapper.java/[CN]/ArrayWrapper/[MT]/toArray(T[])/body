{
  if (a.length < size()) {
    return (T[])Arrays.copyOf(myArray,size(),a.getClass());
  }
  System.arraycopy(myArray,0,a,0,size());
  if (a.length > size()) {
    a[size()]=null;
  }
  return a;
}
