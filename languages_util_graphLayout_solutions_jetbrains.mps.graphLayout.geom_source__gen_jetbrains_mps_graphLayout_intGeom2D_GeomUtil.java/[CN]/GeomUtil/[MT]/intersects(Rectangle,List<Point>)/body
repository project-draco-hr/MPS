{
  Point prev=null;
  for (  Point cur : ListSequence.<Point>fromList(polyline)) {
    if (prev != null && rect.intersectsSegment(prev,cur)) {
      return true;
    }
    prev=cur;
  }
  return false;
}
