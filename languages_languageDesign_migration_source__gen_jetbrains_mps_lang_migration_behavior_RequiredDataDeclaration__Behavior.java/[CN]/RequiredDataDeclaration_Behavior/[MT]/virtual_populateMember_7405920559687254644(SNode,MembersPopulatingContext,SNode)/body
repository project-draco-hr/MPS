{
  ListSequence.fromList(SLinkOperations.getTargets(thisNode,"dependencies",true)).visitAll(new IVisitor<SNode>(){
    public void visit(    SNode it){
      context.addMember(it,null);
    }
  }
);
}
