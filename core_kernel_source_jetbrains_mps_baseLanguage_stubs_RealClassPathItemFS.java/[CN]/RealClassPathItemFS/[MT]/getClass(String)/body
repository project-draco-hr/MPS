{
  checkValidity();
  byte[] res=doWithInputStreamForClass(getBaseFile(),name,new Callback<byte[],InputStream>(){
    @Override public byte[] perform(    InputStream param) throws IOException {
      return ReadUtil.read(param);
    }
  }
);
  return res;
}
