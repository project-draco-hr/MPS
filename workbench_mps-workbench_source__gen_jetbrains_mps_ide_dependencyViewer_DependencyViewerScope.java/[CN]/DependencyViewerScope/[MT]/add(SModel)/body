{
  if (model == null) {
    return;
  }
  ModelAccess.instance().runReadAction(new Runnable(){
    public void run(){
      if (!(contains(model))) {
        ListSequence.fromList(myModels).addElement(model);
      }
    }
  }
);
}
