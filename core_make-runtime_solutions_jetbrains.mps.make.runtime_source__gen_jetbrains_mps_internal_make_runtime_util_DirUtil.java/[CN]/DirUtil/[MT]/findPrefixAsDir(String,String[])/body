{
  String dir=asDir(straighten(urlToPath(dirPath)));
  int idx=Arrays.binarySearch(sortedPaths,dir);
  int absIdx=(idx < 0 ? -1 - idx : idx);
  if (absIdx < sortedPaths.length && startsWith(dir,sortedPaths[absIdx])) {
    return absIdx;
  }
  return idx;
}
