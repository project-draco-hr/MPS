{
{
    DefaultActionGroup gTo=ActionUtils.getGroup(EditorPopup_ActionGroup.ID);
    DefaultActionGroup gWhat=ActionUtils.getGroup(EditorInternal_ActionGroup.ID);
    if (gTo == null || gWhat == null) {
      return;
    }
    gTo.add(gWhat);
  }
}
