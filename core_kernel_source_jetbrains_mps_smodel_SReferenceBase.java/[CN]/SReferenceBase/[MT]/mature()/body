{
  if (!myMature) {
    if (getSourceNode().isRegistered() && getImmatureTargetNode().isRegistered() && !(getSourceNode().isDisposed() || getImmatureTargetNode().isDisposed())) {
      makeMature();
    }
  }
  return myMature;
}
