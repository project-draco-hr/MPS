{
  char c=currentChar();
  if (c == '\"') {
    return readString();
  }
 else   if (c == '[') {
    return readList();
  }
 else   if (c == '{') {
    return readRecord();
  }
 else {
    return null;
  }
}
