{
  if (modelRoot.containsFile(FileBasedModelRoot.EXCLUDED,file.getPath()))   return Modules.ExcludeRoot;
 else   if (modelRoot.containsFile(FileBasedModelRoot.MODEL_ROOTS,file.getPath()))   return Modules.SourceRoot;
  return originalIcon;
}
