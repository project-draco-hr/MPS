{
  if (context.isSynchronous()) {
    ApplicationManager.getApplication().assertWriteAccessAllowed();
  }
  if (findVirtualFile()) {
    myVirtualFile.getChildren();
    myVirtualFile.refresh(!context.isSynchronous(),context.isRecursive());
  }
 else {
    findVirtualFile(true);
  }
}
