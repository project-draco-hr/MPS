{
  if (findVirtualFile()) {
    try {
      myVirtualFile.delete(ourRequestor());
      myVirtualFile=null;
      return true;
    }
 catch (    IOException e) {
      LOG.warn("Could not delete file: ",e);
      return false;
    }
  }
 else {
    return true;
  }
}
