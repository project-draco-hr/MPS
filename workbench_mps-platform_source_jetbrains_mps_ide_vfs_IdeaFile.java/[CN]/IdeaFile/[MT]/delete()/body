{
  if (findVirtualFile()) {
    try {
      myVirtualFile.delete(myFileSystem);
      myVirtualFile=null;
      return true;
    }
 catch (    IOException e) {
      LOG.warn("Could not delete file: ",e);
      return false;
    }
  }
 else {
    return true;
  }
}
