{
  myPool.addTask(new GenerationTask(){
    @Override public void run() throws GenerationCanceledException, GenerationFailureException {
      applyReductionRules_internal(inputNode,clonedOutputNode,blockingContext);
    }
    @Override public boolean requiresReadAccess(){
      return true;
    }
  }
);
}
