{
  super(operationContext,progressMonitor,logger,ruleManager,inputModel,outputModel,isStrict,performance);
  myPool=USE_PARALLEL_POOL ? new GenerationTaskPool(progressMonitor) : new SimpleGenerationTaskPool();
}
