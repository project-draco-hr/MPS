{
  SortedSet<Integer> set=SortedSetSequence.<Integer>fromSetAndArray(new TreeSet<Integer>(),2,1,2,5,4,3,1,5,3,2,3,1);
  this.assertIterableEquals(this.input5(),set);
  Assert.assertSame(1,SortedSetSequence.<Integer>fromSet(set).first());
  Assert.assertSame(5,SortedSetSequence.<Integer>fromSet(set).last());
}
