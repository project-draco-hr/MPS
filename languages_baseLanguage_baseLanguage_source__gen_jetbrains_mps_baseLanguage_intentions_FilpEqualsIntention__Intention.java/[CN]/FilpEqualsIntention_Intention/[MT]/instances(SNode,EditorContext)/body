{
  if (myCachedExecutable == null) {
    myCachedExecutable=Collections.<IntentionExecutable>singletonList(new FilpEqualsIntention_Intention.IntentionImplementation());
  }
  return myCachedExecutable;
}
