{
  check();
  if (work == 0) {
    return;
  }
  if (myTotal <= 0) {
    throw new IllegalStateException("call start() first");
  }
  myDone=Math.min(myTotal,Math.max(0,myDone + work));
  update();
}
