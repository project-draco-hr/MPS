{
  int version=is.readInt();
  if (version != VERSION) {
    return null;
  }
  SModel m=new TransientSModel(myModelReference);
  m.setLoading(true);
  List<SNode> roots=loadNodes(m,is);
  return roots;
}
