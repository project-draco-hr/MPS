{
  int version=is.readInt();
  if (version != VERSION) {
    return null;
  }
  model.setLoading(true);
  List<SNode> roots=loadNodes(model,is);
  for (  SNode r : roots) {
    model.addRoot(r);
  }
  return model;
}
