{
  SModelDescriptor modelDescriptor=SModelRepository.getInstance().getModelDescriptor(SModelUID.fromString(getTypesModelUID(node)));
  if (modelDescriptor == null)   return;
  SModel typesModel=modelDescriptor.getSModel();
  checkTypes(node,typesModel);
}
