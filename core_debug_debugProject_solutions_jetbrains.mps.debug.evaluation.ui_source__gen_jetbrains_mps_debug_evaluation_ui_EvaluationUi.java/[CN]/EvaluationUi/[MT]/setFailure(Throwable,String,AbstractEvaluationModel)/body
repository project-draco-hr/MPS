{
  invokeLaterIfNeeded(new Runnable(){
    public void run(){
      if (error != null) {
        myTree.setError(error,model);
      }
 else {
        myTree.setError(message,model);
      }
      myTree.rebuildLater();
    }
  }
);
}
