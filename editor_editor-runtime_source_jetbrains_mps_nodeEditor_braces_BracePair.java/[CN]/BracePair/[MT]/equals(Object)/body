{
  if (this == o) {
    return true;
  }
  if (o == null || getClass() != o.getClass()) {
    return false;
  }
  BracePair bracePair=(BracePair)o;
  return myFirstCell.equals(bracePair.myFirstCell) && mySecondCell.equals(bracePair.mySecondCell) && myHighlightInEditor == bracePair.myHighlightInEditor && myHighlightInGutter == bracePair.myHighlightInGutter;
}
