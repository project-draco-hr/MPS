{
synchronized (LOCK) {
    myTasks.add(task);
  }
  if (isProcessingAllowed()) {
    process();
  }
 else {
    ApplicationManager.getApplication().executeOnPooledThread(new Runnable(){
      public void run(){
        myTimer.resume();
      }
    }
);
  }
}
