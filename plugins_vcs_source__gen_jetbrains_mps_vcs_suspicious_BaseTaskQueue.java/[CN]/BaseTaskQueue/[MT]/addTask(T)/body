{
synchronized (LOCK) {
    myTasks.add(task);
  }
  if (isProcessingAllowed()) {
    process();
  }
 else {
    SwingUtilities.invokeLater(new Runnable(){
      public void run(){
        myTimer.resume();
      }
    }
);
  }
}
