{
  if (value == null) {
    if (!isEmpty(pointer)) {
      return delete(pointer);
    }
 else {
      return pointer;
    }
  }
 else {
    if (isEmpty(pointer)) {
      return insert(index,pointer,value);
    }
 else {
      values[pointer]=value;
      return pointer;
    }
  }
}
