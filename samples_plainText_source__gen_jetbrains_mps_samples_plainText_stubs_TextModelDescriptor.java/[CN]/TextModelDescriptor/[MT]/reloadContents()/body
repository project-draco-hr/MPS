{
  ModelAccess.assertLegalWrite();
  updateTimestamp();
  if (!(isLoaded())) {
    return;
  }
  final SModel model=loadSModel();
  setChanged(false);
  super.replaceModel(new Runnable(){
    public void run(){
      myModel=model;
    }
  }
);
}
