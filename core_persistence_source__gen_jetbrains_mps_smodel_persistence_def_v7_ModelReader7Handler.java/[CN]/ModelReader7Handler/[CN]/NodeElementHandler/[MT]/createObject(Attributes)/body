{
  boolean needLazy=fieldtoState != ModelLoadingState.FULLY_LOADED;
  return (needLazy ? new LazySNode(fieldhelper.readType(attrs.getValue("type"))) : new jetbrains.mps.smodel.SNode(fieldhelper.readType(attrs.getValue("type"))));
}
