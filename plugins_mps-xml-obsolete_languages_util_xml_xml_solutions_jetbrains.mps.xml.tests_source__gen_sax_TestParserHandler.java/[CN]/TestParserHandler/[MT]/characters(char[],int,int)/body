{
  TestParserHandler.ElementHandler current=(myHandlersStack.empty() ? (TestParserHandler.ElementHandler)null : myHandlersStack.peek());
  if (current != null) {
    current.handleText(myValues.peek(),new String(array,start,len));
  }
}
