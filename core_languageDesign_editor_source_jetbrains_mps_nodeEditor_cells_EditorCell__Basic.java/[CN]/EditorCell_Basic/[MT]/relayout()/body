{
  if (isDrawBrackets()) {
    myX+=BRACKET_WIDTH;
  }
  myX+=myGapLeft;
  relayoutImpl();
  if (isDrawBrackets()) {
    myX-=BRACKET_WIDTH;
    myWidth+=2 * BRACKET_WIDTH;
  }
  myX-=myGapLeft;
  myWidth+=myGapLeft + myGapRight;
}
