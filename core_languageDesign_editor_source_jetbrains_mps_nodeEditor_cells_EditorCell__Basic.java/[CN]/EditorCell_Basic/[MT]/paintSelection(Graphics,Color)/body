{
  g.setColor(c);
  g.fillRect(getX(),getY(),getWidth(),getHeight() - getPaddingTop() - getPaddingBottom());
  if (getEditor().hasFocus()) {
    g.setColor(c.darker());
    g.drawRect(getX(),getY(),getWidth(),getHeight());
  }
}
