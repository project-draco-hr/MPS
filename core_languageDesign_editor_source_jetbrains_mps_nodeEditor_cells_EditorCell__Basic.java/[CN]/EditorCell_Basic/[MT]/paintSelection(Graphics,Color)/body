{
  g.setColor(c);
  g.fillRect(getX(),getY(),getWidth(),getHeight() - getTopInset() - getBottomInset());
  if (getEditor().hasFocus()) {
    g.setColor(c.darker());
    g.drawRect(getX(),getY(),getWidth(),getHeight());
  }
}
