{
  ListIterator<T> e=SortedList.this.listIterator(size());
  if (o == null) {
    while (e.hasPrevious()) {
      if (e.previous() == null) {
        return e.nextIndex();
      }
    }
  }
 else {
    while (e.hasPrevious()) {
      if (myComparator.isEqual((T)o,e.previous())) {
        return e.nextIndex();
      }
    }
  }
  return -1;
}
