{
  if (ScopeUtils.comeFrom("body",thisNode,child) && SConceptOperations.isSubConceptOf(kind,"jetbrains.mps.lang.migration.structure.RequiredDataItem")) {
    return new NamedElementsScope(SLinkOperations.getTargets(thisNode,"requiresData",true));
  }
  return null;
}
