{
  LOG.assertCanWrite();
  monitor.start("Unloading classes...",1);
  try {
    monitor.step("Disposing old classes...");
    callListeners(new ListenerCaller(){
      @Override public void call(      MPSClassesListener l){
        l.onClassesUnload(null);
      }
    }
);
  }
  finally {
    monitor.done();
  }
}
