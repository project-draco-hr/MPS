{
  CodeStyleSettings settings=CodeStyleSettings.getInstance(ProjectHelper.toIdeaProject(project));
  if (settings == null) {
    return "";
  }
  return (settings.getParameterSettings().o1 == null ? "" : settings.getParameterSettings().o1);
}
