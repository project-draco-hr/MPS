{
  if (result.getDependencies() != null) {
    root.addDependencies(new RootDependencies(NameUtil.nodeFQName(outputNode),getValues(result,TextGenManager.DEPENDENCY),getValues(result,TextGenManager.EXTENDS)));
  }
  if (outputNode != null) {
    root.setModel(outputNode.getModel());
  }
}
