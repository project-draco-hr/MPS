{
  int i2=SModelStereotype.withoutStereotype(myModelReference.getModelName()).compareTo(SModelStereotype.withoutStereotype(o.myModelReference.getModelName()));
  if (i2 != 0)   return i2;
  if (myNodeId == null) {
    if (o.myNodeId == null) {
      return 0;
    }
 else {
      return 1;
    }
  }
  return myNodeId.toString().compareTo(o.myNodeId.toString());
}
