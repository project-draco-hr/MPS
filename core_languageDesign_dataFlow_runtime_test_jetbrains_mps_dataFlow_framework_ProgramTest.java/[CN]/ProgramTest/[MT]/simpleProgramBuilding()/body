{
  SimpleProgramBuilder builder=new SimpleProgramBuilder();
  builder.emitRead("x").emitWrite("y");
  Program program=builder.buildProgram();
  Assert.assertEquals("0: read x\n" + "1: write y\n",program.toString());
}
