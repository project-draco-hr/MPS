{
  if (value == null) {
    return null;
  }
  Iterator<BuildLayout_TarCompression> constants=ListSequence.fromList(BuildLayout_TarCompression.getConstants()).iterator();
  while (constants.hasNext()) {
    BuildLayout_TarCompression constant=constants.next();
    if (value.equals(constant.getName())) {
      return constant.getValueAsString();
    }
  }
  return null;
}
