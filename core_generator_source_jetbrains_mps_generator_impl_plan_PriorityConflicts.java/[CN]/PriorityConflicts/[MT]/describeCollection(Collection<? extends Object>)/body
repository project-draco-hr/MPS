{
  if (coll.isEmpty()) {
    return "";
  }
  if (coll.size() == 1) {
    return coll.iterator().next().toString();
  }
  StringBuilder sb=new StringBuilder();
  sb.append('\n');
  for (  Object r : coll) {
    sb.append('\t');
    sb.append(r);
    sb.append('\n');
  }
  return sb.toString();
}
