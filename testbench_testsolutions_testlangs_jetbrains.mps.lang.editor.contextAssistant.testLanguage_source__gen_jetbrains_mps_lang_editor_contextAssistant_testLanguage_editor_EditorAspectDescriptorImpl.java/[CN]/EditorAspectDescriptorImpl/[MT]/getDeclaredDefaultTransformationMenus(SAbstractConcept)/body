{
{
    SAbstractConcept cncpt=concept;
    Integer preIndex=indices_xbvbvu_a0e.get(cncpt);
    int switchIndex=(preIndex == null ? -1 : preIndex);
switch (switchIndex) {
case 0:
      if (true) {
        return Arrays.asList(new TransformationMenu[]{new Child_TransformationMenu(),new DefaultMenuContribution(),new Intentions(),new Parameterized()});
      }
    break;
case 1:
  if (true) {
    return Collections.<TransformationMenu>singletonList(new SubconceptOfChild_TransformationMenu());
  }
break;
default :
}
}
return Collections.<TransformationMenu>emptyList();
}
