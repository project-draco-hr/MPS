{
  final URL[] urlsArr=convertClassPathToUrls(classPath);
  return new InstrumentationClassFinder(urlsArr){
    @Override protected InputStream lookupClassAfterClasspath(    String internalClassName){
      return myClassFile2Bytes.get(internalClassName);
    }
  }
;
}
