{
switch ("") {
default :
  }
  int a=2;
switch (a) {
default :
  }
  NullableState state=NullableState.NOTNULL;
switch (state) {
default :
  }
switch (2) {
default :
  }
switch ("str") {
default :
  }
  List<f> ff=null;
switch (ff) {
default :
  }
}
