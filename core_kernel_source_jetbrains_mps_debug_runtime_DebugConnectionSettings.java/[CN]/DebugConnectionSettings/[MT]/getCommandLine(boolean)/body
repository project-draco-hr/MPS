{
  boolean serverMode=myServerMode;
  if (application)   serverMode=!serverMode;
  return "-Xrunjdwp:transport=dt_socket,server=" + ((serverMode ? 'y' : 'n')) + ",suspend="+ ((mySuspend ? 'y' : 'n'))+ ",address="+ myHostName+ ":"+ myPort;
}
