{
  boolean serverMode=myServerMode;
  if (application)   serverMode=!serverMode;
  return "-Xrunjdwp:transport=" + (myUseSockets ? "dt_socket" : "dt_shmem") + ",server="+ ((serverMode ? 'y' : 'n'))+ ",suspend="+ ((mySuspend ? 'y' : 'n'))+ ",address="+ myHostName+ ":"+ myPort;
}
