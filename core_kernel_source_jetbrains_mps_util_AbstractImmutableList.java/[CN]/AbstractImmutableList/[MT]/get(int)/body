{
  if (index < 0 || mySize >= 0 && index >= mySize) {
    throw new IndexOutOfBoundsException();
  }
  T current=myFirst;
  while (current != null) {
    if (index-- == 0) {
      return current;
    }
    current=next(current);
  }
  throw new IndexOutOfBoundsException();
}
