{
  int count=0;
  T current=myFirst;
  if (mySize == 0 || o == null) {
    return -1;
  }
  while (current != null) {
    if (current.equals(o)) {
      return count;
    }
    if (++count == mySize) {
      return -1;
    }
    current=next(current);
  }
  return -1;
}
