{
  for (  IFileGenerator fileGenerator : myFileGenerators) {
    if (originalInputNode != null && fileGenerator.overridesDefault(outputRootNode,originalInputNode)) {
      return fileGenerator;
    }
  }
  for (  IFileGenerator fileGenerator : myFileGenerators) {
    if (fileGenerator.isDefault(outputRootNode)) {
      return fileGenerator;
    }
  }
  return null;
}
