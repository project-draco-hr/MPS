{
  ThreadUtils.runInEventDispathThread(new Runnable(){
    public void run(){
      try {
        myProject.getComponent(MessageView.class).add(msg);
      }
 catch (      Exception e) {
        e.printStackTrace();
      }
    }
  }
);
}
