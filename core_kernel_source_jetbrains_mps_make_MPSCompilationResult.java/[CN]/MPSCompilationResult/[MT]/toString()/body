{
  if (!isAborted()) {
    return "compilation finished : errors: " + getErrors() + " warnings: "+ getWarnings()+ (getErrors() > 0 ? "\n" + getErrorsText() : "");
  }
 else {
    return "compilation aborted";
  }
}
