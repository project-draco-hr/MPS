{
  if (myWritable) {
    if (!myAllowEmptyText && text != null && text.length() == 0) {
      text=null;
    }
    if (isValidText(text)) {
      myNode.setProperty(myPropertyName,text);
    }
  }
}
