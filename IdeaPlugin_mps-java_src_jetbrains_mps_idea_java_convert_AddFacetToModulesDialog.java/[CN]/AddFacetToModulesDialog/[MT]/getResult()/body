{
  Set<Module> modules=new HashSet<Module>();
  if (getExitCode() == OK_EXIT_CODE) {
    for (    JCheckBox checkBox : myCheckBoxModuleMap.keySet()) {
      if (checkBox.isSelected())       modules.add(myCheckBoxModuleMap.get(checkBox));
    }
  }
  return modules;
}
