{
  ourTestMode=testMode;
  if (testMode == TestMode.CORE_TEST || testMode == TestMode.UI_TEST) {
    MPSCore.getInstance().setTestMode(true);
  }
 else   if (testMode == TestMode.NO_TEST)   MPSCore.getInstance().setTestMode(false);
}
