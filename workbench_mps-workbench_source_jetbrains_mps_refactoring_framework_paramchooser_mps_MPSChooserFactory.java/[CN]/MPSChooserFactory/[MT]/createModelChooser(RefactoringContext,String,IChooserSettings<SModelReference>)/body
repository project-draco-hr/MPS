{
  return ModelAccess.instance().runReadAction(new Computable<IChooser>(){
    @Override public IChooser compute(){
      return new BaseMPSChooser(context,paramName,new ModelChooserType(),settings);
    }
  }
);
}
