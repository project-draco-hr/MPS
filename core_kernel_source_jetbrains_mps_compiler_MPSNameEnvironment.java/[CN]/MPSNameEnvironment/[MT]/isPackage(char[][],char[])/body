{
  String pname=toQualifiedName(parentPackageName,packageName);
  return getClassPathItem().getAvailableClasses(pname).iterator().hasNext() || getClassPathItem().getSubpackages(pname).iterator().hasNext();
}
