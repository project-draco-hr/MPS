{
  super((rootId == null ? "Show Changes" : "Show Difference"),null,getDefaultDiffAction().getTemplatePresentation().getIcon());
  setExecuteOutsideCommand(true);
  myModelDifferenceDialog=modelDifferenceDialog;
  myRootId=rootId;
  copyShortcutFrom(getDefaultDiffAction());
}
