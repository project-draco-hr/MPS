{
  Project project=getOperationContext().getProject();
  return getTree() instanceof LogicalViewTree && ProjectPane.getInstance(ProjectHelper.toIdeaProject(project)).isShowPropertiesAndReferences();
}
