{
  if (!(obj instanceof SNodeDescriptor))   return false;
  SNodeDescriptor sd=(SNodeDescriptor)obj;
  return sd.myModelReference.equals(myModelReference) && EqualUtil.equals(sd.myId,myId) && sd.myConcept.equals(myConcept)&& sd.myNodeName.equals(myNodeName);
}
