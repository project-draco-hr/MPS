{
  if (!(obj instanceof SNodeDescriptor))   return false;
  SNodeDescriptor sd=(SNodeDescriptor)obj;
  return sd.myMostSignificantBits == myMostSignificantBits && sd.myLeastSignificantBits == myLeastSignificantBits && EqualUtil.equals(sd.myId,myId) && sd.myConceptFqName.equals(myConceptFqName) && sd.myNodeName.equals(myNodeName);
}
