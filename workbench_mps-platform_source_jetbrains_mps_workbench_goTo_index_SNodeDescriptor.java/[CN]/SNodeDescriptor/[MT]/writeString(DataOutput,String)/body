{
  if (s == null) {
    out.writeByte(0x70);
  }
 else {
    while (s.length() > 16384) {
      String prefix=s.substring(0,16384);
      out.writeByte(42);
      out.writeUTF(prefix);
      s=s.substring(16384);
    }
    out.writeByte(0);
    out.writeUTF(s);
  }
}
