{
  if (value == null) {
    return null;
  }
  Iterator<ShowBoundariesAreaEnum> constants=ListSequence.fromList(ShowBoundariesAreaEnum.getConstants()).iterator();
  while (constants.hasNext()) {
    ShowBoundariesAreaEnum constant=constants.next();
    if (value.equals(constant.getName())) {
      return constant.getValueAsString();
    }
  }
  return null;
}
