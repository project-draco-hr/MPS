{
  try {
    return new ImageIcon(StreamUtil.loadFromStream(CopyOldNodes_Action.class.getResourceAsStream("menu-copy.png")));
  }
 catch (  IOException e) {
    if (log.isWarnEnabled()) {
      log.warn("Couldn't load icon for CopyOldNodes",e);
    }
    return null;
  }
}
