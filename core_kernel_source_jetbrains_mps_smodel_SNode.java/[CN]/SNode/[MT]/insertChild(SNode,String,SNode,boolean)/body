{
  int index=0;
  if (anchorChild != null) {
    int anchorIndex=myChildrenWrapper.indexOf(anchorChild);
    if (!insertBefore) {
      index=anchorIndex + 1;
    }
 else {
      index=anchorIndex;
    }
  }
  insertChildAt(index,role,child);
}
