{
  List<SNode> result=new ArrayList<SNode>();
  if (includeThis) {
    result.add(this);
  }
  SNode parent=getParent();
  if (parent != null) {
    result.addAll(parent.getAncestors(true));
  }
  return result;
}
