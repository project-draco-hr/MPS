{
  assertCanChange();
  SNode p=getParent();
  if (p != null) {
    p.removeChild(this);
  }
 else   if (myOwner.getModel() != null) {
    myOwner.getModel().getModelDescriptor().removeRootNode(this);
  }
}
