{
  ModelChange.assertLegalNodeChange(getModel(),this);
  assertDisposed();
  SNode p=getParent();
  if (p != null) {
    p.removeChild(this);
  }
 else   if (myModel != null) {
    myModel.removeRootNode(this);
  }
}
