{
  for (  SReference r : myReferences) {
    if (r.getRole().equals(role)) {
      removeReferenceAt(r);
      break;
    }
  }
  if (reference != null) {
    addReference((SReference)reference);
  }
}
