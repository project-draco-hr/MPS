{
  int childDepth=0;
  for (SNode child=myFirstChild; child != null; child=child.myNextSibling) {
    int depth=child.depth();
    if (childDepth < depth) {
      childDepth=depth;
    }
  }
  return childDepth + 1;
}
