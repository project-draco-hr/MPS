{
  assertRead();
  assertDisposed();
  SNode current=this;
  while (true) {
    current.fireNodeReadAccess();
    if (current.treeParent() == null) {
      return current;
    }
 else {
      current=current.treeParent();
    }
  }
}
