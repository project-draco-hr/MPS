{
  assertCanRead();
  SNode p=getParent();
  if (p == null) {
    return null;
  }
  SNode tp=treePrevious();
  SNode ps=tp.next == null ? null : tp;
  if (ps != null) {
    myOwner.fireNodeRead(ps,false);
  }
  return ps;
}
