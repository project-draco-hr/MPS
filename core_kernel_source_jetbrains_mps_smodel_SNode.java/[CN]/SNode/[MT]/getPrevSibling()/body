{
  assertCanRead();
  SNode p=getParent();
  if (p == null) {
    return null;
  }
  SNode tp=treePrevious();
  SNode ps=tp.next == null ? null : tp;
  if (ps != null) {
    ps.fireNodeRead(false);
  }
  return ps;
}
