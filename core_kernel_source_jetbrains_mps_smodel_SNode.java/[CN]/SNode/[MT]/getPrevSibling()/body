{
  assertCanRead();
  SNode p=getParent();
  if (p == null)   return null;
  SNode tp=treePrevious();
  SNode ps=tp.next == null ? null : tp;
  if (ps != null) {
    ps.nodeRead();
    ps.fireNodeReadAccess();
  }
  return ps;
}
