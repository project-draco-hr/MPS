{
  return new AbstractList<SReference>(){
    @Override public SReference get(    int index){
      return myReferences[index];
    }
    @Override public int size(){
      return myReferences.length;
    }
  }
;
}
