{
  node.assertCanRead();
  if (myRole == null) {
    return node.treeNext();
  }
  do {
    node=node.treeNext();
  }
 while (node != null && !myRole.equals(node.getContainmentLink()));
  return node;
}
