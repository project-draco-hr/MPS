{
  if (myCheckedMethodCalls.contains(methodCall) && methodCall.getReferent(IMethodCall.BASE_METHOD_DECLARATION) == myMethodCallsToSetDecls.get(methodCall)) {
    return;
  }
  testAndFixMethodCall(methodCall,resolveTargets);
}
