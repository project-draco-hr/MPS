{
  myManager=((ModelRootManager)value);
  String representation=(myManager == null ? "" : NameUtil.shortNameFromLongName(myManager.getClassName()));
  myLabel.setText(representation);
  return myLabel;
}
