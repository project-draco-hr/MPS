{
  mapWeaveContentNodeToTemplateDeclarationContentNode(environment,outputContextNode,context.getInput());
  weaveTemplateDeclaration(outputContextNode,GeneratorUtil.createConsequenceContext(context.getInput(),null,environment,consequenceNode),environment);
  return true;
}
