{
  try {
    progStrat.setProgressIndicator(progInd);
    code.invoke(jobMon);
  }
 catch (  Throwable e) {
    e.printStackTrace();
    jobMon.reportFeedback(new IFeedback.ERROR(e.getMessage()));
  }
}
