{
  List<IntBuffer> list=null;
  for (  IntBuffer a : list) {
    a.toString();
  }
  ModelAccess.instance().runReadAction(new Runnable(){
    public void run(){
      ForEach.this.foo();
    }
  }
);
}
