{
  try {
    return new ImageIcon(StreamUtil.loadFromStream(ShowDiffFromChanges_Action.class.getResourceAsStream("diff.png")));
  }
 catch (  IOException e) {
    if (log.isWarnEnabled()) {
      log.warn("Couldn't load icon for ShowDiffFromChanges",e);
    }
    return null;
  }
}
