{
  if (node == null) {
    return false;
  }
  String role=((SNode)node).getRoleInParent();
  if (role == null) {
    return false;
  }
  return role.equals("smodelAttribute") || isOldAttributeRole(role);
}
