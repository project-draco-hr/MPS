{
  if (node == null) {
    return false;
  }
  String role=node.getParent().getRoleOf(node);
  if (role == null) {
    return false;
  }
  return role.equals("smodelAttribute") || isOldAttributeRole(role);
}
