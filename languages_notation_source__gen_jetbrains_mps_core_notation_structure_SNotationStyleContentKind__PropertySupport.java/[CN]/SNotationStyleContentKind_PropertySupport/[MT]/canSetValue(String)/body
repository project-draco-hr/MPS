{
  if (value == null) {
    return true;
  }
  Iterator<SNotationStyleContentKind> constants=ListSequence.fromList(SNotationStyleContentKind.getConstants()).iterator();
  while (constants.hasNext()) {
    SNotationStyleContentKind constant=constants.next();
    if (value.equals(constant.getName())) {
      return true;
    }
  }
  return false;
}
