{
  if (!SNodeUtil.isAccessible(selectedNode,MPSModuleRepository.getInstance())) {
    LOG.error("Selected node is disposed: node = " + selectedNode.toString() + " ; node pointer = ("+ currentSelectedReference.toString()+ ")");
    return true;
  }
  return false;
}
