{
  if (jetbrains.mps.util.SNodeOperations.isDisposed(currentSelectedNode)) {
    LOG.error("Selected node is disposed: node = " + currentSelectedNode.toString() + " ; node pointer = ("+ currentSelectedReference.toString()+ ")");
    return true;
  }
  return false;
}
